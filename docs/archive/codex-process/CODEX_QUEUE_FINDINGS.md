## MAJOR (35)

### M1. Fused-coherence and packed-MoE hard-fails fire only AFTER the full render and shard write

- **Status:** VERIFIED (3/3 adversarial lenses)
- **Finder:** fused-coherence | **Category:** risk
- **Where:** `prismaquant/export_native_compressed.py` 6940-7031, 6267-6280, 6409-6430
- **Claim:** Both new hard-fail gates (mixed fused-sibling groups, mixed packed-MoE states) live exclusively inside build_quantization_config, which main() only calls via write_config_with_quantization at line 7031 — after materialize_tensors_streaming (line 6940, the hours-long GPTQ/JSO render) and writer.finalize() (line 7000, the full sharded safetensors write). The canonicalized assignment and profile are both in hand at line 6712-6713, before any rendering, but no early coherence pre-check exists there; validate_layer_config_payload (schemas.py:210-232) checks only JSON shape.
- **Evidence:** export_native_compressed.py:6940 `tensors, hist = materialize_tensors_streaming(`; :7000 `writer.finalize()`; :7031 `write_config_with_quantization(`; the gate at :6414 `raise RuntimeError("fused-sibling coherence violation: ...` and :6269 `raise RuntimeError(f"[export-stream] FusedMoE at {fused_qname!r} has mixed "...` are reached only from build_quantization_config (:7269). Meanwhile :6712 `_assignment_for_cache = _canonicalize_assignment(_layer_config_payload_for_cache)` shows the full assignment is available at main() startup. grep for 'coherence' in main() pre-render: none.
- **Why it matters:** An incoherent layer_config (any pre-24b0f25 allocation — 'ALL AURA 27B allocs silently ran on DefaultProfile' per memory — a hand-edited config, an MSE-promotion rewrite, or the export's own runtime coercion, see next finding) burns a full 27B-class render (~hours of GPU time, ~90 GB of cache traffic, full disk write of all shards) before the export aborts. The fix's own commit message relies on the allocator gate firing 'PROACTIVELY ... rather than rely only on the export's last-line hard-fail', conceding the export gate is last-line — but the allocator gate only covers the DefaultProfile cause, not the other producers of mixed configs.
  - *refute lens* (stands): Every claim reproduces from code. The packed-MoE gate (export_native_compressed.py:6269) and fused-sibling coherence gate (:6414) live exclusively inside build_quantization_config (:6077), whose only live caller is write_config_with_quantization (:7258, calling at :7269), invoked from main() only at :7031 — after materialize_tensors_streaming (:6940) and writer.finalize() (:7000). The canonicalized assignment is available pre-render at :6712/:6888, and no coherence pre-check exists there: validate_layer_config_payload (schemas.py:210-232) is JSON-shape-only; _coerce_runtime_legal_assignment (:
  - *intent lens* (stands): All code claims verify exactly: both hard-fail gates (fused-sibling at export_native_compressed.py:6409-6430, packed-MoE at :6267-6280) live only in build_quantization_config, called only via write_config_with_quantization at main():7031 — after materialize_tensors_streaming (:6940) and writer.finalize() (:7000); the canonicalized+coerced assignment and resolved profile are available pre-render at :6712-6713/:6676 with no coherence pre-check (only a DefaultProfile warning print); validate_layer_config_payload (schemas.py:210-232) is shape-only. The late placement is NOT a recorded deliberate d
  - *impact lens* (stands): Every factual claim verifies on HEAD. Both hard-fail gates (packed-MoE mixed states at export_native_compressed.py:6269, fused-sibling coherence at :6409-6430) are reachable only via build_quantization_config (:6077), whose sole call site is write_config_with_quantization (:7269), invoked in main() at :7031 — strictly after materialize_tensors_streaming (:6940, the render) and writer.finalize() (:7000, the full sharded safetensors write). The canonicalized assignment and resolved profile are in hand pre-render (:6712-6713, :6888-6893) and no coherence pre-check exists there (only JSON-shape va

### M2. Silent DefaultProfile fallback survives in production entrypoints outside allocator/export

- **Status:** VERIFIED (3/3 adversarial lenses)
- **Finder:** fused-coherence | **Category:** risk
- **Where:** `prismaquant/validate_assignments_kl.py` 841-844
- **Claim:** The 24b0f25 gate covers only allocator (hard error) and export main (warn). build_production_cache.py:291-294, production_recache.py:481-484, validate_assignments_kl.py:841-844 and kl_sensitivity_probe.py:2617-2620 all do `try: profile = detect_profile(args.model)\nexcept Exception: profile = DefaultProfile()` with no warning and no gate — and detect_profile itself silently returns DefaultProfile when config.json is missing/unreadable or the arch is unregistered (registry.py:73-83, 126), so the except-clause is not even the main silent path. In validate_assignments_kl the profile drives bpp accounting for the measured frontier (`_profile_excludes_bpp_name` :91-97 uses is_pinned_name + source_passthrough_prefixes), i.e. the (bpp, KL) coordinates the validated-surrogate kneedle selects on.
- **Evidence:** validate_assignments_kl.py:841-844 `try:\n    profile = detect_profile(args.model)\nexcept Exception:\n    profile = DefaultProfile()`; identical pattern at build_production_cache.py:291-294 and production_recache.py:481-484; registry.py:126 `return DefaultProfile(architectures=archs)` with :74-83 silently swallowing missing/corrupt config.json (`if cfg_path.exists(): ... except (json.JSONDecodeError, OSError): pass`).
- **Why it matters:** The incident class being fixed was 'silently proceeded on DefaultProfile'. The ship-selection path (validate_assignments_kl feeding select_validated_frontier) and both cache builders can still do exactly that with zero log line — e.g. an unregistered arch or a mistyped/not-yet-downloaded model path mis-scopes the reported bpp (house rule: bpp over quantizable params only) and pinned-name skipping for the frontier the artifact is selected from.
  - *refute lens* (stands): All cited evidence reproduces exactly. (1) git show 24b0f25 --stat: only allocator.py and export_native_compressed.py changed — the gate covers allocator (hard error, allocator.py:1213-1229) and export (warn, :6675-6687) only. (2) The four cited sites (validate_assignments_kl.py:841-844, build_production_cache.py:291-294, production_recache.py:481-484, kl_sensitivity_probe.py:2617-2620) all contain the bare try/except→DefaultProfile() with no warning, no gate, and no profile-name log line. (3) registry.py:74-83 silently swallows missing/corrupt config.json and :126 is exactly `return DefaultPr
  - *intent lens* (stands): NOT a deliberate recorded decision — genuinely new. (1) All cited code verified verbatim: four production entrypoints (validate_assignments_kl.py:841-844, build_production_cache.py:291-294, production_recache.py:481-484, kl_sensitivity_probe.py:2617-2620) silently fall back to DefaultProfile, and registry.py:74-83,126 confirms detect_profile itself silently returns DefaultProfile on missing/corrupt config.json (e.g. HF hub-id model args) or unregistered arch — the except-clause is indeed not the main silent path. (2) git show 24b0f25 confirms the gate touched ONLY allocator.py + export_native_
  - *impact lens* (stands): Verified accurate. All four cited sites contain the silent `try: detect_profile(args.model) except: DefaultProfile()` with no log line, and registry.py:74-83/126 confirm the main silent path (missing/corrupt config.json, unregistered arch, or model given as HF id instead of local dir) never even raises. git show 24b0f25 confirms the gate covers only allocator (hard error) and export (warn). Impact: build_production_cache and production_recache are DEFAULT pipeline stages (PRODUCTION_CACHE=1/PRODUCTION_RECACHE=1, run-pipeline.sh:463/855 etc.); the allocator's upstream gate only partially mitiga

### M3. Zero test coverage for both gates added in 24b0f25

- **Status:** VERIFIED (3/3 adversarial lenses)
- **Finder:** fused-coherence | **Category:** test-gap
- **Where:** `prismaquant/export_native_compressed.py` 6409-6430
- **Claim:** Commit 24b0f25 touched only allocator.py and export_native_compressed.py (`git show 24b0f25 --stat`: 2 files). No test exercises the new fused-sibling mixed-format RuntimeError, the crash@load vs silent-corruption classification, the allocator's DefaultProfile multi-format hard error, or the --allow-default-profile escape. greps for 'fused-sibling coherence', 'crash@load', 'silent-corruption', 'allow_default_profile' across tests/ return nothing. The pre-existing test_packed_moe_mixed_format_rejected covers only the (older) packed-MoE branch.
- **Evidence:** `git show 24b0f25 --stat` → ` prismaquant/allocator.py | 59 +++ ; prismaquant/export_native_compressed.py | 99 +++` (no tests/); `grep -rln "fused-sibling coherence\|crash@load\|silent-corruption\|allow_default_profile" tests/` → empty. The replaced code was a one-line silent skip: 24b0f25 diff `- continue  # mixed formats → caller's bug; don't emit` — the exact behavior that shipped the broken artifact.
- **Why it matters:** This gate is the backstop for an artifact-destroying failure mode that already shipped an unservable 27B checkpoint. Its predecessor was a silent `continue`; without a regression test, a future refactor of build_quantization_config (a ~7300-line file under active churn — df12b76 touched it the next day) can silently restore the skip and nothing would catch it before another multi-hour export ships corrupt.
  - *refute lens* (stands): The finding fully reproduces. (1) `git show 24b0f25 --stat` confirms only allocator.py and export_native_compressed.py were touched — no tests. (2) All four greps for gate-identifying strings in tests/ return empty; repo-wide those strings appear only in the two production source files. (3) The sole RuntimeError test on build_quantization_config, test_packed_moe_mixed_format_rejected (test file line 1496, added in pre-existing commit 06f9495), covers the older packed-MoE states>1 branch whose message 24b0f25 only reworded — not the new fused-sibling coherence gate. (4) The other multi-format b
  - *intent lens* (stands): Every factual claim in the finding reproduces: commit 24b0f25 touched only prismaquant/allocator.py and prismaquant/export_native_compressed.py (no tests/); greps for 'fused-sibling coherence', 'crash@load', 'silent-corruption', 'allow_default_profile' across tests/ (and docs/, AGENTS.md, CLAUDE.md, archive walls) return nothing — repo-wide those strings exist only in the two source files. The pre-existing test_packed_moe_mixed_format_rejected (tests/test_prismaquant_export_native_compressed.py:1496) covers only the older packed-MoE branch whose message 24b0f25 merely reworded, and test_mixed_
  - *impact lens* (stands): Every factual claim verified against the repo: (1) `git show 24b0f25 --stat` shows only allocator.py and export_native_compressed.py changed, no tests; (2) greps across tests/ for 'fused-sibling coherence', 'crash@load', 'silent-corruption', 'allow_default_profile' all return empty; (3) the only RuntimeError assertion around build_quantization_config is test_packed_moe_mixed_format_rejected (tests/test_prismaquant_export_native_compressed.py:1496), which covers the older packed-MoE branch, not the new fused-sibling merged-column gate at export_native_compressed.py:6381-6430; (4) the allocator 

### M4. Validated-surrogate flow on MoE: frontier cache never renders packed experts, KL silently measures RTN experts, then export hard-fails

- **Status:** VERIFIED (3/3 adversarial lenses)
- **Finder:** moe-gptq-render | **Category:** bug
- **Where:** `/home/rob/prismaquant/prismaquant/run-pipeline.sh` 734-746, 795-797, 858-871, 994
- **Claim:** The validated-surrogate pipeline builds the frontier cache with `--render-scope format-menu` and no `--render-layer-config`, so `fill_packed_expert_cache_entries` never runs (build_production_cache only calls it when a concrete assignment is present). The frontier KL then runs hooks-mode against that cache (packed experts silently fall back to RTN, see strict-default finding), and after selection only `production_recache` runs — which has zero packed-expert handling (`grep packed prismaquant/production_recache.py` is empty) — so the export at line 994 is guaranteed to hit the new RTN-by-omission hard-fail for any MoE model.
- **Evidence:** run-pipeline.sh:746 `--render-scope format-menu` (no --render-layer-config) for the frontier cache; build_production_cache.py:400-407 `elif _model_has_packed_experts(...): print("WARNING: ... experts will RTN at export...")` (warn-only, no fill); run-pipeline.sh:795 `--assignment-materialization hooks`; production_recache.py has no packed/experts references; export_native_compressed.py:5429-5443 `raise RuntimeError("[export-stream] packed expert {full} @ {fmt} has no production-cache render...")`.
- **Why it matters:** SELECTION_MODE=validated-surrogate is the documented path that produced the flagship artifact. On the next MoE run it (a) selects the kneedle from KL numbers measured with RTN experts that do not match the GPTQ bytes export would ship (rendering confound, house rule 4), and (b) dead-ends at export with no stage in the flow able to produce the required expert renders — hours of frontier KL wasted before the failure surfaces.
  - *refute lens* (stands): Every evidence leg reproduced from code. (1) run-pipeline.sh:746 builds the validated-surrogate frontier cache with --render-scope format-menu and no --render-layer-config/--recache-layer-config; build_production_cache.py:383 gates fill_packed_expert_cache_entries on (render_assignment or recache_assignment), both None here, so only the warn-only branch (400-407) fires. (2) run-pipeline.sh:791 uses hooks materialization; PerturbedActivationCache plans include packed 3-D experts (iter_quantizable_tensors, build_rtn_cache.py:252-286) and on production-cache miss falls back silently to RTN (pertu
  - *intent lens* (stands): All cited mechanics verified in code: (1) run-pipeline.sh validated-surrogate branch builds the frontier cache with --render-scope format-menu and no --render-layer-config (~lines 734-748), so build_production_cache.py only hits the warn-only branch (expert_assignment is None -> no fill_packed_expert_cache_entries); (2) frontier KL runs hooks-mode, and iter_quantizable_tensors includes 3-D packed experts, so they miss the format-menu cache and silently RTN via apply_format_quantization (kl_measurement.py ~2508-2522; strict mode is opt-in via PRISMAQUANT_STRICT_PRODUCTION_CACHE, default off); (
  - *impact lens* (stands): Every link in the chain verified against HEAD: (1) run-pipeline.sh:746 builds the validated-surrogate frontier cache with --render-scope format-menu and no --render-layer-config; build_production_cache.py:384-407 only calls fill_packed_expert_cache_entries when render_assignment or recache_assignment is present, otherwise warn-only ("experts will RTN at export"), and the strict coverage gate is likewise inside `if render_assignment is not None`. (2) run-pipeline.sh:791 runs frontier KL with --assignment-materialization hooks; validate_assignments_kl.py has zero packed-expert handling, so measu

### M5. Allocator cost for packed experts is still RTN-measured while export now ships GPTQ-rendered bytes

- **Status:** VERIFIED (3/3 adversarial lenses)
- **Finder:** moe-gptq-render | **Category:** invariant-violation
- **Where:** `/home/rob/prismaquant/prismaquant/measure_quant_cost.py` 870-874
- **Claim:** The only cost path for packed experts quantizes with batched codebook RTN (`_batched_quantize`), and `production_render_cost.py` has no packed-expert handling and falls back to the RTN baseline for any qname without a render-score record — `fill_packed_expert_cache_entries` writes no render-score records. So under the default COST_MODE=production-render-score, allocator costs for ~91% of MoE params are RTN errors while the exported bytes are batched-GPTQ (commit 9971c3e claims 6.7-21x lower error).
- **Evidence:** measure_quant_cost.py:872 `w_hat = _batched_quantize(spec, w)` (RTN, per :1043-1052); production_render_cost.py:284-299 falls back to `per_name` baseline entries with `cost_source: fallback_baseline` when no render-score record exists; `grep packed prismaquant/production_render_cost.py` returns nothing; fill_packed_expert_cache_entries (production_weight_cache.py:2864-3336) contains no `_render_score_record` call.
- **Why it matters:** House rule 4: the surrogate, KL validation, and exported bytes must be identical. The knapsack DP prices expert NVFP4 at an error 6.7-21x higher than what actually ships, systematically distorting the bit allocation for MoE models (over-promoting experts to FP8/BF16 relative to their true shipped cost) — the exact 'rendering confound' that got the JSO wall-off reverted.
  - *refute lens* (stands): Every link in the evidence chain reproduces. (1) measure_quant_cost.py:872 quantizes packed experts with _batched_quantize, which is pure RTN (codebook/int RTN dispatch at :1043-1059); this path is live in the default pipeline via incremental_measure_quant_cost.py:244 (_measure_packed_experts). (2) production_render_cost.py has zero packed-expert handling (grep confirms) and falls back to the RTN baseline entry verbatim with cost_source=fallback_baseline (lines 284-299); the hard-fail guards are default-off (run-pipeline.sh:131 PRODUCTION_RENDER_COST_REQUIRE_SCORES:=0; missing_render_score_pol
  - *intent lens* (REFUTED): The factual observations are accurate (measure_quant_cost.py:872 uses RTN _batched_quantize for packed-expert cost; production_render_cost.py:284-299 has no packed-expert handling and falls back to cost_source=fallback_baseline; export under an active cache ships GPTQ expert bytes per 9971c3e). But the exact behavior — RTN-measured allocator cost while GPTQ bytes ship — is an explicitly documented, deliberately measured current decision, not news. (1) .claude/codex-aura-moe-cost-brief-2026-06-08.md states it verbatim as policy: "ΔW = Q_f(W) − W (RTN render error; per established recipe RTN for
  - *impact lens* (stands): Every evidence pointer in the finding checks out: measure_quant_cost.py:872 uses _batched_quantize (pure RTN per lines 1043-1059) as the only packed-expert cost path; production_render_cost.py contains zero packed/expert handling and its default missing_render_score_policy="fallback" (line 205) silently substitutes the RTN baseline with cost_source=fallback_baseline (lines 284-299); fill_packed_expert_cache_entries (production_weight_cache.py:2864+) writes no render-score records; and commit 9971c3e makes export ship batched-GPTQ bytes for packed experts (6.7-21x lower error than RTN per its o

### M6. PRISMAQUANT_STRICT_PRODUCTION_CACHE defaults to OFF in the KL-validation weight paths (ON in WeightSession); error text implies the opposite

- **Status:** VERIFIED (3/3 adversarial lenses)
- **Finder:** moe-gptq-render | **Category:** bug
- **Where:** `/home/rob/prismaquant/prismaquant/perturbed_x_cache.py` 456-472
- **Claim:** PerturbedActivationCache._quantized_weight_for and layer_state_cache only raise on a production-cache miss when `_env_truthy("PRISMAQUANT_STRICT_PRODUCTION_CACHE")` is true, but env_truthy defaults to False — so with the env var unset (the normal case), any cache miss (including every packed-expert tensor against a format-menu cache) silently substitutes `spec.quantize_dequantize` RTN into the KL measurement. WeightSession uses `default=True` for the same flag, and the raise message says 'set PRISMAQUANT_STRICT_PRODUCTION_CACHE=0 to fall back to RTN', implying strict was intended to be the default.
- **Evidence:** perturbed_x_cache.py:457-460 `if (self._production_weight_cache is not None and fmt != "BF16" and _env_truthy("PRISMAQUANT_STRICT_PRODUCTION_CACHE")): raise ...` then :468-469 `q = spec.quantize_dequantize(original)`; memory_management.py:27 `def env_truthy(name: str, default: bool = False)`; weight_session.py:80-84 `strict_production_cache = _env_truthy("PRISMAQUANT_STRICT_PRODUCTION_CACHE", default=True)`; same default-off pattern at layer_state_cache.py:478-490.
- **Why it matters:** This is silent RTN-by-omission — the precise failure class commits 9971c3e/df12b76 were written to ban — alive inside the hooks-mode real-KL path that gates frontier selection. Validated KL numbers can be measured on different bytes than the cache holds and the export ships, with no error and no counter recording the substitution.
  - *refute lens* (stands): Evidence reproduces exactly: perturbed_x_cache.py:457-472 and layer_state_cache.py:478-490 gate the production-cache-miss raise on _env_truthy("PRISMAQUANT_STRICT_PRODUCTION_CACHE") with env_truthy defaulting to False (memory_management.py:27), falling back silently to spec.quantize_dequantize RTN; weight_session.py:80-84 uses default=True for the same flag. No live code sets the env var — only archived entry points (polish_2026-05-15, grouped_kl_2026-05-28) did setdefault("1"); run-pipeline.sh never sets it. The path is reachable in default production configs: validate_assignments_kl uses hoo
  - *intent lens* (stands): Verified the code: perturbed_x_cache.py:457-467/:658-667 and layer_state_cache.py:477-487 gate the production-cache-miss raise on _env_truthy("PRISMAQUANT_STRICT_PRODUCTION_CACHE") with default False (memory_management.py:27), while weight_session.py:80-84 uses default=True — the asymmetry is real. The finding actually understates it: kl_measurement.py:2512 (build_quant_weight_cache, L3 path) uses env_flag_enabled whose default is True (memory_management.py:19), and the archived polish CLI recorded provenance defaulting the flag to "1" — both confirm strict-by-default was the design intent. No
  - *impact lens* (stands): Every factual claim verified against code. perturbed_x_cache.py:457-472 and :649-669, plus layer_state_cache.py:477-493, gate the production-cache-miss raise on _env_truthy("PRISMAQUANT_STRICT_PRODUCTION_CACHE"), and env_truthy (memory_management.py:27) defaults False — so with the env unset (the normal case; nothing in the live tree or run-pipeline.sh sets it, only archived modules setdefault it to "1"), a miss silently substitutes RTN with no counter or log. weight_session.py:80-84 uses default=True for the same flag, and kl_measurement.py:2512 routes it through env_flag_enabled which defaul

### M7. Zero test coverage for the entire packed-expert GPTQ render path; commit claims a unit test that does not exist

- **Status:** VERIFIED (3/3 adversarial lenses)
- **Finder:** moe-gptq-render | **Category:** test-gap
- **Where:** `/home/rob/prismaquant/prismaquant/production_weight_cache.py` 2864-3336
- **Claim:** No test anywhere references `fill_packed_expert_cache_entries`, `derive_per_expert_activations`, `_read_cached_packed_expert`, `PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN`, or the `packed_expert_max_abs` sidecar — a repo-wide grep matches only the four production files. Commit 9971c3e's message claims 'derive_per_expert_activations ... Unit-tested: reconstructs the experts forward to 4.2e-3 (bf16 noise), exact per-expert row counts', but that test is not in the tree; tests/test_measure_quant_cost_packed_experts.py covers only the older cost-measurement functions.
- **Evidence:** `grep -rln "derive_per_expert_activations|fill_packed_expert_cache_entries|_read_cached_packed_expert|ALLOW_PACKED_EXPERT_RTN|packed_expert_max_abs" tests/ prismaquant/ scratch/` → only build_production_cache.py, export_native_compressed.py, measure_quant_cost.py, production_weight_cache.py. test_measure_quant_cost_packed_experts.py test list: test_packed_expert_dloss..., test_packed_experts_measure_output_mse..., test_packed_experts_replay..., test_packed_router_topk... — none touch the new render path.
- **Why it matters:** This is the freshest, highest-risk production-gating code (hard-fail gates, sidecar resume logic, held-out do-no-harm split, escape hatch). None of it is regression-protected; the '139 export/cache/MoE unit tests pass' line in the commit refers entirely to pre-existing tests, and the claimed reconstruction-accuracy test cannot be re-run by anyone.
  - *refute lens* (stands): PARTIALLY CONFIRMED, with the headline accusation refuted; severity downgraded.

CONFIRMED (evidence trail reproduces exactly):
- Repo-wide grep for `derive_per_expert_activations|fill_packed_expert_cache_entries|_read_cached_packed_expert|ALLOW_PACKED_EXPERT_RTN|packed_expert_max_abs` over /home/rob/prismaquant matches ONLY the four production files (production_weight_cache.py, build_production_cache.py, measure_quant_cost.py, export_native_compressed.py). Nothing in tests/, no indirect coverage (no test imports build_production_cache either), and `git log --all -S ... -- tests/` is empty — n
  - *intent lens* (stands): Core claim verified: repo-wide grep confirms the five render-path symbols appear only in the four production files; commits 9971c3e (+838 lines) and df12b76 (+49 lines) added no tests; tests/test_measure_quant_cost_packed_experts.py covers only the older cost-measurement helpers. Not a recorded deliberate decision: the 2026-06-09 handover's explicit 'NOT verified / not done' deferral list does not include test coverage; no codex deliberation doc, docs/ entry, or archive wall records a decision to skip in-tree tests; and AGENTS.md's implementation checklist explicitly REQUIRES adding tests for 
  - *impact lens* (stands): Core finding verified: zero in-repo test coverage for the packed-expert GPTQ render path. Repo-wide grep for fill_packed_expert_cache_entries / derive_per_expert_activations / _read_cached_packed_expert / PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN / packed_expert_max_abs matches only the 4 production files; tests/test_measure_quant_cost_packed_experts.py covers only the older cost functions. This path is DEFAULT production for every MoE model (build_production_cache.py:387-389 calls fill_packed_expert_cache_entries under default PRODUCTION_CACHE=1; export_native_compressed.py:1395+/5419+/5673+ hard-f

### M8. RTN escape hatch and RTN-export warning paths leave no provenance in the exported artifact

- **Status:** VERIFIED (3/3 adversarial lenses)
- **Finder:** moe-gptq-render | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/export_native_compressed.py` 1398-1400, 5498, 7037-7055
- **Claim:** `_ALLOW_PACKED_EXPERT_RTN` is read from the environment at import time and never recorded in any output: the manifest written at export end (`mixed_native_manifest.json`) records format_histogram/runtime_coercions/bf16_audit/ignore only, and the per-expert histogram label is identical for cached-GPTQ and escape-hatch-RTN exports (`hist[("packed_moe_per_expert", fmt)]`). The only trace is a per-tensor stdout print. The cache-side rtn_fallbacks/heldout_reverts coverage dict (cache.metadata['packed_expert_coverage']) is likewise never propagated into the export — grep finds no consumer.
- **Evidence:** export_native_compressed.py:1398-1400 `_ALLOW_PACKED_EXPERT_RTN = (os.environ.get("PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN", "0") == "1")`; :5498/:5734 `hist[("packed_moe_per_expert", label if is_bf16 else fmt)] += 1` (same label both arms); :7037-7055 manifest json dump contains no env-flag/expert-render field; `grep -rn packed_expert_coverage` → only the write in build_production_cache.py:399.
- **Why it matters:** House gate: 'Bake git commit, calibration hash, assignment hash, cache hit/miss/RTN-fallback counts into output JSON. An irreproducible number is quarantined.' An A/B RTN artifact is byte-different but manifest-identical to a production GPTQ artifact; once the shell session scrolls away, nothing distinguishes the served-A/B arm from a shippable build — the exact 'leaks into production unnoticed' scenario, on a flag added one commit after the served A/B inverted the screen.
  - *refute lens* (stands): Every element of the evidence trail reproduces. (1) export_native_compressed.py:1398-1400 reads PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN at module import; main() resets other globals but not this one, and repo-wide grep shows the env var exists nowhere else (no run-pipeline.sh handling, no manifest field). (2) Both per-expert histogram sites (streaming :5498, in-memory :5734) use hist[("packed_moe_per_expert", fmt)] identically whether the expert came from the cached GPTQ render (cached_sub) or source RTN (sub_packed fallback at :5485-5489/:5722-5725); the only RTN trace is the stdout print at :544
  - *intent lens* (stands): All technical claims verified against code: the flag is an import-time env read confined to export_native_compressed.py (lines 1398-1400, uses at 5426-5447/5678-5695); the mixed_native_manifest.json dump (7037-7055) records no flag/render-mode/hash/commit field; both export arms write the identical hist[("packed_moe_per_expert", ...)] label (5498, 5734); cache.metadata["packed_expert_coverage"] is written only at build_production_cache.py:399 with no consumer anywhere (code or tests). The KNOWN/INTENDED lens fails: while the escape hatch ITSELF is a deliberate, recorded decision (commit df12b7
  - *impact lens* (stands): Verified at HEAD: (1) _ALLOW_PACKED_EXPERT_RTN is an import-time env read (export_native_compressed.py:1398-1400) gating two RTN paths (:5426-5450 streaming, :5678-5696 in-memory) whose only trace is a stdout print; (2) the mixed_native_manifest.json dump (:7037-7055) records no env flag and no RTN count, and both histogram sites (:5498, :5734) use the identical key for cached-GPTQ and RTN-fallback experts, so a flag-on A/B export is manifest-identical to a production export; (3) cache.metadata['packed_expert_coverage'] (with rtn_fallbacks counts from production_weight_cache.py:3304) is writte

### M9. AURA cost generator silently omits packed-MoE experts; allocator drops cost-less names without warning

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** allocator-invariants | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/aura_cost.py` 103-111
- **Claim:** aura_cost._target_linears enumerates only nn.Linear modules, so 3D packed-expert params (nn.Parameter on an Experts container, ~91% of MoE params) never get stats or cost entries. The allocator then silently skips any name absent from costs, so an AURA-cost MoE allocation would leave every routed expert unallocated (defaulting to source precision downstream) with no fail-fast anywhere.
- **Evidence:** aura_cost.py:104-106: `for name, mod in model.named_modules():\n    if not isinstance(mod, nn.Linear):\n        continue` — no expert/MoE handling exists anywhere in the file (grep for expert|moe|packed returns nothing). Contrast sensitivity_probe.py:545-556 (_packed_experts_param_names handles 3D packed params) which AURA does not use. allocator_candidates.py:301-303: `for name, s in stats.items():\n    if name not in costs:\n        continue` — names missing from costs are dropped with no per-name diagnostic.
- **Why it matters:** This is the exact bug class just closed in commit 9971c3e ('Render packed-MoE experts through the shared GPTQ path (close RTN-by-omission)') and the standing instruction 'every Linear/expert/sidecar MUST pass the full deliberate render'. AURA is now the paper spine (commit 5db5a9e) and produced ship-candidate 27B allocations; the first AURA run on a Qwen3.6-35B/LFM2.5/DSv4-class MoE will quietly exclude all experts from the bit budget — the artifact balloons or the byte-budget selector declares infeasibility, with nothing pointing at the omission.

### M10. AURA cost silently omits packed-MoE experts; codex-agreed hybrid expert cost never landed

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** aura | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/aura_cost.py` 104-109
- **Claim:** aura_cost only costs 2-D nn.Linear modules, so 3-D packed-MoE expert params (91% of MoE params) get no cost rows, and the allocator silently drops any stats entry without a cost row — no coverage gate, no warning. The 2026-06-09 codex deliberation concluded packed-expert AURA FAILS its gate (Spearman 0.465, format gap wrong ~10x) and agreed on a hybrid (empirical single-group NVFP4-vs-BF16 serving-unit cost for experts, additivity gate before export); none of that exists anywhere in the tree.
- **Evidence:** aura_cost.py:104-109: `if not isinstance(mod, nn.Linear): continue ... if mod.weight.dim() == 2 and min(mod.weight.shape) >= 16`. allocator_candidates.py:301-303: `for name, s in stats.items(): if name not in costs: continue` (silent drop; allocator.py:1125 only prints counts). Probe DOES emit packed-expert stats (incremental_probe.py:2163-2164 `_packed_experts_module`) and allocator_candidates expects them (`_stats_indicates_packed_expert`, :195-201). .claude/codex-aura-moe-gate-2026-06-09.out.md:15: "Use empirical KL for routed experts"; :23 "do not let the allocator double-count gate_up + down"; :25 additivity gate — grep for `single_group|expert_kl|group_aura|route_flip` over prismaquant/ and scripts/ returns nothing.
- **Why it matters:** Running the documented AURA flow (allocator --probe probe.pkl --costs aura_cost.pkl) on the in-flight 35B MoE silently allocates the bit budget over only the ~9% non-expert params while every routed expert falls out of the DP, the bpp denominator, and the layer_config — the cost-side twin of the render RTN-by-omission just closed in commit 9971c3e, but with no hard gate. The agreed shippable expert-cost decision exists only in deliberation docs.

### M11. Validated-frontier selection KL (kneedle AND saturation B*) is in-sample under pipeline defaults

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** aura | **Category:** invariant-violation
- **Where:** `/home/rob/prismaquant/prismaquant/validate_assignments_kl.py` 394-413
- **Claim:** Under run-pipeline.sh defaults, the 'real KL' that select_validated_frontier's kneedle/saturation pick consumes is measured on byte-identical text to what generated the probe/costs: same DATASET jsonl, same n_samples/seqlen (VALIDATED_FRONTIER_NSAMPLES:=$NSAMPLES), and a hardcoded seed 42 — validate_assignments_kl never passes its --calib-seed into load_calibration on the --dataset path (both the repeats==1 and repeats>1 branches), so even an explicit --calib-seed is silently ignored. This violates House Rule 6 and contradicts CLAUDE.md §5 ('Held-out split is disjoint from cost generation... kneedle... use the held-out split').
- **Evidence:** validate_assignments_kl.py:395-400 and 409-414: `load_calibration(tokenizer, args.dataset, n_samples, args.calib_seqlen,)` — no `calib_seed=` kwarg despite `--calib-seed` existing (:594). run-pipeline.sh:68 `: "${DATASET:=/home/rob/dq-runs/calibration/diverse-v1.jsonl}"`; :327-328 probe uses `--dataset "$DATASET" --nsamples "$NSAMPLES" --seqlen "$SEQLEN"`; :182 `: "${VALIDATED_FRONTIER_NSAMPLES:=$NSAMPLES}"`; :783 validation uses `--dataset "$DATASET"` with no split/seed override. sensitivity_probe.py:1771-1782: "Default 42 is the historical value and is byte-stable" + `random.seed(calib_seed)` — deterministic, so the 32x1024 validation windows equal the 32x1024 probe windows exactly.
- **Why it matters:** The selection step that decides which Pareto point ships (and the new saturation B*, whose entire justification is the held-out measurement noise floor) is scored on the calibration text the surrogates already saw — the exact in-sample-validation failure CLAUDE.md says a prior audit fixed. Frontier picks can overfit calibration and the published 'measured KL' selection story is weaker than documented; either the code regressed or the doc claim is stale.

### M12. propagated_cost.md and three live launchers document a module deleted from the tree

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** doc-audit | **Category:** stale-doc
- **Where:** `docs/propagated_cost.md` 123-266
- **Claim:** The entire L3-polish doc (276 lines) is a usage manual for `python3 -m prismaquant.iterate_perturbed_allocation`, and three non-archived launchers still invoke it — but the module does not exist anywhere in the current tree (archived in 915616e, fully deleted via 6261632/7517362). Every documented command fails with ModuleNotFoundError.
- **Evidence:** docs/propagated_cost.md:149 "python3 -m prismaquant.iterate_perturbed_allocation \\ ... --l3-polish" (also :200, :218); examples/launchers/run-perturbed-x-smoke.sh:36 "python3 -m prismaquant.iterate_perturbed_allocation \\"; examples/launchers/launch-qwen3p6-35b-a3b-prismascout-knee.sh:131 and bench-l3-speed-kit.sh:25 same import. Verification: `git ls-files | grep -i iterate_perturbed` returns nothing; `find /home/rob/prismaquant -name 'iterate_perturbed_allocation*'` (excluding .git) returns nothing; git log shows 915616e "refactor: archive cross-layer machinery" moved it and 6261632/7517362 deleted it.
- **Why it matters:** CLAUDE.md §11 still recommends docs/propagated_cost.md as "the L3 propagated-cost spec". Anyone following it (or running the L3_POLISH=1 smoke launcher it cites at line 266) hits an immediate failure — or worse, revives the archived L3-polish-of-many DP that the graveyard explicitly rejected (non-additive, regressed 0.371→0.461 on 4B). The doc presents a rejected, deleted workflow as the current opt-in production pass.

### M13. Design doc says packed-MoE render-side GPTQ gap is open/intentional; HEAD closed it and hard-bans RTN-by-omission

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** doc-audit | **Category:** stale-doc
- **Where:** `docs/prismaquant_design.md` 558-569, 613-617, 1062-1067
- **Claim:** Three sections (§6.3 'Packed-MoE exclusion is intentional', §7.2, §12.2) state packed experts have no render-side GPTQ/JSO and that this is a known open gap. Commit 9971c3e (2026-06-09) closed it: packed experts are now rendered through the shared batched-GPTQ path into the same ProductionWeightCache, and export HARD-FAILS on any uncached non-BF16 packed expert.
- **Evidence:** Doc §6.3:558,567-569: "Packed-MoE exclusion is intentional ... The remaining gap is render-side: GPTQ, joint_scale_opt, scale_sweep, and calibrated activation clip are still not applied inside `_quantize_3d_packed`"; §7.2:613-616: "packed experts still have no render-side GPTQ, no JSO". Code: production_weight_cache.py:2864 `def fill_packed_expert_cache_entries(` (render_mode default "batched" at :2878); export_native_compressed.py:5426-5444: `if not _ALLOW_PACKED_EXPERT_RTN: ... raise RuntimeError(f"[export-stream] packed expert {full} @ {fmt} has no production-cache render. Non-BF16 packed experts MUST be rendered through the deliberate GPTQ+JSO path ... RTN ... is banned")`.
- **Why it matters:** A reader planning MoE work from the 'most authoritative narrative' doc would believe expert RTN is by-design and the exporter silently RTNs experts — the exact RTN-by-omission bug Robert ordered closed (feedback: 'everything goes through PrismaQuant'). They might re-implement the fix, mis-assess shipped MoE artifacts, or be blindsided by the new export hard-fail. Note the 2026-06-09 served A/B found RTN-experts actually win — but as an explicit measured choice through the gate, not the silent omission the doc describes.

### M14. Public README recommends the de-menued MXFP8_E4M3 menu and omits FP8_DYNAMIC

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** doc-audit | **Category:** stale-doc
- **Where:** `README.md` 48, 149
- **Claim:** The top-level README's quick start and 'Recommended bundle for shipping today' both specify FORMATS=NVFP4,MXFP8_E4M3,BF16. The pipeline default and shipping menu is NVFP4,FP8_DYNAMIC,BF16; MXFP8 is de-menued for inference (exact-scale FP8 Pareto-dominates it) and Robert's standing rule is FP8 in every production recipe.
- **Evidence:** README.md:48 "export FORMATS=NVFP4,MXFP8_E4M3,BF16"; README.md:149 "Recommended bundle for shipping today: `--formats NVFP4,MXFP8_E4M3,BF16`". Code: prismaquant/run-pipeline.sh:45 ': "${FORMATS:=NVFP4,FP8_DYNAMIC,BF16}"' and :8 header comment "FORMATS=NVFP4,FP8_DYNAMIC,BF16". Memory feedback_fp8_in_every_recipe: "Fp8 should be included in every recipe. That's the whole point of pq."
- **Why it matters:** This is the public GitHub front page. An external user (or a future agent) following it ships a Pareto-dominated MXFP8 menu and drops FP8_DYNAMIC — the format the 2026-06-05 finding showed bends the RD frontier 3-8x at matched bpp. It directly contradicts the owner's explicit recipe mandate.

### M15. Design doc presents the cancelled 27B JSO isolation A/B as queued, citing a script that does not exist

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** doc-audit | **Category:** stale-doc
- **Where:** `docs/prismaquant_design.md` 1020-1024
- **Claim:** §11.10 says a 27B JSO isolation A/B is queued via `qwen36_27b_jso_isolation.sh` and frames it as the bar for the JSO decision. The script exists nowhere in the tree, and the experiment was explicitly cancelled by Robert ('we don't need a JSO isolation test. We already know JSO works beautifully when picking either 4/6'); JSO ships as default (cdb3022 made the {6,4} grid the JSO default).
- **Evidence:** Doc:1020-1023: "A queued **27B isolation A/B** (`qwen36_27b_jso_isolation.sh`) holds cost surrogate constant and varies only the renderer. The bar for removing JSO is reproducibility...". Verification: `find /home/rob/prismaquant -name '*jso_isolation*'` (excluding .git) returns nothing. Code: run-pipeline.sh:117 ': "${PRODUCTION_CACHE_LEVERS:=gptq,static_act_order,joint_scale_opt}"' — JSO is a shipping default; commit cdb3022 "Default JSO per-group scale grid to FourOverSix {6,4}".
- **Why it matters:** This is the prompt's canonical mislead: an autonomous session reading the design doc would burn a multi-day 27B GPU A/B that the owner cancelled on 2026-06-04, and would treat the shipping default JSO as provisionally suspect. CLAUDE.md's claim that the design doc's 'archive banners are kept current' is itself falsified by this section.

### M16. FP8-native sources: BF16/unassigned Linears are emitted as FP8_SOURCE bytes but listed in config ignore

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** export-codecs | **Category:** bug
- **Where:** `/home/rob/prismaquant/prismaquant/export_native_compressed.py` 4987-4991, 1078-1096, 7031-7034
- **Claim:** materialize_tensors_streaming silently flips fmt to FP8_SOURCE for any fp8-block-scaled source Linear whose recipe entry is BF16, absent, or pinned — emitting fp8 codes + weight_scale on disk — but write_config_with_quantization is called with the un-flipped assignment, so those same names land in the compressed-tensors `ignore` list (BF16 entries via build_quantization_config, unassigned via compute_extra_ignore). Disk and config disagree about the tensor's format.
- **Evidence:** export_native_compressed.py:4987-4991: `if (source_is_fp8_scaled and (fmt is None or fmt == "BF16" or recipe_key in bf16_passthrough)): fmt = "FP8_SOURCE"` → branch at 5054-5056 writes `out[f"{emit_full}.weight"] = w_fp8.cpu().contiguous()` + `.weight_scale`. Config side: build_quantization_config:6110-6111 `if fmt == "BF16": ignore.append(vllm_name)` and compute_extra_ignore:6552-6559 adds every unassigned 2D weight to ignore. Nothing propagates the FP8_SOURCE flip into the config (main():7031 passes the original `assignment`). The flip is fed by _coerce_runtime_legal_assignment:1082/1095 which coerces runtime-illegal formats to literal "BF16" with no source-dtype awareness, so on an FP8-native source every runtime coercion produces this disk/config mismatch.
- **Why it matters:** vLLM treats an ignored module as unquantized: it creates a BF16 param and copies the raw fp8 codes into it without applying the block scale (weights off by up to ~448x), and the orphan `.weight_scale` key crashes load_weights on most vLLM model classes. This is live for the in-flight DSv4-Flash-Base (284B FP8 source) and any MiniMax re-export the moment one Linear is runtime-coerced, --ignore'd, or missing from the recipe. The correct coercion target on fp8 sources is FP8_SOURCE in BOTH tensors and config (passthrough rule), or a hard fail.

### M17. MTP sidecar Linears bypass the production cache and are silently RTN-rendered in the default production path

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** export-codecs | **Category:** invariant-violation
- **Where:** `/home/rob/prismaquant/prismaquant/export_native_compressed.py` 5618-5622, 6823-6827, 3607-3609
- **Claim:** _materialize_tensors_inmemory (the MTP export path) never consults _pack_production_cached_2d for 2-D Linears, and in the default pipeline configuration (both --activation-cache-dir and --production-weight-cache supplied, as run-pipeline.sh does) _CACHED_ACTIVATIONS is left None — so every non-BF16 mtp.* Linear is quantized by bare RTN, not the cache's GPTQ+JSO render, with no hard fail. This is the same RTN-by-omission class that commit 9971c3e closed for packed experts, still open for the MTP sidecar, and a one-cache violation (exported MTP bytes ≠ the cache render the upfront check at 6737-6741 required to exist).
- **Evidence:** export_native_compressed.py:5618-5622 (inmemory path): `compressed = _quantize_2d(mod.weight.detach().float(), fmt, ...)` — no _pack_production_cached_2d call, unlike the streaming body path (5065) and even the head path (4731). main():6823-6827: `if args.activation_cache_dir and _PRODUCTION_WEIGHT_CACHE is not None: print("...Raw activation cache will not drive body export")` — _CACHED_ACTIVATIONS stays None. _quantize_2d:3607-3609: `if gptq_enabled: acts_work = _acts_for_error_passes(); if acts_work is not None:` — with acts None, GPTQ/JSO are silently skipped and the weight falls through to RTN packing. run-pipeline.sh:972+992 passes both --activation-cache-dir and --production-weight-cache.
- **Why it matters:** Every production MoE export with quantized mtp.* entries (Qwen3.5-122B, Qwen3.6 27B/35B family — MTP drives spec-decode acceptance, a shipped metric) ships RTN MTP weights while validation measured the GPTQ render sitting unused in the cache. Per the house memory, NVFP4 under RTN is a severe quality regression, and 'every Linear/expert/sidecar MUST pass the full deliberate render'. The fix pattern already exists twice in this file (cache read + hard-fail), just not in the MTP helper.

### M18. Registry NVFP4 RTN/activation emulation omits the served two-level scale scheme

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** format-registry | **Category:** bug
- **Where:** `/home/rob/prismaquant/prismaquant/format_registry.py` 437-446, 594-595
- **Claim:** FormatSpec NVFP4 quantize_dequantize / activation_quantize_dequantize use a raw fp32 per-group scale (max_abs/6) with no FP8-E4M3 quantization of the block scale and no per-tensor global scale, and ignore PRISMAQUANT_NVFP4_SCALE_RULE entirely — unlike the export renderer (_rtn_dequant_nvfp4 / _nvfp4_quantize_grouped_codec) and the served CUTLASS kernel, which both use fp8(scale/global)*global.
- **Evidence:** format_registry.py:437-439 in _make_rtn's _inner_eager: `max_abs = w2.abs().amax(dim=-1, keepdim=True).clamp_min(1e-8); scale = max_abs / cmax` (mx_scale=False for NVFP4, so no snap) vs export_native_compressed.py:391-399: `scale_fp8 = _nvfp4_fp8_scale_from_real(..., quantize_fp8=True); eff_scale = _nvfp4_effective_scale_from_fp8(scale_fp8, global_real)`. Verified numerically: same Gaussian weight, registry MSE 0.009080 vs export-RTN MSE 0.009170 (~1% systematic under-count), max per-element diff 1.23. Consumers on metric paths: aura_cost.py:141-146 (`qdq = getattr(spec, "quantize_dequantize", None) ... return qdq(weight.float()) - weight.float()` — AURA's RTN ΔW fallback), incremental_measure_quant_cost.py:368 (`X_hat = spec.activation_quantize_dequantize(X.clone())`), perturbed_x_cache.py:774 & 948 (`x_runtime = act_spec.activation_quantize_dequantize(x_in)` — the resident KL evaluator's W4A4 activation model). Test gap: tests/test_format_registry.py asserts bit-exact registry==export parity for FP8_E4M3 (lines 35-150) and MXFP8 (153-199) but has NO NVFP4 parity test, and tests/test_prismaquant_export_native_compressed.py:549's NVFP4 branch compares enc._rtn_dequant_nvfp4 to served metadata, never the registry entry.
- **Why it matters:** Every NVFP4 cost/KL number measured through the registry path is optimistically biased relative to FP8/BF16 (whose registry paths ARE bit-exact vs export per tests), skewing cross-format allocation: AURA RTN-cost mode (the current paper-spine cost, which Robert found 'decisive at fp8'), the L1 incremental output-MSE cost's activation term, and the resident KL used by polish/frontier selection all model NVFP4 activation quantization without the FP8-snapped scales the kernel applies. This is the code-level root of the recorded 'resident W4A4 emulation under-counts baseline ~3x vs CUTLASS at 4B — trust served, not resident' incident; the infidelity is still in the tree with no parity test to catch it.

### M19. Exported NVFP4 bytes are re-derived from bf16-stored dequant — not the KL-validated bytes

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** format-registry | **Category:** invariant-violation
- **Where:** `/home/rob/prismaquant/prismaquant/export_native_compressed.py` 1261-1267, 1344-1352
- **Claim:** The production export path does not pack the scales chosen during the (JSO/GPTQ) render; it stores the dequant cast to bf16 and re-derives NVFP4 codes + group scales from that tensor at export time, which the code itself acknowledges is not bit-lossless. The bytes vLLM serves therefore differ from the bytes the real-KL selection/polish measured.
- **Evidence:** export_native_compressed.py:1261-1267 (_pack_production_cached_2d): `wp, ws, wg = quantize_dequantize_nvfp4(w_work, group_size=16, ...)` re-quantizes the cached dequant from scratch; :1349-1352 (_read_cached_packed_expert docstring): "This is NOT bit-lossless (the re-derived group scales differ slightly from those used during the render, ~1e-3 weight error), but it is the established production contract". production_weight_cache.py:1001-1002 (_store_rendered_weight_entry): `target_dtype = weight_dtype if weight_dtype != torch.float32 else torch.bfloat16; stored = tensor.to(target_dtype).cpu()` — fp32 render output is rounded to bf16 before the export-time re-derive. Used on the shipped path at export_native_compressed.py:4731 and 5065.
- **Why it matters:** House invariant: the surrogate, the KL validation, and the exported bytes must be identical (the exact rationale for reverting the JSO wall-off). The validated-frontier kneedle and the coord-descent polish's 'provably no worse' guarantee are contracts on the cache dequant; the shipped checkpoint is a perturbed copy of it (bf16 rounding + re-derived scales, with JSO-time {6,4} level choices not persisted). Post-export serving re-validation mitigates regression detection but the selection itself was made on different bytes, re-introducing a (small, unquantified per-artifact) rendering confound into every published matched-bpp comparison.

### M20. Serving-unit promotion never re-checks per-member format legality; export coercion can then re-break the coherence it enforced

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** fused-coherence | **Category:** invariant-violation
- **Where:** `prismaquant/allocator_solver.py` 101-108
- **Claim:** _promote_group_components writes the unit's max-rank format onto every member without consulting candidates[member] — violating build_candidates' contract that 'the DP must never see choices that the selected serving profile cannot run' (allocator_candidates.py:292-294). Fused-sibling groups are protected by the pre-aggregation candidate intersection (allocator_candidates.py:520-531), but packed-MoE units are promotion-only ('without collapsing experts into allocator super-items', allocator.py:2003-2004), and legality is shape/qname-dependent per projection (check_format_applicability gates on out_features, scale_block divisibility and qname regex rules at allocator_candidates.py:101-152). The only post-promotion re-check is passthrough integrity for BF16/FP8_SOURCE (allocator.py:2073-2107) — general format legality is not re-checked. Export then 'fixes' an illegal member per-Linear via _coerce_runtime_legal_assignment (BF16, no group-aware re-promotion, export_native_compressed.py:1062-1097), recreating exactly the quantized+BF16 'silent-corruption' mix — caught only by the late gate of the previous finding, or not at all when the export resolves DefaultProfile (warn-only).
- **Evidence:** allocator_solver.py:104-108 `best_fmt = max((out[member] for member in members), key=lambda fmt: format_rank[fmt]) ... out[member] = best_fmt` (no candidate/legality lookup); allocator.py:2073-2075 `# Passthrough-integrity belt-and-suspenders. The filter in # build_candidates drops mismatched FP8_SOURCE / BF16 per-Linear # candidate` (passthrough only); export_native_compressed.py:1094-1096 `if not verdict.legal:\n  out[qname] = "BF16"\n  coerced.append(...)` — per-qname, no promote_serving_units re-run afterwards.
- **Why it matters:** A packed-MoE unit where the DP legally picks FP8 for gate_up_proj but FP8 is shape-illegal for down_proj gets promoted to an unservable all-FP8 unit; export coerces down_proj back to BF16, producing a mixed FusedMoE that aborts at config-build time after the full render — or ships silently corrupt if the export ran under DefaultProfile. The platform principle is that errors are bounded by measurement, but here the promotion step can emit assignments the legality measurement already rejected.

### M21. Model-staging dirs (rewritten config.json + symlink tree) land in /tmp and are never cleaned up

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** hygiene-landmines | **Category:** risk
- **Where:** `prismaquant/sensitivity_probe.py` 157, 204
- **Claim:** stage_text_only/stage_multimodal (and build_rtn_cache's mirror) mkdtemp without dir=, so the staged config.json that the production probe/cost/validate model loads depend on lives in /tmp; the sensitivity_probe variants return no cleanup handle, so the dirs also leak permanently.
- **Evidence:** sensitivity_probe.py:157: `staged = Path(tempfile.mkdtemp(prefix="prismaquant_stage_"))` and :204: `staged = Path(tempfile.mkdtemp(prefix="prismaquant_mm_stage_"))` — both then write `with open(staged / "config.json", "w")` and `return str(staged)` with no cleanup tracking. Same pattern at build_rtn_cache.py:111 and :140 (`staged = tempfile.mkdtemp(prefix="rtn_staged_")`). Production callers: incremental_probe.py:950 `staged = stage_text_only(model_path)` and :3038; measure_quant_cost.py:381; build_production_cache.py:44 (imports stage_multimodal); validate_assignments_kl.py:750 `staged, cleanup = stage_multimodal(args.model)`; aura_cost.py:473. grep for rmtree in incremental_probe.py: zero hits.
- **Why it matters:** The probe is production stage 1 on every run of a multimodal/quant-config model (Gemma4, Qwen3.5/3.6-VL, FP8-source models). transformers loads the model THROUGH the /tmp staged dir — a /tmp clear between staging and the end of a tens-of-minutes 100B-class load crashes the run; even absent a clear, each run litters /tmp, the directory the house rule says to never touch.

### M22. Default-on production_recache stage builds its activation hook cache in /tmp

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** hygiene-landmines | **Category:** risk
- **Where:** `prismaquant/production_recache.py` 210
- **Claim:** measure_production_activation_max_abs — run by recache_production_weight_cache, the PRODUCTION_RECACHE=1 default-on pipeline stage — creates its PerturbedActivationCache directory via TemporaryDirectory with no dir= and no redirect knob, so it lands in /tmp.
- **Evidence:** production_recache.py:210: `with tempfile.TemporaryDirectory(prefix="prismaquant_recache_") as tmp:` followed by `builder = PerturbedActivationCache(model, assignment, tmp, ...)`; enclosing def at line 171 `measure_production_activation_max_abs`, called from line 342 inside `recache_production_weight_cache`; run-pipeline.sh:855/918 `python3 -m prismaquant.production_recache` (PRODUCTION_RECACHE defaults to 1 per the pipeline defaults). grep TMPDIR over run-pipeline.sh and examples/launchers/*.sh: zero hits — nothing redirects the default.
- **Why it matters:** The recache stage runs full-model calibration forwards (the act-max-abs measurement for static activation clip) while holding state under /tmp on every default production build; an OOM-triggered /tmp clear mid-stage (the documented failure mode on this host) aborts the cache build for the artifact being shipped.

### M23. Do-no-harm export gate swallows all exceptions silently (gate failure = no gate, no log)

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** hygiene-landmines | **Category:** risk
- **Where:** `prismaquant/export_native_compressed.py` 3690-3691, 3830-3831, 3971-3972
- **Claim:** All three per-Linear do-no-harm blocks (NVFP4, MXFP8, MXFP4) — the default-on gate that reverts GPTQ to RTN when GPTQ locally degraded reconstruction — end in a bare `except Exception: pass`, so if the gate's helper errors (e.g. device/shape mismatch) the protection silently vanishes for the whole export. Reverts themselves are also only printed under PRISMAQUANT_DO_NO_HARM_VERBOSE=1 and are not counted in export metadata.
- **Evidence:** export_native_compressed.py:3690-3691: `except Exception as _e:\n    pass  # never fail the export over the gate`; :3830-3831 and :3971-3972: `except Exception:\n    pass`. The gate is default-on: :3663 `os.environ.get("PRISMAQUANT_DO_NO_HARM", "1") != "0"`. Revert print is verbose-gated: :3684 `os.environ.get("PRISMAQUANT_DO_NO_HARM_VERBOSE") == "1"`. By contrast the batched variant DOES warn — :4283-4284: `except Exception as _e:\n    print(f"[do-no-harm batched] WARN failed: {_e}", flush=True)` and prints `reverted {n_reverted}/{n}` (:4281).
- **Why it matters:** This code path is shared by the production cache render (production_weight_cache.py:1992-1995 calls the same _quantize_2d), so a systematically erroring gate silently strips a default-on quality protection from every rendered Linear in a shipped artifact with zero log evidence — directly against the house 'honest accounting' and 'bake counts into output JSON' rules, and inconsistent with the batched path that already warns.

### M24. Table 3 footer attributes small-corpus all-position KL numbers to WikiText-test

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** paper-audit | **Category:** bug
- **Where:** `/home/rob/prismaquant/paper/main.tex` 622
- **Claim:** The headline 27B table's footer 'all-position KL (WikiText-test): 0.03575 vs 0.04094, −12.7%' quotes the SMALL synthetic corpus (935 positions) all-position numbers, not WikiText-test. The actual WikiText-test all-position KLs are 0.06969 vs 0.07899 (−11.8%). The '+0.57pp top-1' in the same line IS from WikiText-test — the footer mixes two corpora in one labelled row.
- **Evidence:** main.tex:622 '\multicolumn{4}{l}{\emph{all-position} KL (WikiText-test): $0.03575$ vs $0.04094$, $-12.7\%$; top-1 $+0.57$pp}'. Reproduced from raw dumps: kl_ab.py compare kl_teacher.json kl_aura.json → 'positions=935 ALL: meanKL=0.03575' (baseline 0.04094) — the small corpus; kl_ab.py compare kl_teacher_big.json kl_{aura,baseline}_big.json → 'positions=8176 ALL: meanKL=0.06969' vs 0.07899 with confident top1 97.63% vs 97.06% (=+0.57pp). −12.7% = (0.04094−0.03575)/0.04094, the small-corpus delta.
- **Why it matters:** A wrong number in the paper's main results table (Table 3). Any reviewer re-running the cited repro (aura-27b-flagship/) gets 0.0697, roughly 2× the printed 0.0358, and the credibility of the whole table goes with it. The qualitative claim survives (−11.8% vs −12.7%) so the fix is a one-line correction, but it must land before any circulation.

### M25. 'Three disjoint held-out corpora' is false — two are overlapping WikiText-test windows with an unreconciled 1.6× discrepancy

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** paper-audit | **Category:** risk
- **Where:** `/home/rob/prismaquant/paper/main.tex` 563-571 (and 619-620)
- **Claim:** The paper counts the Table 3 'WikiText-test (4393 conf. pos.)' run (0.05594, −17.9%) and the re-served 'WikiText test split' run (0.0344, −33.5%) as two of 'three disjoint held-out corpora'. The two corpora are overlapping slices of the same WikiText test split (88 of 90 long lines of kl_corpus_big.txt appear verbatim in wt_test.txt), and the same exported artifacts give confident-KL 0.0559 in one and 0.0344 in the other — a 1.6× absolute and −17.9% vs −33.5% relative discrepancy the paper never reconciles.
- **Evidence:** main.tex:570-571 'The 27B confident-KL gap is therefore $-17$ to $-34\%$ across three disjoint held-out corpora'. Corpus overlap measured directly: kl_corpus_big.txt vs wt_test.txt share 88 of 90 long lines (both WikiText test prose, both dumped at 8176 positions). Both numbers reproduce from raw dumps with the same kl_ab.py: kl_teacher_big/kl_aura_big → 'BF16-CONFIDENT: n=4393 meanKL=0.055943'; aura-blocker-runs kl_teacher_served27b/kl_aura_served27b → 'BF16-CONFIDENT: n=4237 meanKL=0.034375'. Same exported 5.99bpp artifacts (reserve.sh re-served exported/{aura,baseline}).
- **Why it matters:** Two problems a hostile reviewer will find in minutes: (a) the replication count is inflated — the honest statement is two disjoint corpora (synthetic small + WikiText-test), with the abstract's 'two disjoint held-out corpora' claim being the defensible one; (b) two measurements of near-identical text on identical artifacts differing 1.6× in absolute KL and 2× in relative gap, presented side-by-side without explanation, undermines the reproducibility story ('an irreproducible number is quarantined'). The −33.5% should be presented as a re-measurement window of the same split, and the window-to-window variance acknowledged.

### M26. Default validated-surrogate final selection runs on last-token hook-KL — the metric CLAUDE.md classifies as triage-only

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** pipeline-contract | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/run-pipeline.sh` 184, 789-791, 805-812
- **Claim:** The frontier point that becomes layer_config.json (and is exported) is selected by default on last-token-scope KL measured through hooks materialization, i.e. the cheap 'hook KL screen', not full-sequence KL.
- **Evidence:** Line 184 `: "${VALIDATED_FRONTIER_KL_SCOPE:=last_token}"`; lines 789-791 pass `--kl-scope "$VALIDATED_FRONTIER_KL_SCOPE"` and `--assignment-materialization hooks`; lines 805-812 `select_validated_frontier ... --output-layer-config "${WORK_DIR}/artifacts/layer_config.json"` overwrite the config export consumes (line 969). validate_assignments_kl.py:624-628: `choices=("last_token", "full_sequence"), default="last_token", help="... matches production probe gates and avoids full-sequence reference tensor residency on 27B."` CLAUDE.md §5 rank 5: "Cheap last-token 'hook KL' screens — triage only, never final selection", while §8/§6 describe SELECTION_MODE=validated-surrogate as "the real-KL frontier selection that produced the shipped 27B".
- **Why it matters:** The kneedle/saturation pick among Pareto candidates — the decision that determines the shipped bpp/quality point — is made on a metric the project's own metric-authority ladder forbids for final selection. Combined with the in-sample text (finding 1), the default 'validated' selection is two rungs below what the docs and paper narrative claim, unless the operator manually overrides VALIDATED_FRONTIER_KL_SCOPE. There is a real residency rationale in the help text, but per house rules that is a measurement gap to fix, not a default to silently accept.

### M27. CLAUDE.md claim that production-render-score costs come 'from the SAME rendered weights export will ship' is false: separate cache, 8-sample vs 32-sample calibration

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** pipeline-contract | **Category:** stale-doc
- **Where:** `/home/rob/prismaquant/CLAUDE.md` §6 code map (production_render_cost row)
- **Claim:** Allocator costs are measured on a dedicated render cache calibrated with PRODUCTION_RENDER_COST_NSAMPLES=8 samples in artifacts/production_render_score_weight_cache, while the shipped/validated cache is rendered separately with NSAMPLES=32 in artifacts/production_weight_cache* — different GPTQ Hessians and JSO act-weighting, therefore different bytes than what ships.
- **Evidence:** CLAUDE.md: "production_render_cost (default: derive allocator cost from the SAME rendered weights export will ship)". run-pipeline.sh line 127 `: "${PRODUCTION_RENDER_COST_NSAMPLES:=8}"`; lines 463-477 build the cost cache with `--n-calib-samples "$PRODUCTION_RENDER_COST_NSAMPLES"` into `--cache-dir "$PRODUCTION_RENDER_COST_CACHE_DIR"` (line 221: artifacts/production_render_score_weight_cache). The export/validation cache is built separately at lines 734-748 / 898-915 with `--n-calib-samples "$NSAMPLES"` (=32) into `--cache-dir "$PROD_CACHE_DIR"` (line 681: artifacts/production_weight_cache).
- **Why it matters:** House rule 4 requires the surrogate, KL validation, and exported bytes to be identical; here the surrogate scores renders that are not the shipped renders (4x less calibration), so allocator format choices are ranked on systematically different error than what is exported. Anyone trusting the CLAUDE.md sentence (or the design-doc equivalent) when debugging a surrogate-vs-served mismatch will look in the wrong place; the doc should be corrected or PRODUCTION_RENDER_COST_NSAMPLES defaulted to NSAMPLES.

### M28. Gemma4 KV-sharing phase-3 reconstruction is keyed by layer_type but transformers keys shared_kv_states by layer INDEX — the feature cannot work

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** streaming-profiles | **Category:** bug
- **Where:** `/home/rob/prismaquant/prismaquant/model_profiles/gemma4.py` 104-116
- **Claim:** isolated_layer_pass_state looks up the captured KV by the attention's layer_type string and returns a string-keyed dict, but the installed transformers 5.5.4 Gemma4 modeling stores and reads shared_kv_states by integer layer index. The lookup can never hit, and even if it did, the reconstructed dict is mis-keyed for the consumer. Commit a729c72's 'validated on Gemma4-31B' never exercised the path: that checkpoint has num_kv_shared_layers=0.
- **Evidence:** gemma4.py:111-116: `lt = getattr(attn, "layer_type", None); kv = captured.get(lt); if kv is not None: return {"shared_kv_states": {lt: kv}}` — lt is a string ("full_attention"). Installed transformers 5.5.4 Gemma4TextAttention: `self.kv_shared_layer_index = len(prev_layers) - 1 - prev_layers[::-1].index(...)` (an int); storing: `shared_kv_states[self.layer_idx] = key_states, value_states`; consuming: `key_states, value_states = shared_kv_states[self.kv_shared_layer_index]` (signature: `shared_kv_states: dict[int, tuple[...]]`). /home/rob/.cache/huggingface/gemma-4-31b-it-bf16/config.json: `num_kv_shared_layers: 0` — so phase-3 'validation' only needed the writable empty dict. gemma4.py:94 docstring ('shared_kv_states[type] holds the K/V') restates the wrong keying. No test references shared_kv anywhere under tests/ (grep -rln "shared_kv" tests/ returns nothing).
- **Why it matters:** The first Gemma4-family checkpoint probed with num_kv_shared_layers>0 crashes phase-3 with `KeyError: <int>` inside Gemma4TextAttention.forward (the shared layer has no k/v_proj to fall back on), after a multi-hour phase-1. The KV-sharing support that a729c72 claims to enable for probe phase-1/phase-3 is dead code on the production toolchain (prismaquant-cu130, transformers 5.5.4); only the incidental 'writable empty dict' fix is live. Additionally, gemma4.py:97-102 `except Exception: pass` in capture_forward_pass_state silently drops capture entries, moving any future failure far from its cause.

### M29. Streaming forwards apply one full causal mask to every layer; sliding-window attention layers (Gemma3/Gemma4) silently get the wrong attention pattern whenever SEQLEN > sliding_window

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** streaming-profiles | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/layer_streaming.py` 1203-1208
- **Claim:** The manual streaming forward builds a single full upper-triangular causal mask and passes it to all layers; HF's Gemma3/Gemma4 model.forward builds a per-layer-type mask mapping (sliding vs full) and the window constraint is carried entirely by the mask in eager/sdpa. ce21000 ported only the rope half of the multi-layer-type story — masks were not ported, and nothing checks SEQLEN against sliding_window.
- **Evidence:** layer_streaming.py:1203-1208: `_make_causal_mask` — "Build an additive causal mask [1, 1, T, T]. Standard upper-triangle -inf convention."; incremental_probe.py:1130 builds it once and 1177-1184 passes the same `attention_mask=causal_mask` to every layer. Installed transformers Gemma4TextModel.forward: `attention_mask=causal_mask_mapping[self.config.layer_types[i]]` (per-type sliding/full masks). Verified `eager_attention_forward` ignores the `sliding_window` kwarg (it only adds `attention_mask`), so the window lives in the mask. gemma-4-31b-it config: sliding_window=1024; run-pipeline.sh:53 `: "${SEQLEN:=1024}"` — exactly boundary-equal today, so shipped Gemma4 artifacts were unaffected only by coincidence. Gemma3TextConfig default sliding_window=512 < 1024.
- **Why it matters:** Any probe/cost run on a sliding-window model with SEQLEN above the window (e.g. bumping SEQLEN to 2048 on Gemma4, or probing any Gemma3-style arch with window 512 at the default 1024) computes phase-1 activations, phase-3 Fisher, and propagated costs under full attention on layers that serve with windowed attention — a silent serving-vs-measurement divergence on most layers (Gemma4's layer_types are ~5:1 sliding:full), corrupting the allocation that vLLM then serves. Violates 'the platform measures' with no error or warning; a hard check (SEQLEN <= sliding_window or per-type masks) is absent.

### M30. Packed-expert RTN-by-omission hard gate and its escape hatch are completely untested

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** test-quality | **Category:** test-gap
- **Where:** `/home/rob/prismaquant/prismaquant/export_native_compressed.py` 1398-1399, 5426-5450, 5678-5695
- **Claim:** The brand-new export hard-fail that bans silent RTN of non-BF16 packed experts (df12b76), and the PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN escape hatch, have zero test coverage. No test triggers the raise, pins the escape hatch as default-off, or covers the warn-only branch when no production cache is active.
- **Evidence:** export_native_compressed.py:1398 `_ALLOW_PACKED_EXPERT_RTN = (\n    os.environ.get("PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN", "0") == "1"` and :5430-5432 `if (_PRODUCTION_WEIGHT_CACHE is not None and not _ALLOW_PACKED_EXPERT_RTN): raise RuntimeError(f"[export-stream] packed expert {full} @ {fmt} has no production-cache render...")`. `grep -rln "ALLOW_PACKED_EXPERT_RTN\|RTN-by-omission" tests/` returns nothing. Note the gate is also conditional on `_PRODUCTION_WEIGHT_CACHE is not None` — a cache-less export still RTNs experts with only a print WARNING (line 5444), a branch no test pins either.
- **Why it matters:** This gate exists because every shipped MoE artifact silently RTN'd 91% of MoE params (per the fused-coherence postmortem). If a refactor inverts the condition, reads the env var wrong, or drops the raise, the next MoE export silently regresses to RTN experts again — the exact severe-quality failure the commit was written to make impossible — and the suite stays green. The env var is also read once at module import (line 1398), so a stray env in a build wrapper flips it process-wide with no test to catch it.

### M31. fill_packed_expert_cache_entries (the new packed-MoE GPTQ render, ~470 lines) has zero tests; build-side coverage gate also untested

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** test-quality | **Category:** test-gap
- **Where:** `/home/rob/prismaquant/prismaquant/production_weight_cache.py` 2864-3336
- **Claim:** The core deliverable of 9971c3e/df12b76 — rendering packed experts through the shared GPTQ path with a held-out GPTQ-vs-RTN gate, gate-weight row weighting (err² scaled by top_k weight²), joint gate/up global scale, RTN-fallback accounting, and resume (_needs_work) — has no unit test at all. The build-side hard gate (`ProductionWeightCache assignment coverage failure`) is also untested.
- **Evidence:** production_weight_cache.py:2864 `def fill_packed_expert_cache_entries(` (file is 3336 lines; this function plus its hook collector is ~570 new lines per `git diff main...HEAD --stat`). `grep -rln "fill_packed_expert_cache_entries" tests/` → no hits; `grep -rn "validate_coverage\|coverage failure\|packed_expert_coverage" tests/*.py` → no hits. build_production_cache.py:420-424 `raise RuntimeError(f"ProductionWeightCache assignment coverage failure: {len(missing)} misses, {len(failed)} failed renders...")` is likewise untested. The branch's only packed-expert tests (tests/test_measure_quant_cost_packed_experts.py) cover the COST path in measure_quant_cost.py, a different module.
- **Why it matters:** This is the newest, least battle-tested, numerically subtle code on the branch, and it renders the bytes that ship in every future MoE artifact. The held-out GPTQ-vs-RTN decision (lines ~3280-3290: `if s_rtn <= s_gptq: rendered[e] = w_rtn`), the per-expert gate-weight weighting, and the gate/up shared-scale max are exactly the kind of logic that the in-process CPU TinyModel pattern already used in test_measure_quant_cost_packed_experts could pin cheaply — a sign error or weight-transpose here produces a quietly worse artifact that only an expensive served-KL A/B would ever catch.

### M32. Fused-sibling coherence hard-fail (24b0f25) untested for both failure classes; allocator DefaultProfile gate untested

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** test-quality | **Category:** test-gap
- **Where:** `/home/rob/prismaquant/prismaquant/export_native_compressed.py` 6381-6430
- **Claim:** The new merged-column coherence RuntimeError in build_quantization_config (covering both '>=2 quantized schemes -> crash@load' and 'quant+BF16 -> silent corruption') has no test, and neither does the allocator's proactive DefaultProfile SystemExit with its --allow-default-profile escape (allocator.py:1215-1228). The only mixed-format test, test_packed_moe_mixed_format_rejected, predates the branch and exercises a different gate (the FusedMoE gate_up/down pair check at line 6269).
- **Evidence:** export_native_compressed.py:6414 `raise RuntimeError("fused-sibling coherence violation: " f"{len(fused_coherence_violations)} merged-column group(s) " "carry MIXED formats among their siblings...")` — `grep -rn "coherence violation\|fused_coherence" tests/` returns nothing. allocator.py:1215-1217 `if (using_default_profile and len(distinct_fmt_names) > 1 and not args.allow_default_profile): raise SystemExit(...)` — `grep -rn "allow-default-profile\|allow_default_profile" tests/*.py` returns nothing. `git show main:tests/test_prismaquant_export_native_compressed.py | grep -n test_packed_moe_mixed_format_rejected` → line 1460 (pre-existing, different code path).
- **Why it matters:** This gate is the fix for the bug that made ALL AURA 27B allocations silently unservable/corrupt (measured 4.3x worse served KL on DeltaNet in_proj_ba). A regression that re-introduces the old silent `continue` (which the commit explicitly replaced) ships a broken checkpoint again with zero test signal. The silent-corruption class (quant + BF16/IGNORE sibling) in particular cannot be caught by vLLM's load assert, so the unit test is the ONLY cheap guard. A 5-line test calling the real build_quantization_config with a Qwen3_5Profile and q=NVFP4/k=FP8 assignment would pin it.

### M33. test_incomplete_fused_group pins only the detector helper, not the allocator's BF16 forcing — removing the enforcement keeps the suite green

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** test-quality | **Category:** test-gap
- **Where:** `/home/rob/prismaquant/tests/test_incomplete_fused_group.py` 6, 29-65
- **Claim:** The test imports and exercises only `incomplete_fused_group_members` (a pure set-classifier) with a hand-rolled _FakeProfile. The actual serving invariant — the allocator excluding those members from the DP so they ship BF16 (allocator.py:1143-1158) — is exercised by no test; deleting the call site in allocator.py leaves every test passing.
- **Evidence:** test_incomplete_fused_group.py:6 `from prismaquant.decision_units import incomplete_fused_group_members` — all 7 tests assert only on this helper's return value with `class _FakeProfile`. The single production call site is allocator.py:1144-1147 `incomplete_members = incomplete_fused_group_members(set(stats) | set(costs), model_profile)\n    incomplete_added = sorted(incomplete_members - set(allocation_excluded))\n    allocation_excluded.extend(incomplete_added)` inside the monolithic CLI `main()`; `grep -rn incomplete_fused_group_members prismaquant/ tests/` confirms no other consumer and no test of the wiring. (The EXPORT half of the invariant IS properly pinned: test_incomplete_fused_ignore.py uses the real Gemma4Profile + real build_quantization_config.)
- **Why it matters:** The invariant (Gemma4 k_eq_v layers must ship q/k as BF16 or vLLM KeyErrors on the absent v_scale at load) is enforced solely by ~5 lines buried in allocator main(). The helper test gives false confidence that the invariant is covered; a refactor of main() that drops or reorders the exclusion (e.g. moving it after `accounting_stats = dict(stats)` consumers) re-ships an unloadable Gemma4-class artifact with no test failure.

### M34. --calib-seed and --calib-split are silently ignored on dataset-backed calibration in validate_assignments_kl (and build_production_cache)

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** validation-selection | **Category:** bug
- **Where:** `prismaquant/validate_assignments_kl.py` 391-424, 594
- **Claim:** validate_assignments_kl exposes `--calib-seed` (line 594) and `--calib-split`, but `_load_calibration_repeats` never forwards them when `--dataset` is set — `load_calibration` is called without `calib_seed`, so it always uses the default 42. An operator deliberately trying to decontaminate the validation set via a different seed gets the calibration-identical windows with no warning. build_production_cache.py has the same dead knob.
- **Evidence:** validate_assignments_kl.py:396-401/409-415: `return [load_calibration(tokenizer, args.dataset, n_samples, args.calib_seqlen,)]` and `all_ids = load_calibration(tokenizer, args.dataset, n_samples * repeats, args.calib_seqlen)` — no `calib_seed=` despite sensitivity_probe.py:1630 `*, calib_seed: int = 42`. Same pattern in build_production_cache.py:259-264 (`load_calibration(tokenizer, args.dataset, args.n_calib_samples, args.calib_seqlen,)`) even though it declares `--calib-seed` at line 97; the seed is only honored on the WikiText fallback (lines 265-272 pass `seed=args.calib_seed`).
- **Why it matters:** This makes the in-sample selection problem (finding 1) unfixable from the CLI: the only knobs that look like they decorrelate validation from calibration are no-ops on the default `--dataset` path. It also silently breaks any A/B that varies `--calib-seed` for noise estimation on jsonl calibration — all 'repeats' beyond the chunking path would be the same draw.

### M35. Gemma BOS fix (commit 3889916) is unreachable from the validator's CLI and run_validation — the ship gate still produces ~ln(vocab) garbage for BOS-sensitive exports

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** validation-selection | **Category:** bug
- **Where:** `prismaquant/validate_quantized_model.py` 448-451, 490-506
- **Claim:** check_perplexity gained `bos_token`/`add_special_tokens` parameters, but run_validation never passes them and main() defines no `--bos-token` CLI flag. No production caller in the tree passes bos_token (only unit tests; even the archived gemma4-31b launcher calls it without). So the documented pre-ship entry point (`python -m prismaquant.validate_quantized_model`) still teacher-forces Gemma-class artifacts without BOS and false-FAILs the perplexity gate. There is also no detection of BOS sensitivity (tokenizer-driven or otherwise) — the fix is opt-in via an argument nothing supplies.
- **Evidence:** validate_quantized_model.py:448-451: `rep.checks.append(check_perplexity(\n        base_url, model_name,\n        max_ppl=max_ppl, max_p99_nll=max_p99_nll, max_mean_nll=max_mean_nll,\n    ))` — no bos_token. main() argparse block (490-506) has max-ppl/max-mean-nll/max-p99-nll/min-gen-len/min-mtp-accept-p0/wait-seconds/report only. archive/entmoot/examples/launchers/gemma4-31b-validator.py:117 also calls `check_perplexity(BASE_URL, MODEL_NAME, max_ppl=50.0, ...)` without bos_token. Commit 3889916 touched only the function signature/body (19 lines in this file).
- **Why it matters:** The pre-ship gate is the last line of defense before HF upload. For Gemma-family exports that ship `add_bos_token=false` (the exact case the commit message describes: mean NLL ~11.9 / PPL ~1.5e5 on a healthy artifact), the gate as invocable today reproduces the bug the commit claims to fix — either blocking healthy ships or training operators to ignore the validator.

## MINOR (42)

### M1. Docstring claims experts get the 'identical GPTQ + JSO + damp-sweep' stack; the production default disables JSO/act-order, fixes damp, and ignores the levers argument

- **Status:** VERIFIED (3/3 adversarial lenses)
- **Finder:** moe-gptq-render | **Category:** stale-doc
- **Where:** `/home/rob/prismaquant/prismaquant/production_weight_cache.py` 2893-2898, 2907-2908, 3232-3237
- **Claim:** fill_packed_expert_cache_entries' docstring says every expert is 'rendered per-expert through render_production_weight (the identical GPTQ + JSO + damp-sweep + do-no-harm stack the dense Linears use)' and that `levers` ensures 'experts get identical treatment' — but the default `render_mode="batched"` (the only mode build_production_cache invokes, lines 389-395 pass no render_mode) calls `gptq_obs_rounding_nvfp4_batched(..., static_act_order=False, joint_scale_opt=False)` with fixed damp=0.01 and never reads `levers`; only the non-default `per_expert` mode matches the docstring.
- **Evidence:** production_weight_cache.py:2893-2896 'every non-BF16 packed expert tensor is rendered per-expert through render_production_weight (the identical GPTQ + JSO + damp-sweep + do-no-harm stack...)' vs :3232-3237 `rendered = gptq_obs_rounding_nvfp4_batched(src, render_acts, group_size=16, global_real_overrides=overrides, static_act_order=False, joint_scale_opt=False)`; :2907-2908 'levers: ... so experts get identical treatment' — `levers` is referenced only in the per_expert branch (:3282).
- **Why it matters:** Anyone (including a paper reviewer or future session) reading the function contract concludes the RTN-by-omission gap is closed with the full dense recipe. In reality experts ship without JSO, act-order, or the damp_sweep that is documented as load-bearing at 4-bit on dense Linears (+137.5% KL when disabled); the commit message discloses this tradeoff, the code's own docstring inverts it.
  - *refute lens* (stands): Evidence trail fully reproduces. The docstring paragraph at production_weight_cache.py:2893-2896 claims every non-BF16 packed expert is rendered "per-expert through render_production_weight (the identical GPTQ + JSO + damp-sweep + do-no-harm stack the dense Linears use)" and :2907-2908 claims levers gives "identical treatment" — but the default render_mode="batched" (line 2878) is the only mode invoked in production (build_production_cache.py:389-395 passes no render_mode; no env gate or other caller selects per_expert anywhere). For NVFP4 (line 3177 gate), the batched path calls gptq_obs_roun
  - *intent lens* (stands): Not refuted, but materially weakened. The code behavior is verified and is an explicitly deliberate, heavily recorded decision: the batched default (fixed damp=0.01, no JSO/act-order, levers unread) is documented in the SAME docstring's render_mode section (production_weight_cache.py:2883-2891: 'Fixed damp, no JSO/act-order (those force a per-expert loop that is ~hours on a 35B)'; per_expert 'used for the fidelity A/B that justifies the production default'), in commit 9971c3e's message ('JSO+damp a ~2x residual at 70x the wall time, deferred to a served-KL decision'), in .claude/codex-moe-gptq
  - *impact lens* (stands): Finding fully verified: build_production_cache.py:389-395 invokes fill_packed_expert_cache_entries with no render_mode, so the default "batched" (production_weight_cache.py:2878) governs all production MoE builds; the NVFP4 batched branch (:3177, :3232-3237) calls gptq_obs_rounding_nvfp4_batched with static_act_order=False, joint_scale_opt=False and fixed damp=0.01 (export_batched_gptq.py:127 — no damp_sweep), and `levers` is read only in the per_expert branch (:3282). The docstring paragraph at :2893-2898 ("identical GPTQ + JSO + damp-sweep + do-no-harm stack") and the levers arg doc (:2907-2

### M2. Do-no-harm GPTQ-vs-RTN gate is in-sample exactly for the thin-Hessian experts it exists to protect, via heuristic constants

- **Status:** VERIFIED (3/3 adversarial lenses)
- **Finder:** moe-gptq-render | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/production_weight_cache.py` 3212, 3250-3266
- **Claim:** The held-out split exists only when an expert has >= 20 routed rows (`n_eval = min(eval_rows_per_expert, n // 5) if n >= 20 else 0`); below that the GPTQ-vs-RTN comparison uses the in-sample render set (`gate_acts = ev if ev is not None else render_acts[e]`), where GPTQ — which optimizes that exact objective — essentially always wins. So the commit's 'provably non-regressive vs RTN' guarantee is vacuous precisely in the sparse-routing rank-deficient regime the surrounding comment names as the motivation. The constants 20, n//5, 128 are not objective-derived (house rule 2), and the GPTQ fit itself is unweighted while the eval is gate-weight²-weighted (`row_weights_list` is supported by the batched core but never passed).
- **Evidence:** production_weight_cache.py:3212 `n_eval = min(eval_rows_per_expert, n // 5) if n >= 20 else 0`; :3251-3253 `# held-out gate when available, else in-sample render set\n gate_acts = ev if ev is not None else render_acts[e]`; :3186-3191 comment: 'catching the rank-deficient-Hessian overfit where in-sample GPTQ always "wins" but generalizes worse than RTN (observed on down_proj at low row counts)'; :3232-3237 batched call omits `row_weights_list` while :3219-3223 weights the eval by `gw...pow(2)`.
- **Why it matters:** df12b76's own served A/B found RTN-experts beating GPTQ-experts on served KL (0.0302 vs 0.0334) with 'thin Hessians from sparse routing' as the diagnosis — i.e., the gate demonstrably under-protected, yet the export hard-fail makes the GPTQ arm the only production-reachable path. The non-regressive claim should not be cited for low-row experts, and the fit/eval objective mismatch is a measurable gap in the cost model, not the optimizer.
  - *refute lens* (stands): All cited evidence reproduces exactly in /home/rob/prismaquant/prismaquant/production_weight_cache.py: line 3212 gates the held-out split on n >= 20 with unexplained constants (20, n//5, eval_rows_per_expert=128 default at line 2874, no env gate, sole production caller build_production_cache.py:389 uses defaults); lines 3250-3253 fall back to the in-sample render set for the GPTQ-vs-RTN decision; the motivating comment (3185-3191) names exactly the low-row rank-deficient regime; the batched GPTQ call (3232-3237) omits row_weights_list even though export_batched_gptq.py:131 supports it, while t
  - *intent lens* (stands): Not refuted, but partially pre-empted by the deliberation record. The core mechanism the finding describes — experts with <20 routed rows get no held-out set and are gated in-sample, making the non-regressive guarantee scoped — was explicitly flagged by Codex in its final re-review verdict (.claude/codex-response-moe-gptq-blocker-fixes-2026-06-09.md, final codex block: "experts with <20 rows get no held-out set and are gated on the same rows used for rendering... 'provably non-regressive vs RTN' is only true for experts with held-out rows"). However, being flagged as a residual weakness in a r
  - *impact lens* (stands): Every cited fact verified against /home/rob/prismaquant/prismaquant/production_weight_cache.py: line 3212 has the n>=20 / n//5 / eval_rows_per_expert=128 (default at line 2874) heuristic split; lines 3251-3253 fall back to the in-sample render set with row_w=None when no held-out rows exist; the comment at 3185-3191 claims 'provably non-regressive vs RTN' while naming the exact in-sample-GPTQ-overfit failure mode that the n<20 fallback reintroduces; and the batched call at 3232-3237 omits row_weights_list (supported at export_batched_gptq.py:131) while the held-out eval weights by gate_weight^

### M3. is_uncached_packed_expert_qname is stale and still exempts packed experts from every build/export coverage gate, so the new hard-fail fires late or after wasted work

- **Status:** VERIFIED (3/3 adversarial lenses)
- **Finder:** moe-gptq-render | **Category:** stale-doc
- **Where:** `/home/rob/prismaquant/prismaquant/production_weight_cache.py` 105-113, 293-296
- **Claim:** The docstring ('ProductionWeightCache currently renders production-faithful 2D nn.Linear weights. Packed 3D MoE expert tensors are quantized by the packed exporter/validation fallback path') is now false post-9971c3e, and its exemption is still live in `assignment_keys` (missing packed entries silently skipped) and the export's `_production_cache_expected_keys`. Consequently build_production_cache's 'strict coverage validation' (lines 410-427) passes even when no expert was rendered, and the export's up-front expected-keys/fingerprint check skips them too — the RTN-by-omission hard-fail only fires per-tensor deep inside layer materialization.
- **Evidence:** production_weight_cache.py:106-111 docstring quoted above; :294-296 `if key is None: if is_uncached_packed_expert_qname(str(qname)): continue`; export_native_compressed.py:953-955 `if key is None: if is_uncached_packed_expert_qname(qname): continue`; build_production_cache.py:415-416 `_, missing = cache.assignment_keys(render_assignment)` is the only build-time coverage gate.
- **Why it matters:** The claimed 'HARD-FAIL on RTN-by-omission' is real but late: a cache missing expert renders sails through the build coverage gate and the export's residency/fingerprint checks, then aborts mid-export (on a 35B, after substantial materialization work). Tightening assignment_keys to stop exempting names the cache is now responsible for would move the failure to build time, and the docstring actively misleads about who renders packed experts.
  - *refute lens* (stands): All cited evidence reproduces exactly. (1) The docstring at production_weight_cache.py:105-113 is stale post-9971c3e: it claims packed 3D MoE experts are handled by an exporter/validation fallback, but fill_packed_expert_cache_entries (same file, :2864) now renders them into this very cache and export hard-fails without them. (2) The exemption is live in both coverage gates: assignment_keys (:293-296) silently skips missing keys matching _UNCACHED_PACKED_EXPERT_RE, and the export's _production_cache_expected_keys (export_native_compressed.py:953-955) does the same, so the up-front export gate 
  - *intent lens* (REFUTED): The finding's code facts are accurate, but its substantive claim — that the exemption is a stale leftover silently weakening the new hard-fail so it "fires late" — re-litigates an explicitly recorded, same-day, twice-reviewed deliberate decision. The exact concern, down to the same files and lines, was raised by Codex as a blocker (.claude/codex-response-moe-gptq-render-2026-06-09.md:6723: "_production_cache_expected_keys and ProductionWeightCache.assignment_keys still skip packed experts, so preflight/fingerprint/prefetch do not include the new 3-D expert shards. The streaming split path hard
  - *impact lens* (stands): All cited mechanics verified at HEAD: (1) the is_uncached_packed_expert_qname docstring (production_weight_cache.py:105-113) is stale post-9971c3e — the cache now renders packed experts via fill_packed_expert_cache_entries (line 2864) and the export bans the 'fallback path' the docstring describes; (2) the exemption is live in assignment_keys (:293-296), which is the sole check in build_production_cache.py's strict coverage gate (:415-416), so a cache with zero expert renders passes 'coverage check passed'; (3) the same exemption in export_native_compressed.py:_production_cache_expected_keys (

### M4. DefaultProfile fallback map protects in_proj_qkvz but not in_proj_ba; the gate's own text misstates coverage both ways

- **Status:** PARTIALLY VERIFIED (1/3 lenses before spend limit)
- **Finder:** fused-coherence | **Category:** bug
- **Where:** `prismaquant/model_profiles/default.py` 58-66
- **Claim:** DefaultProfile's hardcoded fallback fused map includes the DeltaNet in_proj_qkvz group but omits its twin in_proj_ba — the precise group whose incoherence cost 4.3x served KL in the incident. Verified live (no vLLM in the build venv): `DefaultProfile().fused_sibling_group('model.layers.3.linear_attn.in_proj_b')` → None while `...in_proj_qkv` maps to in_proj_qkvz. Meanwhile the new gate/help text in allocator.py claims DefaultProfile enforces 'only the universal fused groups (qkv_proj/gate_up_proj)' — understating actual coverage (qkvz IS enforced) while the map omits ba.
- **Evidence:** default.py:62-66 `return {\n  "qkv_proj": ("q_proj", "k_proj", "v_proj"),\n  "gate_up_proj": ("gate_proj", "up_proj"),\n  "in_proj_qkvz": ("in_proj_qkv", "in_proj_z"),\n}` — no in_proj_ba entry; allocator.py:893-897 help text `"DefaultProfile only enforces the universal fused groups (qkv_proj/gate_up_proj)"`; live check output: `default in_proj_b -> None`, `default leaf mapping: {... 'in_proj_qkvz': ('in_proj_qkv', 'in_proj_z')}`.
- **Why it matters:** Under the documented escape hatch (--allow-default-profile) or an export resolving DefaultProfile, in_proj_qkvz groups are coherence-protected and in_proj_ba groups are not — an asymmetry that reproduces the original 4.3x-KL silent corruption on exactly the group from the incident while appearing partially protected. The inaccurate gate message also misleads the operator deciding whether the escape is 'safe for vanilla transformers'.
  - *refute lens* (stands): Finding verified live: DefaultProfile's hardcoded fallback map (default.py:62-66) covers in_proj_qkvz but omits in_proj_ba (reproduced in the build venv, which lacks vLLM so the fallback map is what actually runs), and allocator.py:896/1217 text understates coverage by claiming only qkv_proj/gate_up_proj are enforced. The export coherence hard-fail gate keys off the same profile mapping, so the blind spot extends to the last defense under a DefaultProfile export. However, blast radius is narrow: (1) the default pipeline never hits DefaultProfile (probe writes meta['model']; multi-format+Defaul

### M5. aggregate_fused_siblings does not mirror build_candidates' format-alias resolution despite a comment claiming it does

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** allocator-invariants | **Category:** bug
- **Where:** `/home/rob/prismaquant/prismaquant/allocator_candidates.py` 500-512
- **Claim:** build_candidates resolves cost-pickle keys through fr.aliases_for(spec.name), but aggregate_fused_siblings checks only the exact spec.name. A legacy/alias-keyed cost pickle (e.g. entries under 'FP8_DYNAMIC' instead of canonical 'FP8_E4M3') would make every fused-sibling super-item mark that format `{'error': 'partial'}` and drop it from the fused groups' menus while ungrouped Linears keep it — a silent, systematic format skew between fused and unfused Linears.
- **Evidence:** allocator_candidates.py:501-506: `missing = [m for m in members\n           if spec.name not in costs.get(m, {})\n           or "error" in costs.get(m, {}).get(spec.name, {})]\nif missing:\n    super_cost[spec.name] = {"error": "partial"}` vs build_candidates lines 312-316: `for candidate_name in fr.aliases_for(spec.name):\n    if candidate_name in costs[name]:` — and the in-function comment at 510-511 claims '# Mirrors build_candidates, including unmeasured packed output_mse fallback.' (it mirrors only the dloss fallback, not alias lookup). Same family: line 542 `gain = float(gains.get(spec.name, 1.0))` lacks build_candidates' alias fallback `gains.get(spec.name, gains.get(entry_fmt, 1.0))` (line 343).
- **Why it matters:** Current producers canonicalize keys (aura_cost.py:239, production_render_cost.py:232), so this is dormant today — but the alias loop in build_candidates exists precisely because alias-keyed pickles occur. Re-running the allocator against an older cost pickle would silently restrict fused qkv/gate_up groups to a narrower menu than their unfused neighbors, biasing the DP with no error or mask record.

### M6. Passthrough source gating classifies by scale-sidecar presence, not dtype, and default-allows BF16 for unmapped names

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** allocator-invariants | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/allocator_candidates.py` 27-36, 627-635
- **Claim:** The 'source dtype' manifest never reads tensor dtypes: anything without a `.weight_scale_inv` sibling is labelled 'bf16', so fp16/fp32 sources — or FP8 checkpoints using the compressed-tensors `weight_scale` convention — are classified bf16, making BF16 'passthrough'-legal where it is actually a synthesized downcast (house rule 3). Separately, any Linear whose checkpoint name fails the profile recipe mapping drops out of the manifest, and both the candidate gate (source_kind=None => BF16 allowed) and the final post-allocation assertion (skips kind None) wave it through.
- **Evidence:** allocator_candidates.py:631: `source_kind = "fp8" if "weight_scale_inv" in suffixes else "bf16"` (suffix scan only, lines 585-590 look at '.weight_scale_inv'/'.weight'). Lines 34-35: `if source_kind is None:\n    return format_name == "BF16"`. allocator.py:2086-2091: `kind = source_manifest.get(name)\nif kind is None:\n    # Not in manifest ... Skip.`
- **Why it matters:** On an fp8-native source whose scale sidecars use a different suffix (e.g. a compressed-tensors-quantized upstream with `weight_scale`), the entire passthrough-integrity system — per-candidate gate AND the belt-and-suspenders SystemExit at allocator.py:2100 — would classify every Linear as bf16 and legally synthesize BF16 from dequantized FP8 ('shame!!' case), with the violation invisible because the manifest itself is wrong. Today's targets (DeepSeek-convention weight_scale_inv, bf16 sources) are covered, so this is a latent hole, not a live violation.

### M7. Final serving-unit promotion runs after achieved-bits/Δloss are computed, with no assertion that it is a no-op

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** allocator-invariants | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/allocator.py` 1982-2009, 2135-2143
- **Claim:** The emitted assignment gets one more promote_serving_units pass (allocator.py:2005) after `achieved`, `total`, the Pareto curve, knees, and CSV were all computed from the pre-pass assignment. For current profiles this pass is a no-op (packed-expert names like `experts.gate_up_proj` don't match the fused-sibling matcher, so MoE coupling already happened inside solve_with_promotion's loop), but nothing verifies that: if it ever changes a format — e.g. an arch whose expert projection leaves (`w1`/`gate_proj`) match the vLLM packed_modules_mapping regex and get swallowed into a `.__siblings__.` super-item, making the in-loop packed-group key lookup return None — every reported bpp/Δloss number and the overshoot-tightening loop are silently stale.
- **Evidence:** allocator.py:1982: `assignment, achieved, total, mutable_total = _solve_for_target(args.target_bits)`; then 2005-2009: `assignment_expanded = promote_serving_units(\n    assignment_expanded,\n    format_rank,\n    profile=model_profile,\n)` with no recompute of achieved/total afterwards and no `assert promoted == pre` check; 2135-2143 passes the stale `achieved_bits=achieved` to the bit-attribution report. The fragile precondition: structure.py:288-298 returns None for super-item names (`tail = ['__siblings__', ...]` fails both the len==1 and digit checks), so packed coupling is only enforced in-loop when expert names escape fused aggregation. vllm_registry.py:165 regex `^(.*\.)?(?:{alt})$` matches member leaves under ANY parent, including experts paths.
- **Why it matters:** The bpp number is a published, compared-against-competitors quantity. A one-line invariant check (promotion-after-expansion changed nothing, else recompute + warn) would convert a silent accounting drift into a loud failure. Note the --target-disk-gb path is immune (it sizes the post-promotion expanded assignment via _expand_assignment_for_seed_json), so only the bpp-target reporting/tightening path is exposed.

### M8. Split-per-expert packed_expert_format_group keys are per-expert, not per-layer — vLLM per-layer expert uniformity not enforced by the allocator

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** allocator-invariants | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/model_profiles/structure.py` 293-296
- **Claim:** For the split per-expert form (`...experts.7.gate_proj`), the format-group key embeds the expert index, so the allocator's union-find only forces coherence among one expert's projections; experts 3 and 9 in the same layer can legally get different formats, violating the vLLM FusedMoE per-layer-uniformity invariant (house rule 8 / the LFM2.5 lesson). The only backstop is a RuntimeError deep in export-stream, after the full cache build has been paid for.
- **Evidence:** structure.py:293-296: `elif len(tail) == 2 and tail[0].isdigit():\n    leaf = tail[1]\n    parent = ".".join(parts[:experts_idx + 2])  # includes the expert index\n    split_per_expert = True` then 306: `return f"{parent}::__packed_format__:{','.join(group)}"` — per-expert key. Same logic in base.py:551-554. Backstop at export_native_compressed.py:6267-6280: `if len(states) > 1: raise RuntimeError(f"[export-stream] FusedMoE at {fused_qname!r} has mixed states across packed expert projections...")`.
- **Why it matters:** All currently-registered specs use packed naming (`experts.gate_up_proj`, len(tail)==1, per-layer parent), so today this is unreachable from the default pipeline. But MiniMax-class per-expert checkpoints take the split path, and the design statement in this same module family says the legality gate belongs in the optimization layer ('illegal candidates are excluded before DP, rather than caught later by export', allocator_candidates.py:374-377). A late export crash after a multi-hour 27B+ cache build is exactly the failure mode that statement exists to prevent.

### M9. Default auto-chunk sizing is knowingly wrong on the default fp32 load; the fix is opt-in despite being output-identical

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** aura | **Category:** bug
- **Where:** `/home/rob/prismaquant/prismaquant/aura_cost.py` 191-194
- **Claim:** compute_aura_cost's default memory sizing hardcodes 2 bytes/weight and a single dW term, which the module's own docstring admits under-counts ~1.5-2x on the CLI-default fp32 load (--dtype defaults to float32) and 'trips the watchdog mid-run'; the corrected accounting (--accurate-chunk-bytes) is opt-in even though the docstring also states chunk count 'never perturbs the cost output' (bit-identical for any G) — so there is no bit-exactness reason to default to the broken sizing.
- **Evidence:** aura_cost.py:191-194: `if not accurate_chunk_bytes: # Legacy path, preserved bit-for-bit: 2 bytes/weight ... wgib = numel * 2 / (1024 ** 3)`. :177-180 docstring: "it silently assumes a bf16 model with one nonzero format, and under-counts by ~2x on the default fp32 load (4-byte weights+grads) or with multiple nonzero formats ... picking too few chunks and tripping the watchdog mid-run". :421 `p.add_argument("--dtype", default="float32", ...)`. :326-328 the watchdog hard-aborts: `raise RuntimeError(f"free UMA {_free_gib():.1f} < floor {min_free_gib}; abort")` — no checkpointing, all completed probe passes are lost.
- **Why it matters:** A multi-hour 27B fp32 AURA cost run on the 121 GB unified-memory box can abort mid-flight by design under the defaults; the operator must know to pass an opt-in flag that the code itself documents as the correct accounting with zero numerical effect. This is a known-bug default, not backwards compatibility.

### M10. aura cost.pkl carries no provenance: seed_base unrecorded and RTN-vs-rendered dW mixing uncounted

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** aura | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/aura_cost.py` 398-405
- **Claim:** The payload records only schema/n_probes/formats/token_scope/stats/costs — no model path, calibration provenance, git commit, or seed_base, and no per-row or aggregate count of which dW came from the production cache vs the silent RTN fallback (cost_source is always 'aura' either way; cache.get and qdq exceptions are swallowed). Both omissions are result-changing facts by the project's own validated evidence.
- **Evidence:** aura_cost.py:398-405: `return {"schema": SCHEMA, "n_probes": n_probes, "formats": fmts, "token_scope": token_scope, "stats": stats, "costs": costs}` — seed_base accepted at :217 but never persisted. :131-135 non-strict path: `try: rendered = cache.get(...) except Exception: rendered = None` then falls through to RTN with no counter. :428-431 the CLI itself documents seed sensitivity: "Vary it (same calibration) to test probe-direction stability of the allocation". Memory notes record allocation is probe-seed-noisy (±0.017 KL across seeds) and RTN-vs-GPTQ dW is decisive at FP8 (+36%). CLAUDE.md §5 gate: "Bake git commit, calibration hash, ... cache hit/miss/RTN-fallback counts into output JSON."
- **Why it matters:** Two aura cost.pkl files that produce materially different served-KL allocations (different probe seed, or different fraction of RTN-fallback dW) are indistinguishable on disk — the exact irreproducibility class the repo quarantines. A 'production-faithful' run that silently fell back to RTN for some (Linear,format) rows cannot be detected after the fact without --require-production-cache, which is off by default.

### M11. Zero unit tests for aura_cost.py, the paper-spine cost module

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** aura | **Category:** test-gap
- **Where:** `/home/rob/prismaquant/prismaquant/aura_cost.py` 206-405
- **Claim:** There is no test_aura_cost.py. The module's two load-bearing correctness claims — (a) the projection estimator 0.5*mean_k<g_k,dW>^2 matches the exact fisher_quadratic_form on a tiny model, and (b) chunked execution is 'bit-identical to the single-pass (G=1) path' (asserted three times in docstrings/help) — are never tested; neither are the zero-cost passthrough rows, the strict require_production_cache failure, nor the dw_dtype paths. test_kl_fisher.py covers only the quadratic form and probe centering, not the E[r r^T]=F estimator property aura relies on.
- **Evidence:** `ls tests/ | grep -i aura` returns nothing; `grep -rln "aura_cost|compute_aura_cost" tests/` returns nothing. aura_cost.py:230-232: "the chunked result is bit-identical to the single-pass (G=1) path"; :444-445 "(bit-identical for any G)" — untested. tests/test_kl_fisher.py:22-72 contains only test_fisher_quadratic_matches_forward_kl_second_order and test_fisher_probe_gradient_is_centered_and_respects_last_scope.
- **Why it matters:** AURA just became the paper's allocation cost (commit 5db5a9e) and its numbers back published claims (-38% served KL at 4B, -17.9% at 27B). A regression in the projection accumulation, chunk partitioning, or passthrough zeroing would currently be caught only by an expensive end-to-end GPU re-measurement, not by CI.

### M12. --include-lm-head has no tied-embeddings guard; mis-costs lm_head on tie_word_embeddings models

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** aura | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/aura_cost.py` 94-111
- **Claim:** With --include-lm-head on a weight-tied model (tie_word_embeddings=True, e.g. the Qwen3-0.6B/4B small-scale validation models), lm_head.weight is the same Parameter as embed_tokens.weight, so the retained grad includes the embedding-path contribution; the measured cost is the KL effect of quantizing BOTH uses of the shared tensor, while the export ships only a quantized lm_head view — the cost over-states (mis-states) the lm_head decision the allocator actually prices. aura_cost.py contains no tie check (grep 'tie' = 0 hits).
- **Evidence:** aura_cost.py:97-102: "include_lm_head adds it so Aura can MEASURE its KL-sensitivity ... the KL probe gradient flows directly into lm_head (it produces the logits), so its cost is measured the same way as any body Linear" — this stated assumption is false for tied weights, where grad also flows via the embedding lookup. :284 `linears[n].weight.requires_grad_(True)` operates on the shared Parameter. No `tie_word_embeddings` / `get_input_embeddings` check anywhere in the file (`grep -c tie aura_cost.py` -> 0).
- **Why it matters:** The flag exists exactly to let the allocator un-pin lm_head as a budget decision (allocator --allow-pinned lm_head, allocator.py:919-928). On the mandated 0.6B/4B validation models the lm_head cost row would be systematically wrong, and a budget decision made there would not transfer to untied 27B-class targets — an A/B confound in the path used to validate the lever.

### M13. runtime_flags.md documents PRODUCTION_RENDER_COST_SCORE_FIELD default as score_sum; code default is output_mse

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** doc-audit | **Category:** stale-doc
- **Where:** `docs/runtime_flags.md` 54
- **Claim:** The doc states the default allocator cost field for production-render-score is `score_sum` ("the GPTQ-style summed reconstruction objective"). run-pipeline.sh has defaulted to `output_mse` since 2026-05-21 (commit 8e071bf), which routes predicted_dloss through the h_trace × output_mse path instead.
- **Evidence:** Doc:54 "`PRODUCTION_RENDER_COST_SCORE_FIELD` | `score_sum` | Render-score field used as allocator `predicted_dloss`. `score_sum` is the GPTQ-style summed reconstruction objective". Code: run-pipeline.sh:130 ': "${PRODUCTION_RENDER_COST_SCORE_FIELD:=output_mse}"'; production_render_cost.py:142-158: 'if score_field == "output_mse": ... "output_mse_measured": True ... triggers the allocator's path 1 (h_trace * output_mse)'. git log -L confirms 8e071bf introduced the output_mse default; the doc was last edited 2026-05-31 (55f88cc) without correcting it.
- **Why it matters:** Anyone reproducing or A/B-ing the production cost surrogate from the doc would set score_sum believing they're restoring the default — silently changing the allocator's objective (summed reconstruction vs Fisher-weighted output MSE) and invalidating the comparison. The doc is the designated env-flag vocabulary (CLAUDE.md §11).

### M14. prismaquant/README.md lists archived polish_from_assignment as a current live entrypoint

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** doc-audit | **Category:** stale-doc
- **Where:** `prismaquant/README.md` 12, 16-18
- **Claim:** The package README's 'Current live entrypoints' list includes `python -m prismaquant.polish_from_assignment`, but that module was archived under archive/polish_2026-05-15/ (commit b9635e4) and is absent from prismaquant/. The README also says archived tools live only under archive/cross_layer_2026-05-09/, while the archive now has 17 dated walls.
- **Evidence:** prismaquant/README.md:12 "- `python -m prismaquant.polish_from_assignment`"; :16-18 "...archived for artifact replay and comparison under `archive/cross_layer_2026-05-09/`". Verification: no polish_from_assignment.py in prismaquant/ (ls); it exists only at archive/polish_2026-05-15/prismaquant/polish_from_assignment.py; git log: b9635e4 "refactor: archive Polish (polish.py + polish_from_assignment.py)". File last touched 2026-05-09 (915616e).
- **Why it matters:** The documented entrypoint fails with ModuleNotFoundError, and a reader is pointed at the wrong archive directory when looking for the polish lineage. Coord-descent polish is part of the published method narrative, so a broken pointer here wastes time exactly where a reviewer/replicator would look first.

### M15. runtime_flags.md misses 71 of 94 live PRISMAQUANT_* flags, including the new production gates, and omits the production NVFP4 joint_mse scale rule

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** doc-audit | **Category:** stale-doc
- **Where:** `docs/runtime_flags.md` 1-91
- **Claim:** Diffing `grep -rhoE 'PRISMAQUANT_[A-Z0-9_]+' prismaquant/` (94 unique flags) against the doc (26 listed, of which the 3 *_MIN_CALLS exist via a constructed name) leaves 71 code flags undocumented — including this week's production-relevant gates (PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN, PRISMAQUANT_STRICT_ASSIGNMENT_COVERAGE, PRISMAQUANT_DO_NO_HARM*) and the whole JSO family (PRISMAQUANT_NVFP4_JOINT_SCALE_LEVELS/_OPT/_GLOBAL_*). The PRISMAQUANT_NVFP4_SCALE_RULE entry documents only static_6/four_over_six_mse and labels four_over_six 'Experimental', omitting the third rule `joint_mse` that the shipping joint_scale_opt lever maps to.
- **Evidence:** Flag diff: comm of sorted code-flags vs doc-flags = 71 code-only (e.g. PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN at export_native_compressed.py:1398-1399, PRISMAQUANT_NVFP4_JOINT_SCALE_LEVELS at export_native_compressed.py:268). Doc:28 documents only "`static_6` ... `four_over_six_mse` ... Experimental until .8B/4B/27B KL and vLLM smokes land" while code export_native_compressed.py:110 defines NVFP4_SCALE_RULE_JOINT_MSE with aliases "joint_scale_opt" → joint_mse (:127), the rule the default PRODUCTION_CACHE_LEVERS=...joint_scale_opt (run-pipeline.sh:117) uses.
- **Why it matters:** CLAUDE.md designates this doc as 'the env-flag vocabulary'. The undocumented PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN is a research escape hatch that disables a quality hard-gate — exactly the kind of flag that must be documented as debug-only; and the scale-rule entry implies the shipping JSO mechanism is an unvalidated experiment, inviting someone to 'fix' the default.

### M16. README architecture/status claims: GLM-4 'active integration' has no code, DSv4 'waiting on transformers class' is vendored in-tree, shipped Gemma4/LFM2.5 profiles omitted

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** doc-audit | **Category:** stale-doc
- **Where:** `README.md` 153-177
- **Claim:** The 'Supported architectures' and 'Status' sections list GLM-4 as active integration (no profile or code exists), say DeepSeek-V4-Flash is 'blocked on transformers DeepseekV4ForCausalLM' / 'waiting on transformers class' (the class is vendored in-tree and a profile exists), and omit the Gemma4 and LFM2.5 profiles behind two artifacts already shipped publicly (2026-05-29 / 2026-06-01).
- **Evidence:** README.md:163 "**DeepSeek-V4-Flash** (waiting on `transformers` class)"; :164 "**GLM-4**"; :176 "blocked on transformers `DeepseekV4ForCausalLM`"; :155-158 first-class list = Qwen + MiniMax only. Code: prismaquant/model_profiles/ contains deepseek_v4.py, gemma4.py, lfm2_moe.py, minimax_m2.py (ls); prismaquant/vendored/transformers_deepseek_v4/ exists; no glm* anywhere in model_profiles/. (Same section's pipeline diagram at :113 also shows incremental_measure_quant_cost as the cost step, while run-pipeline.sh:126 defaults COST_MODE=production-render-score.)
- **Why it matters:** Public-facing capability claims cut both ways: GLM-4 is advertised support that doesn't exist (a user filing 'GLM-4 broken' is correct), while two architectures with shipped, downloadable artifacts are unlisted — understating exactly the breadth the README is supposed to sell, and sending a DSv4 user to wait on an upstream dependency that's already vendored.

### M17. _apply_visual_recipe_quant band-aids quant failures into BF16 passthrough while config still declares the quantized scheme

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** export-codecs | **Category:** invariant-violation
- **Where:** `/home/rob/prismaquant/prismaquant/export_native_compressed.py` 7466-7475
- **Claim:** On ANY exception during visual-Linear quantization, the export silently emits the raw BF16 weight and continues — but the Linear's non-BF16 assignment entry still routes it into a quantized config group (it is never moved to ignore), producing a disk/config mismatch; and the catch-all demotion is exactly the post-allocator band-aid house rule 1 forbids in production paths.
- **Evidence:** export_native_compressed.py:7466-7475: `except Exception as e: # Fail-safe: fall back to passthrough on any arithmetic error. Better to land a BF16 visual Linear than crash the whole export ... print(f"[export-stream] WARN visual quant failed for {base} ({fmt}): {e}; falling back to BF16 passthrough") ... out[key] = tensor; continue`. The assignment entry for `base` is non-BF16 (checked at 7452), so build_quantization_config:6124 puts it in `by_fmt` → a quantized config group; nothing adds it to ignore.
- **Why it matters:** When it fires, the exported artifact has a plain `.weight` where vLLM's compressed-tensors scheme expects `weight_packed`/`weight_scale` → load failure discovered only after a multi-hour export, with the cause buried in one WARN line. The platform norm is to hard-fail (as the FP8_SOURCE and packed-expert branches in this same file do), not to demote and continue.

### M18. Production-cache export path unifies fused-sibling input_global_scale without the model profile

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** export-codecs | **Category:** bug
- **Where:** `/home/rob/prismaquant/prismaquant/export_native_compressed.py` 1029, 6752
- **Claim:** _production_cache_scales calls _unify_input_global_scales_across_fused_siblings(scales) without passing the profile, so on the default production path (cache supplied) fused-group detection falls back to the hardcoded legacy _FUSED_DENSE_PATTERNS; profile-declared fused groups not in that list silently skip scale unification. The activation-cache path passes profile=profile (6870-6873), so the two paths diverge and the production one is the weaker.
- **Evidence:** export_native_compressed.py:1029: `return _unify_input_global_scales_across_fused_siblings(scales)` (no profile kwarg; signature at 3391-3394 defaults profile=None) vs 6870-6873: `scales = _unify_input_global_scales_across_fused_siblings(scales, profile=profile)`. With profile=None, _fused_group_key_for_name:3364-3388 only matches the legacy regex list at 3339-3350. The call site 6752 has `profile` in scope.
- **Why it matters:** For any architecture whose fused siblings aren't named like the five legacy patterns, vLLM's fused loader sees differing input_global_scale across q/k/v or gate/up and warns 'will likely result in reduced accuracy', picking one scale arbitrarily — a quality drift on the served artifact that the profile mechanism (and test test_profile_fused_group_drives_input_scale_unification) was built to prevent. One-line fix: pass profile.

### M19. Dense fused-sibling coherence hard-fail (commit 24b0f25's headline) has no test

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** export-codecs | **Category:** test-gap
- **Where:** `/home/rob/prismaquant/tests/test_prismaquant_export_native_compressed.py` 1496-1508
- **Claim:** The RuntimeError for MIXED formats among present dense fused siblings (export_native_compressed.py:6381-6430, including the quantized+BF16 'silent-corruption' case that measured 4.3x worse served KL) is exercised by zero tests. Only the packed-MoE mixed-state raise (test_packed_moe_mixed_format_rejected) and the all-BF16 incomplete-group ignore path (test_incomplete_fused_ignore.py) are covered.
- **Evidence:** Only raise-tests in the file: lines 212, 375-381, 1505 (`test_packed_moe_mixed_format_rejected ... with self.assertRaises(RuntimeError)` — packed experts only), 2100 (mtp coverage). `grep -rn 'fused.sibling coherence' tests/` returns nothing; the only repo hits are the implementation (export_native_compressed.py:6415) and allocator.py:1202/1222.
- **Why it matters:** This raise is the last gate between a DefaultProfile-corrupted allocation and an unservable/silently-corrupt shipped checkpoint (the exact failure that contaminated 'ALL AURA 27B allocs'). It is the newest, least battle-tested code in the export; a refactor of leaf_state/ignore bookkeeping could silently neuter it (e.g. revert to the pre-24b0f25 'defensively skip' behavior) and no test would notice.

### M20. Quantized head params with a plain 'weight' suffix are emitted under a key missing '.weight'

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** export-codecs | **Category:** bug
- **Where:** `/home/rob/prismaquant/prismaquant/export_native_compressed.py` 4743-4750
- **Claim:** _emit_head_param maps the compressed dict's 'weight' suffix to `base_name` (e.g. 'lm_head') instead of `base_name + '.weight'`, so an FP8/MXFP8-quantized head (formats whose codec returns {'weight': q, 'weight_scale': qs}) would write the tensor under the malformed key 'lm_head' that no loader resolves. NVFP4 heads are unaffected (suffix 'weight_packed').
- **Evidence:** export_native_compressed.py:4744-4750: `base_name = (full_qname[:-len(".weight")] if full_qname.endswith(".weight") else full_qname); out_key = (base_name if suffix == "weight" else f"{base_name}.{suffix}")`. full_qname here is always a parameter name ending '.weight' (named_parameters loop at 4757), so suffix=='weight' yields e.g. 'lm_head', not 'lm_head.weight'. _fp8_dynamic codec returns suffix 'weight' (see _pack_production_cached_2d:1322 `return {"weight": q, "weight_scale": qs}`).
- **Why it matters:** Reachable only on the documented research path (`--ignore` with no values 'for a runtime that supports quantized heads' + an FP8-class head assignment), but when used the checkpoint silently lacks lm_head.weight — the head loads uninitialized or load fails — and the bug is invisible until serve time.

### M21. four_over_six/joint NVFP4 level selection optimizes un-snapped-scale MSE while shipping FP8-snapped scales

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** format-registry | **Category:** bug
- **Where:** `/home/rob/prismaquant/prismaquant/export_native_compressed.py` 216-224, 240-255, 302-305
- **Claim:** _select_nvfp4_group_scales (four_over_six_mse and joint_mse rules) picks the {6,4} level per group by MSE computed with the real-valued scale (_nvfp4_mse_for_group_scale applies `scale.unsqueeze(-1)` directly), but the shipped dequant uses the FP8-quantized scale; the GPTQ-time JSO search (_select_nvfp4_joint_gptq_eff_scale) correctly evaluates under `quantize_fp8=True` effective scales — the two selection objectives are inconsistent, and the un-snapped one is on the production export path.
- **Evidence:** export_native_compressed.py:220-224: `_idx, dq = _nvfp4_quantize_dequantize_with_eff_scale(grouped, scale.unsqueeze(-1))` — no _nvfp4_fp8_scale_from_real before scoring — versus :436-441: `eff = _nvfp4_effective_scale_from_real(scale, global_real, quantize_fp8=True); dq = _nvfp4_quant_dequant_with_eff_scale(grouped, eff.unsqueeze(-1))`. The chosen scale_real is then FP8-snapped at :391-395 in _nvfp4_quantize_grouped_codec, so the shipped objective differs from the selection objective. Measured: on 2000 random groups of 16, 245/2000 (12%) of groups flip their {6,4} choice when the objective is evaluated under the snapped scales actually shipped. This rule fires at production export-time repack (quantize_dequantize_nvfp4 → _select_nvfp4_group_scales, called from _pack_production_cached_2d) when joint_mse/four_over_six is active.
- **Why it matters:** House rule 2 (decide from the objective): the level decision is made against an objective that is not the shipped one, so ~12% of groups (the near-tie ones) get the wrong level for the served artifact. Per-group damage is small (flips occur where the two levels are nearly tied) but it is systematic, sits on the shipped-bytes path, and the codebase already contains the correct snapped-scale evaluator four lines of plumbing away.

### M22. fill_packed_expert_cache_entries docstring contradicts its own default: batched experts get fixed-damp GPTQ, no JSO, no damp-sweep

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** format-registry | **Category:** stale-doc
- **Where:** `/home/rob/prismaquant/prismaquant/production_weight_cache.py` 2884-2899, 3232-3237
- **Claim:** The same docstring says the default 'batched' mode is 'Fixed damp, no JSO/act-order' and, two paragraphs later, that 'every non-BF16 packed expert tensor is rendered per-expert through render_production_weight (the identical GPTQ + JSO + damp-sweep + do-no-harm stack the dense Linears use)'. The second paragraph (and commit 9971c3e's 'shared GPTQ path' framing) describes only the non-default per_expert mode; in production, experts do NOT get the dense recipe.
- **Evidence:** production_weight_cache.py:2884-2887: "``batched`` (default, production): ... Fixed damp, no JSO/act-order" vs :2897-2899: "every non-BF16 packed expert tensor is rendered per-expert through ``render_production_weight`` (the identical GPTQ + JSO + damp-sweep + do-no-harm stack the dense Linears use)". Confirmed in code at :3232-3237: `rendered = gptq_obs_rounding_nvfp4_batched(src, render_acts, group_size=16, global_real_overrides=overrides, static_act_order=False, joint_scale_opt=False)` with export_batched_gptq.py:127 `damp: float = 0.01` (no 5-candidate damp sweep). The per-expert held-out GPTQ-vs-RTN do-no-harm gate (:3240-3265) is the measured mitigation.
- **Why it matters:** Anyone (human or agent) reading the docstring or the 9971c3e commit message will believe MoE experts — ~91% of MoE params — ship under the same JSO+damp-sweep recipe as dense Linears and will draw wrong conclusions in expert-render A/Bs and provenance (the build's lever metadata says joint_scale_opt/damp_sweep on). The actual default is a deliberately weaker, faster recipe justified by a separate A/B; the doc must say so, especially given df12b76's served finding that the expert-render direction is an open question.

### M23. Residency/missing checks still silently exempt packed-expert cache entries after the RTN-by-omission fix

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** format-registry | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/production_weight_cache.py` 105-113, 293-297
- **Claim:** is_uncached_packed_expert_qname and its callers predate 9971c3e: the cache now DOES render 3-D packed experts, but assignment_keys (and the export's _production_cache_expected_keys) still drop missing packed-expert entries from the 'missing' list instead of flagging them, so an incomplete/skipped expert fill passes every prefetch/residency gate and is only caught by the export-stream hard-fail hours into a build. The function's docstring is also now false.
- **Evidence:** production_weight_cache.py:108-111 docstring: "``ProductionWeightCache`` currently renders production-faithful 2D ``nn.Linear`` weights. Packed 3D MoE expert tensors are quantized by the packed exporter/validation fallback path, so missing cache entries for those names must not fail production-cache residency checks" — contradicted by fill_packed_expert_cache_entries (:2864+) which stores 3-D expert entries in the same cache. :293-297: `if key is None: if is_uncached_packed_expert_qname(str(qname)): continue` (silently skipped, not appended to `missing`); same exemption at export_native_compressed.py:952-955. assignment_keys feeds prefetch_assignment (:414) and assignment_file_paths (:315).
- **Why it matters:** The early, cheap gates (prefetch residency, expected-keys fingerprinting of the export layer cache) are blind to absent expert renders, so the only protection against shipping RTN-by-omission experts is the late export hard gate (which PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN can disable). A resumed or partially-filled cache wastes a full build before failing, and validation paths that rely on assignment_keys cannot tell a complete expert fill from an empty one.

### M24. Research codebooks are wrong: E5M2 includes Inf/NaN codes; FP6 codebooks have no subnormals

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** format-registry | **Category:** bug
- **Where:** `/home/rob/prismaquant/prismaquant/format_registry.py` 305-322, 341-351
- **Claim:** _e5m2_codebook generates values for all 32 exponents with no clip, so it contains 65536/81920/98304/114688 — encodings that are Inf/NaN in real E5M2 (max finite 57344); the _e4m3_codebook clips to 448 but e5m2 has no equivalent. _e3m2_codebook and _e2m3_codebook treat exponent 0 as normal instead of OCP subnormal, producing non-existent codes and missing real ones.
- **Evidence:** format_registry.py:341-351: `for exp in range(32): ... val = (1 + m / 4) * (2 ** (exp - 15))` with no clip (contrast :337-338 e4m3's `if abs(c) <= 448.0`). Verified: `fr._CODEBOOKS['fp8_e5m2'].abs().max() == 114688.0`; codes above 57344 = [65536, 81920, 98304, 114688]. e3m2 smallest nonzero codes = [0.125, 0.15625, 0.1875, 0.21875] vs OCP subnormals {0.0625, 0.125, 0.1875}; e2m3 codes <1 are a 0.0625-step grid {0.5..0.9375} vs the real 0.125-step subnormal grid. Knock-on: with cmax=114688 instead of 57344, _snap_scale_e8m0's element_offset for MXFP8_E5M2 is one binade off versus the export's _fp8_element_dtype_and_max-based packing, so registry RTN error for MXFP8_E5M2 diverges from export bytes.
- **Why it matters:** Any cost measurement of MXFP8_E5M2/MXFP6 through the registry under-states or mis-states quantization error (values land on codes the hardware cannot represent), so research ablations comparing these formats against NVFP4/FP8 are quietly invalid. Bounded blast radius: MXFP8_E5M2 is deny-listed for serving and MXFP6 has no vLLM kernel, so no shipped artifact is affected — but these registry entries exist precisely to rank research formats, and they rank them with wrong math.

### M25. Mixed-and-incomplete fused groups bypass the export hard-fail, and the justifying assumption is the failure mode itself

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** fused-coherence | **Category:** risk
- **Where:** `prismaquant/export_native_compressed.py` 6349-6380
- **Claim:** In the coherence pre-pass, any group with an absent sibling takes the `if absent_leaves:` branch and `continue`s before the mixed-state check; only the all-IGNORE case is emitted. So a group like q=NVFP4, k=IGNORE, v=absent records no violation and emits no fused target — failure is deferred to vLLM load. The in-code justification ('a mixed-and-incomplete group should not reach here' because the allocator's incomplete_fused_group_members pins present members to BF16) holds only when the allocator resolved the correct profile — but incomplete_fused_group_members (decision_units.py:82-118) reads the same profile leaf map, so under --allow-default-profile an arch-specific incomplete group escapes both the allocator pin and the export gate.
- **Evidence:** export_native_compressed.py:6351 `if absent_leaves:` ... :6375-6380 `if set(present) == {"IGNORE"}: ... continue` — the mixed check `if len(set(states)) != 1:` at :6381 is unreachable for any group with an absent sibling; :6360-6362 comment `(Quantized incomplete groups are pinned to BF16 upstream by the allocator's incomplete-fused-group rule, so a mixed-and-incomplete group should not reach here.)`.
- **Why it matters:** The acknowledged partial-assignment ambiguity makes the silent skip defensible, but the net effect is that the one gate guarding a known artifact-destroying state has a documented hole whose closure depends on an upstream rule that shares the gate's blind spot (the profile). A mixed-and-incomplete arch-specific group ships and fails only at vLLM load — after upload in the worst case.

### M26. _call_layer silently substitutes an arbitrary rope entry for untagged layers on multi-rope models

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** hygiene-landmines | **Category:** risk
- **Where:** `prismaquant/layer_streaming.py` 1148-1157
- **Claim:** On multi-layer-type-rope models (Gemma3/Gemma4: distinct sliding vs full rope_theta), a layer whose layer_type is missing/unknown gets whatever rope entry happens to be first in the dict, with no warning — in the production probe/cost forward path, so a tagging mismatch corrupts the Fisher/cost measurement silently.
- **Evidence:** layer_streaming.py:1152-1157: `pe = pe.get(lt)` then `if pe is None:` → comment `# Unknown/missing layer_type — fall back to any entry rather than crash` → `pe = next(iter(position_embeddings.values()), None)`. No print/log/counter. Callers on production paths: incremental_probe.py:1177 and :2197 (`out = _call_layer(...)`), sensitivity_probe.py:2486/2530.
- **Why it matters:** House rule 1: if the measurement is wrong the allocator converges on the wrong assignment. Feeding a sliding-window rope to a full-attention layer (or vice versa) during the probe produces wrong activations and wrong h_trace for that layer with no signal anywhere; a one-line warning would make the failure observable. Added in the newest, least battle-tested hardening commit (8953514).

### M27. Repo hygiene: stale audit docs tracked at root, a tracked file inside the 'not to publish' scratch/ dir, root-owned egg-info

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** hygiene-landmines | **Category:** cleanup
- **Where:** `audit_findings.md` 1
- **Claim:** audit_findings.md + audit_questions.md (dated 2026-05-22 point-in-time audit notes) are git-tracked at the repo root rather than docs/; scratch/smoke_graph_memory.py is git-tracked even though .gitignore declares scratch/ local-only; prismaquant.egg-info/ at root is owned by root (docker leftover); texput.log and tmp/ clutter the root (both correctly ignored).
- **Evidence:** git ls-files shows `audit_findings.md`, `audit_questions.md`, `scratch/smoke_graph_memory.py` tracked. .gitignore: `# Local reference materials (third-party papers / code; copyrighted, not to publish)\nreferences/\nscratch/` — yet the pre-existing tracked file inside scratch/ still ships (gitignore does not untrack). ls -la: `drwxr-xr-x 2 root root 4096 May 13 11:14 prismaquant.egg-info` (root-owned; *.egg-info/ is gitignored but the dir needs sudo to remove), `-rw------- texput.log` (May 4 LaTeX droppings, ignored via *.log), `tmp/` with May-21 run dirs (ignored).
- **Why it matters:** This is a public GitHub repo. audit_findings.md describes bugs as current ('Finding 1: MXFP4 E8M0 Scale Encoding Disagreed...') that were fixed in May — exactly the stale-doc rot the prime directive warns about, sitting at top level where readers find it first. The tracked scratch/ file contradicts the dir's declared local-only policy, and the root-owned egg-info blocks `git clean`/rm without sudo (the documented docker bind-mount landmine).

### M28. FP8-rung '6× lower KL' claim rests on an in-sample (train-split) resident RTN screen, undisclosed in prose

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** paper-audit | **Category:** risk
- **Where:** `/home/rob/prismaquant/paper/main.tex` 496-499 (and figures/fig_aura_rd_geometry.tex:14,50)
- **Claim:** The §5 claim that adding FP8 bends the 4B frontier to a knee 'near 8 bpp (6× lower KL at matched bytes)' is sourced from a resident hook-emulated, RTN, weight-only KL measured on the calibration TRAIN split (in-sample). The figure caption discloses 'RTN weight-only' but neither figure nor prose discloses the resident emulation or the train-split measurement, and the prose carries no qualifier at all.
- **Evidence:** main.tex:497-499 'the two-rung frontier is near-linear while $\{$NVFP4, FP8, BF16$\}$ bends to a knee near $8$~bpp ($6\times$ lower KL at matched bytes)'. Source data /home/rob/dq-runs/aura-sat-validate-4b/artifacts/vfk_rtn.json & vfk_fp8.json: '"calib_split": "train"', '"assignment_materialization": "hooks"', '"production_cache": null', n_calib_samples=4, seqlen=256. Figure y-axis says only 'measured KL vs. BF16'. The auto-memory note for this exact result (fp8_missing_rung) says 'confirm on served vLLM KL next' — never done.
- **Why it matters:** §5's central structural claim (the menu makes the knee) is supported by a number that fails both halves of the paper's own shipping criterion stated at main.tex:214-219 (served metric, held-out split). The geometry argument is probably robust, but the 6× figure is screen-quality; at minimum the prose needs the 'resident RTN screen, train split' qualifier, and ideally the two finalists get one served confirmation point each.

### M29. CLAUDE.md still describes the retired PrismaSCOUT paper as paper/main.tex

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** paper-audit | **Category:** stale-doc
- **Where:** `/home/rob/prismaquant/CLAUDE.md` 41-43, 79-80, 507-510
- **Claim:** Post-commit 5db5a9e, CLAUDE.md's description of the paper is stale on four counts: the title, the claimed contents of main.tex (monotone-polish proposition, downstream-regression tables, rejected-methods catalog — all now only in paper/archive/), the 'key evidence figure', and the standing claim that the paper has a 'Methods Considered and Rejected' section.
- **Evidence:** CLAUDE.md:41 'PrismaQuant and PrismaSCOUT: Operational Rate-Distortion for Mixed-Precision LLM Quantization (paper/main.tex)' — actual main.tex:63 title is 'Aura: Production-Faithful KL--Fisher Allocation...'. CLAUDE.md:507-509 'paper/main.tex — the thesis, the monotone-polish proposition (and its scope caveat), the honest downstream-regression tables, and the full rejected-methods catalog. paper/figures/fig_validated_27b.tex is the key evidence figure' — main.tex contains none of these and its only figure include is main.tex:506 '\input{figures/fig_aura_rd_geometry.tex}'; the old paper lives at paper/archive/prismascout_paper_2026-06-05.tex. CLAUDE.md:79-80 'The paper's "Methods Considered and Rejected" section exists on purpose' — no such section exists in the canonical main.tex.
- **Why it matters:** CLAUDE.md is the durable brief every agent session reads first; it currently sends readers to a nonexistent thesis, figure, and section. Separately worth Robert's explicit sign-off: the rejected-methods catalog — a stated deliverable of the project's honest-accounting ethos — did not carry over into the new AURA spine at all (the new paper has Limitations but no graveyard section).

### M30. One-cache rule 'structural enforcement' in pipeline.py is a write-only contract; the spec also records stages the run never executes

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** pipeline-contract | **Category:** stale-doc
- **Where:** `/home/rob/prismaquant/prismaquant/pipeline.py` 19-26, 539-545, 929-975
- **Claim:** APPROVED_RESOURCE_OWNERS is enforced only via PipelineSpec.validate() on the spec JSON that pipeline.py itself generates at run start; no production stage imports or consumes pipeline_spec.json, so an ad-hoc cache in any stage module would never be caught. The written spec additionally declares validate.kl and validate.vllm_smoke stages that the default surrogate run never executes.
- **Evidence:** run-pipeline.sh lines 298-317 generate+validate the spec once; grep shows `load_pipeline_spec` is referenced only inside pipeline.py itself (pipeline.py:600, 1124) — no stage module reads the spec. pipeline.py:3-6 admits this: "This module is intentionally descriptive first ... Execution stays in the current GPU-bound probe/cache/export paths until call sites opt into these contracts." pipeline.py:929-975 declare `validate.kl` (gated by gate.validation.end_kl) and `validate.vllm_smoke` stages, but in SELECTION_MODE=surrogate run-pipeline.sh runs no KL validation and never runs the vLLM smoke (it only prints the command, lines 1005-1008). CLAUDE.md §4.8 says the one-cache rule "is structurally enforced (pipeline.py APPROVED_RESOURCE_OWNERS)".
- **Why it matters:** The enforcement claim in CLAUDE.md overstates reality — the recent packed-MoE RTN-by-omission bug (commit 9971c3e) is proof a parallel render path lived undetected under this 'enforcement'. The pipeline_spec.json baked into WORK_DIR/artifacts reads as provenance that KL validation and a vLLM smoke were part of the run; a reader auditing an artifact directory would over-trust what was executed (metadata selection_mode is the only tell).

### M31. MSE_PROMOTION=1 is silently ignored outside the validated-surrogate + production-cache branch

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** pipeline-contract | **Category:** bug
- **Where:** `/home/rob/prismaquant/prismaquant/run-pipeline.sh` 814-842
- **Claim:** The MSE-promotion rewrite block is nested inside `if [[ "$SELECTION_MODE" == "validated-surrogate" ]]` (line 685), itself inside the PRODUCTION_CACHE-enabled block (line 675). With the default SELECTION_MODE=surrogate, setting MSE_PROMOTION=1 (plus its required TARGET_BPP/MAX_BPP_DELTA) does nothing and prints nothing — no error, no log line.
- **Evidence:** Line 814 `if [[ "$MSE_PROMOTION" != "0" ...` appears between the frontier selection (line 805) and the digest recache (line 844), all within the validated-surrogate arm opened at line 685. Contrast the script's house style of failing fast on inert config: lines 671-674 error when validated-surrogate lacks PRODUCTION_CACHE, and lines 820-823 error when MSE_PROMOTION lacks a budget — but only once inside the branch.
- **Why it matters:** The env block (lines 139-141) sells MSE_PROMOTION as 'an explicit recipe arm for testing'; an operator running that arm under default surrogate selection gets a baseline artifact silently mislabeled as the promotion arm — the same silent-no-op A/B failure mode the archive guards exist to prevent.

### M32. Dead H_DETAIL plumbing and unreachable LEVER_CACHE_TAG branch

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** pipeline-contract | **Category:** cleanup
- **Where:** `/home/rob/prismaquant/prismaquant/run-pipeline.sh` 180, 237-239, 381-386, 412-454, 677-680
- **Claim:** PROBE_H_DETAIL_ARGS/COST_H_DETAIL_ARGS/PROD_H_DETAIL_ARGS are initialized empty (lines 237-239) and never appended anywhere (grep and `git log -S 'PROBE_H_DETAIL_ARGS+='` both come up empty), so the H_DETAIL_DIR knob (line 180, echoed in the config banner at line 293) is a no-op and the ~45 lines of h-detail reuse-guard code (381-386, 412-454, including an embedded python heredoc) are unreachable. Likewise LEVER_CACHE_TAG can only become '_fisher' via FISHER_WEIGHTED_GPTQ != 0 (lines 677-680), which the archive guard at lines 163-171 turns into `exit 2` before reaching there.
- **Evidence:** Lines 237-239 `PROBE_H_DETAIL_ARGS=() COST_H_DETAIL_ARGS=() PROD_H_DETAIL_ARGS=()`; only other references are expansions at lines 341, 409, 747, 914, 956 and the `-gt 0` guards at 381 and 412. Lines 163-171: `echo "... Fisher levers are archived ..." >&2; exit 2` for any truthy FISHER_WEIGHTED_GPTQ; line 679 `*) LEVER_CACHE_TAG="${LEVER_CACHE_TAG}_fisher" ;;` is therefore dead.
- **Why it matters:** H_DETAIL_DIR is printed in every run's config banner as if it does something, misleading operators and future auditors; the dead heredoc guard (412-454) reads like a live compatibility check and cost real review time. Dead config surface in the orchestrator is exactly where the next stale-reuse bug hides.

### M33. Even with correct keying, phase-3's detached captured KV severs the Fisher contribution of shared-consumer layers to the storing layer's k/v_proj

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** streaming-profiles | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/model_profiles/gemma4.py` 92-102
- **Claim:** capture_forward_pass_state snapshots KV with `.detach().to("cpu")` and phase-3 forwards each layer in isolation with grad chained only through x_in (incremental_probe.py:2323 `grad_out = x_in.grad...`). On a KV-sharing checkpoint, gradients flowing from the N consumer layers through the borrowed K/V back to the storing layer's k_proj/v_proj (and to its input hidden) are discarded, so those Linears' h_trace systematically under-counts their true sensitivity.
- **Evidence:** gemma4.py:99: `out[lt] = tuple(t.detach().to("cpu") for t in kv)`; incremental_probe.py:2197-2209: isolated `out = _call_layer(layers[L], x_in, ...)` then `out.backward(grad_out.to(device))`; 2323: `grad_out = x_in.grad.detach().clone().cpu()` — the only cross-layer gradient channel is the hidden state, but Gemma4 KV sharing is a skip connection from layer M's k/v_proj directly into layers N>M (Gemma4TextAttention: `key_states, value_states = shared_kv_states[self.kv_shared_layer_index]`).
- **Why it matters:** Once finding #1's keying bug is fixed and a real num_kv_shared_layers>0 model is probed, the storing layers' k/v_proj — which feed every shared consumer layer's attention — get Fisher measured as if they fed only their own layer, biasing the allocator to under-bit exactly the highest-fan-out attention Linears. This is a measurement gap (house rule: fix the cost model, not the optimizer) that will not crash and produces no warning.

### M34. _call_layer silently applies an arbitrary rope when a multi-rope layer's type cannot be resolved — band-aid pinned by test

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** streaming-profiles | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/layer_streaming.py` 1147-1157
- **Claim:** When position_embeddings is a multi-entry {layer_type: (cos, sin)} dict and a layer exposes none of layer_type/self_attn.layer_type/attention.layer_type (or its type is missing from the dict), the code falls back to `next(iter(position_embeddings.values()))` — an arbitrary rope — instead of failing. For a model with distinct sliding/full ropes this silently applies the wrong frequencies to the whole forward.
- **Evidence:** layer_streaming.py:1153-1157: `if pe is None:  # Unknown/missing layer_type — fall back to any entry rather than crash ... pe = next(iter(position_embeddings.values()), None)`. tests/test_multilayer_rope_forward.py `test_call_layer_dict_unknown_type_falls_back` pins the behavior: `layer = _Layer(layer_type="missing")... assert layer.received == ("x", "x")  # falls back to an entry, no crash`. Gemma3/Gemma4 in the installed transformers do tag `self_attn.layer_type`, so the trigger today is a future multi-rope arch or an HF attribute rename.
- **Why it matters:** House rule 1: hardening that swallows a structural error is a band-aid. The failure mode is exactly the silent-corruption class 24b0f25 just cleaned up — an entire probe/cost/KL run completes with wrong rotary frequencies on some layers and no error, producing a plausible-looking but corrupt allocation. The safe behavior when the dict has >=2 distinct entries and the type is unresolvable is a hard fail (the single-entry fallback is fine).

### M35. WeightSession.initialize records the target format in _current before materialization succeeds — failed installs leave a silent assignment/weights desync

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** streaming-profiles | **Category:** bug
- **Where:** `/home/rob/prismaquant/prismaquant/weight_session.py` 274-293
- **Claim:** initialize() sets `self._current[qname]` to the assigned non-BF16 format, then `continue`s without rolling _current back if `_format_weight` returns None ('cache miss; leave at BF16') or the live weight is unavailable. The session then believes the unit is quantized while the live weight is still BF16: current_assignment() lies, and a later stage_format() to that same format no-ops (new_canon == prev_fmt), so polish 'measures' a flip that never happened.
- **Evidence:** weight_session.py:279 `self._current[qname] = fr.canonical_format_name(fmt)` precedes 286-291: `replacement = self._format_weight(qname, self._current[qname])\n if replacement is None:\n     continue  # cache miss; leave at BF16\n live = self._live_weight(qname)\n if live is None:\n     continue`. Contrast apply_assignment (361-380), which only updates `self._current[qname] = new_canon` after the successful `live.copy_(...)`. Under the default strict cache (line 81-85, PRISMAQUANT_STRICT_PRODUCTION_CACHE default True) a pure cache miss raises instead, so the reachable window is meta/unmaterialized live weights or non-strict runs.
- **Why it matters:** Polish's non-regression guarantee is 'provably no worse under the fixed evaluator' — that proof assumes the evaluated assignment is the materialized one. A desynced _current makes coord-descent measure and report KL for an assignment that differs from what it claims (and from what export later renders), invalidating accepted/rejected flips with no diagnostic (the unit is not counted in _initialize_missing, which only tracks unresolvable qnames).

### M36. Multimodal visual-probe streaming forward never threads profile pass-state; per-sample except would silently eat the resulting Gemma4 crash

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** streaming-profiles | **Category:** test-gap
- **Where:** `/home/rob/prismaquant/prismaquant/sensitivity_probe.py` 2486-2492, 2530-2536
- **Claim:** run_multimodal_visual_probe_pass calls _call_layer without new_forward_pass_state()/extra kwargs, unlike incremental_probe phase-1/phase-3 (a729c72 only wired the hooks there). On Gemma4 multimodal, the decoder layer receives shared_kv_states=None and the storing layers' unconditional `shared_kv_states[self.layer_idx] = ...` raises TypeError; the surrounding per-sample `except Exception ... continue` then skips every sample, completing the probe with empty visual Fisher.
- **Evidence:** sensitivity_probe.py:2486-2492: `out = _call_layer(layers[L], hidden, position_embeddings=..., attention_mask=causal_mask, position_ids=position_ids,)` — no `**pass_state` (compare incremental_probe.py:1183 `**pass_state,`). Gemma4TextDecoderLayer.forward (installed transformers): `shared_kv_states: dict[int, ...] | None = None`, and Gemma4TextAttention.forward stores `shared_kv_states[self.layer_idx] = key_states, value_states` when store_full_length_kv (true for the last layer of each type even at num_kv_shared_layers=0). The loop's tail: `except Exception as e: print(f"[probe/mm-stream] sample {i}: ..."); ...; continue`.
- **Why it matters:** Latent today (Gemma4 modality towers are BF16 passthrough per the profile; the mm probe currently serves Qwen3.6), but the moment Gemma4 multimodal calibration is wired — explicitly planned in gemma4.py's docstring ('until we wire real multimodal calibration') — the visual probe degrades to a no-op that reports success, the worst failure mode for a measurement platform. A shared helper that threads pass_state through every _call_layer site would close it.

### M37. test_run_pipeline_defaults does not pin FORMATS (FP8-in-every-recipe), SELECTION_MODE, or TARGET_BITS defaults

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** test-quality | **Category:** test-gap
- **Where:** `/home/rob/prismaquant/tests/test_run_pipeline_defaults.py` 4-58
- **Claim:** The test correctly parses the real run-pipeline.sh (good — not a copy), but its assertions cover only cache/cost-mode/archive strings. The defaults `FORMATS:=NVFP4,FP8_DYNAMIC,BF16` (the standing 'FP8 should be included in every recipe' rule), `SELECTION_MODE`, and `TARGET_BITS:=4.75` are unpinned, so dropping FP8_DYNAMIC from the default menu would pass the suite. Assertions are also bare substring checks that a commented-out line would still satisfy.
- **Evidence:** test_run_pipeline_defaults.py:9-22 asserts only `"PRODUCTION_CACHE:=1"`, `"PRODUCTION_RECACHE:=1"`, `"COST_MODE:=production-render-score"`, `"PRODUCTION_CACHE_LEVERS:=gptq,static_act_order,joint_scale_opt"` plus archive pointers; `grep -n FORMATS tests/test_run_pipeline_defaults.py` → no hits, while run-pipeline.sh:45 reads `: "${FORMATS:=NVFP4,FP8_DYNAMIC,BF16}"`.
- **Why it matters:** A two-rung NVFP4+BF16 default menu was explicitly declared never-shippable (it existed only for cost-model A/B isolation), and the AURA work created multiple branches/scripts where exactly that menu was used. If a future A/B convenience edit lands in run-pipeline.sh's default, every downstream recipe silently loses its FP8 middle rung (3-8x KL at matched bpp per the FP8-rung result) and this 'defaults' test — the file whose entire purpose is default-drift detection — stays green.

### M38. Surrogate-vs-KL Spearman and worst_rank_inversion are computed only on the dominance-filtered frontier — survivor bias makes the fidelity diagnostic near-vacuous

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** validation-selection | **Category:** risk
- **Where:** `prismaquant/select_validated_frontier.py` 520-521, 196-200
- **Claim:** main() computes the surrogate-fidelity diagnostics on `frontier`, not on `results`. measured_frontier keeps a row only if its measured KL strictly improves on every lower-bpp row, so frontier KL is monotone-decreasing in bpp by construction; the candidates the surrogate misranked (measured KL out of bpp order) are exactly the ones filtered out before the statistic is taken. The Spearman is therefore biased toward +1.0 and the worst-rank-inversion can only see surrogate non-monotonicity among already-concordant survivors.
- **Evidence:** select_validated_frontier.py:520-521: `rank_corr = spearman_rank_correlation(frontier)` / `worst_inversion = worst_rank_inversion(frontier)` while `results` (line 477) is available; measured_frontier filter at 196-199: `for row in rows:\n        if row["kl"] < best_kl - floor - 1e-12:\n            frontier.append(row)` after sorting by bpp — dominated (misranked) rows never reach the diagnostics.
- **Why it matters:** These diagnostics exist to detect exactly the failure CLAUDE.md's graveyard documents ('outside the additive trust region, bpp order ≠ KL order' — surrogate knee 5.857/0.056 vs validated 5.31/0.015 on 27B). Computed post-filter, a badly misranking surrogate can still print spearman≈1.0 in the selection summary, giving false confidence in surrogate-mode runs and in any paper claim sourced from `surrogate_spearman`.

### M39. validation_harness end-KL calibration is drawn from WikiText train with a hardcoded seed 42 and no override, while PPL uses test

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** validation-selection | **Category:** risk
- **Where:** `prismaquant/validation_harness.py` 700-714
- **Claim:** _fixed_calib_ids builds the harness's end-KL sample set from `split="train"` with `random.Random(42)` and exposes no split/seed parameter, while _wikitext_ppl correctly uses `split="test"`. For any artifact whose costs/calibration were generated from WikiText train (the loaders' default split in calibration_data.py:79 and build_production_cache.py:96), the harness's reported end_kl is measured on the calibration corpus, not held-out text.
- **Evidence:** validation_harness.py:702-706: `ids = _load_wikitext_ids(\n        tokenizer,\n        load_dataset,\n        cache_dir=cache_dir,\n        split="train",\n        n_tokens=None,\n    )[0]`; line 713: `rng = random.Random(42)`. Contrast _wikitext_ppl at 360-365: `split="test"`. calibration_data.py:79 `split: str = "train"`, build_production_cache.py:96 `--calib-split", default="train"`.
- **Why it matters:** The harness's end_kl feeds artifact-registry records used for cross-artifact comparison. On WikiText-calibrated builds it shares the train corpus (and the same fixed seed across every invocation), so it inherits the in-sample optimism of finding 1 and cannot be decorrelated without code changes; the memory note about re-serving AURA 'on WikiText test' confirms held-out end-KL currently requires manual out-of-repo tooling.

### M40. No reproducibility provenance in the selection-driving output JSONs (git commit, calibration hash, assignment hash, cache hit/miss counts all absent)

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** validation-selection | **Category:** stale-doc
- **Where:** `prismaquant/validate_assignments_kl.py` 990-1023
- **Claim:** CLAUDE.md §5 states as a gate: 'Bake git commit, calibration hash, assignment hash, cache hit/miss/RTN-fallback counts into output JSON.' The validated_frontier_kl.json payload and the select_validated_frontier summary contain none of these: the `out` dict carries calibration *parameters* (seed/split/n) but no data hash, no git commit, no assignment hash, no cache hit/miss counters. Only kl_sensitivity_probe.py emits `git_commit`.
- **Evidence:** validate_assignments_kl.py:990-1023 `out = {"model": ..., "probe": ..., "calibration": {"n_calib_samples": ..., "calib_seed": ...}, ... "results": results}` — no hash/commit keys; `assignment_hash` is imported (line 36) but used only as a CUDA-graph key (line 518). Repo-wide grep for git_commit/provenance hits only kl_sensitivity_probe.py:171/3206 and sensitivity_response.py:266. select_validated_frontier.py:539-560 summary keys likewise lack provenance.
- **Why it matters:** CLAUDE.md documents 4-8x cross-session KL drift and declares irreproducible numbers quarantined; the JSONs that pick the shipped assignment can't be tied back to a code revision or calibration bytes after the fact, so a drifted selection run is indistinguishable from a healthy one. Either the doc's claim is stale or the implementation never landed in this path.

### M41. Validator 'p99 NLL' is actually max: the p99 index computation is dead code, immediately overwritten

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** validation-selection | **Category:** cleanup
- **Where:** `prismaquant/validate_quantized_model.py` 343-352
- **Claim:** check_perplexity computes a p99 index over per-prompt NLLs, then unconditionally overwrites it with the max, but still reports the value under the key `p99_nll_per_tok` and CLAUDE.md/report language calls the gate 'p99 per-prompt NLL'.
- **Evidence:** validate_quantized_model.py:344-347: `k = max(0, int(0.99 * len(per_prompt_avg_nll)) - 1)\n    p99 = per_prompt_avg_nll[-1] if len(per_prompt_avg_nll) <= 2 else per_prompt_avg_nll[k]\n    # Actually for small N, "p99" is basically "max" — use max for clarity.\n    p99 = per_prompt_avg_nll[-1]`; reported at 352 as `"p99_nll_per_tok": p99`.
- **Why it matters:** With the fixed 12-prompt suite max==p99 in effect (and max is the stricter, intended gate per the 27B postmortem), so behavior is fine — but the metric name in ship reports and registry JSON is wrong, and if anyone enlarges EVAL_PROMPTS expecting a percentile they'll silently get a max-gate with different sensitivity. Lines 344-345 are dead code.

### M42. _profile_excludes_bpp_name ends in a dead two-branch return False — a format-conditional bpp exclusion was lopped off

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** validation-selection | **Category:** cleanup
- **Where:** `prismaquant/validate_assignments_kl.py` 105-107
- **Claim:** The final block of the bpp-exclusion predicate is `if fr.canonical_format_name(fmt) != "BF16": return False` followed by `return False` — both branches return False, so the `fmt` parameter and the BF16 check are dead. The shape strongly suggests an intended exclusion of profile-forced/ignored BF16 entries from the reported bpp denominator (house rule 7) that was truncated.
- **Evidence:** validate_assignments_kl.py:105-107: `    if fr.canonical_format_name(fmt) != "BF16":\n        return False\n    return False`.
- **Why it matters:** If forced-BF16 Linears (e.g. incomplete fused-sibling groups forced to BF16 + ignore list on this branch, commits 07454e7/63b29aa) are not covered by `is_pinned_name`/passthrough prefixes, they count at 16 bits in the validation rows' bpp, skewing the (bpp, KL) frontier coordinates the kneedle selects on and the bpp labels recorded for shipped artifacts. At minimum the dead code should be resolved to whatever exclusion was intended.

## INFO (5)

### I1. Post-cliff kneedle trim and RD log-linearity test use heuristic constants (half-log-range, 1-decade gate, R-squared >= 0.99)

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** allocator-invariants | **Category:** cleanup
- **Where:** `/home/rob/prismaquant/prismaquant/allocator.py` 160-178, 287
- **Claim:** Three constants govern the knee machinery without an objective derivation: the catastrophic-prefix trim fires only when the log-error span exceeds 1.0 decade and cuts at the midpoint 0.5*(ymin+ymax); the RD diagnostic declares 'no intrinsic knee' at r2 >= 0.99. These are heuristics in the house-rule-2 sense, but they live exclusively in diagnostic/advisory paths: the default ship selection is an explicit TARGET_BITS (run-pipeline.sh:46,654) or the exact byte-budget selector, and the validated-surrogate ship pick uses measured-KL kneedle in select_validated_frontier, not these.
- **Evidence:** allocator.py:174-177: `if ymax - ymin < 1.0:\n    return 0\nthreshold = 0.5 * (ymin + ymax)`; allocator.py:287: `log_linear = r2 >= 0.99`. Consumption is print-only / knees.json sidecar (allocator.py:1808-1828 'suggested knee'); the emitted layer_config comes from args.target_bits (line 1982) or the byte-budget override (line 1954).
- **Why it matters:** Low stakes today because the knee was explicitly demoted to a diagnostic (--knee-refine default OFF, rd_curve note). The risk is doc/handover drift re-elevating 'the allocator kneedle' to a selection mechanism (CLAUDE.md still describes 'allocator + log-error kneedle' as the spine); if that ever happens, these constants become unprincipled ship decisions.

### I2. find_saturation_bpp bisection assumes a monotone within-noise predicate that the project's own measurements violate at the margin

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** aura | **Category:** risk
- **Where:** `/home/rob/prismaquant/prismaquant/saturation_select.py` 84-113
- **Claim:** The bisection returns the leftmost within-band index only if 'within z*stderr of the asymptote' is prefix-false/suffix-true over the bpp grid. The docstring's robustness argument (comparing each probe to the asymptote rather than its neighbour) does not restore the invariant: one spurious within at mid permanently discards the upper half without re-validation, and one spurious not-within discards every lower point including true within-band ones. The repo's own data shows distortion is non-monotone in bpp at the margin (Qwen3-4B: 5.5 bpp beats 6.0 bpp).
- **Evidence:** saturation_select.py:85-87: "Monotonicity assumption: distortion is (weakly) decreasing in bpp; the bisection is robust to mild non-monotonic noise because it compares each probe to the asymptote, not to its neighbour." :100-113: `if within: best_i = mid_i; hi_i = mid_i else: lo_i = mid_i + 1` — O(log n) probes, no confirmation pass on the returned B*.
- **Why it matters:** B* selects the shipped bpp in the unconstrained mode (VALIDATED_FRONTIER_PICK=saturation). With n=8-style KL noise (between-seed std ~0.02 per CLAUDE.md), a single noisy probe can move the shipped artifact a full grid rung in either direction with no trace-level detection; the assumption is disclosed but unenforced and uncheckable from the trace alone. Mitigated in practice by the auto-bumped calib-repeats>=4 noise floor (run-pipeline.sh:187-196).

### I3. Stale in-code comment: batched NVFP4 export described as 'disabled by default' but v26 defaults it ON

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** export-codecs | **Category:** stale-doc
- **Where:** `/home/rob/prismaquant/prismaquant/export_native_compressed.py` 4930-4946
- **Claim:** The comment block introducing grouped_nvfp4_batched says the path is 'Activated by env var PRISMAQUANT_BATCHED_NVFP4_EXPORT=1 — disabled by default while the path is being validated', but the code ten lines below defaults it ON when the env var is unset.
- **Evidence:** export_native_compressed.py:4931-4933: '# Activated by env var PRISMAQUANT_BATCHED_NVFP4_EXPORT=1 — disabled by default while the path is being validated against the per-Linear baseline.' vs 4940-4945: '# v26: default ON. Set PRISMAQUANT_BATCHED_NVFP4_EXPORT=0 to revert' / `_batched_env_on = (True if _raw_batched is None else ...)`. docs/runtime_flags.md:27 correctly documents the ON default.
- **Why it matters:** Anyone auditing export determinism (e.g. for an A/B or a bit-exactness check against the per-Linear baseline) who reads the first comment will assume the slow provably-correct path is active by default and misattribute byte differences. Trivial fix: delete the stale v23 comment.

### I4. _build_target_list docstring promises enumerated alternation for sparse expert subsets; code always emits the [0-9]+ wildcard

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** export-codecs | **Category:** stale-doc
- **Where:** `/home/rob/prismaquant/prismaquant/export_native_compressed.py` 3146-3148 vs 3178-3186
- **Claim:** The docstring says 'if every expert index 0..N-1 is present we emit a [0-9]+ regex; sparse subsets get an enumerated alternation', but the implementation ignores the collected expert-index set entirely and unconditionally emits the wildcard, relying on per-layer expert uniformity being enforced elsewhere.
- **Evidence:** export_native_compressed.py:3146-3148: 'Within a (layer, proj) bucket, if every expert index 0..N-1 is present we emit a `[0-9]+` regex; sparse subsets get an enumerated alternation.' vs 3175-3186: `for (prefix, L, inner, proj), _experts in sorted(bucketed.items()): ... # Always emit the [0-9]+ wildcard ... expr = "[0-9]+"` — `_experts` is never read.
- **Why it matters:** collapse_config_groups.py reuses this function to retroactively rewrite already-shipped HF configs. If a (hypothetical, supposedly-impossible) within-layer mixed-format unpacked-expert config were collapsed, two config groups would emit identical wildcard regexes over the same Linears and vLLM's first-match dispatch would silently reassign experts — the docstring describes the safe behavior, the code implements the assumption. At minimum the doc should match the code and state the uniformity precondition.

### I5. 13 of 14 figure files in paper/figures/ are orphaned; legacy PDF and animation left in paper/ root

- **Status:** UNVERIFIED (verification cut off by spend limit)
- **Finder:** paper-audit | **Category:** cleanup
- **Where:** `/home/rob/prismaquant/paper/figures` n/a
- **Claim:** Only fig_aura_rd_geometry.tex is \input by main.tex; the other 13 PrismaSCOUT-era figure files (fig_validated_27b.tex, fig_cost_hierarchy.tex, fig_kneedle.tex, fig_coord_descent.tex, etc.) are dead, and paper/ root still carries prismaquant_prismascout_paper.pdf and prismaquant_animation.html alongside the archive copy.
- **Evidence:** grep 'input{figures' main.tex → single hit at main.tex:506 '\input{figures/fig_aura_rd_geometry.tex}'; ls paper/figures/ shows 14 .tex files; paper/ root contains prismaquant_prismascout_paper.pdf (536KB, May 4) while the retired paper is already archived at paper/archive/prismascout_paper_2026-06-05.{tex,pdf}.
- **Why it matters:** Low stakes, but stale figures whose numbers contradict the new spine (e.g. fig_validated_27b's PrismaSCOUT evidence) sitting next to the canonical paper invite exactly the doc-rot this repo's prime directive warns about; either move them under paper/archive/ or delete.

## Coverage notes by finder (what was checked and found correct / unreachable)

### moe-gptq-render

Scope reviewed: commits 9971c3e and df12b76 in full (diffs + current HEAD state) across production_weight_cache.py (fill_packed_expert_cache_entries, _PackedExpertActivationCollector, do-no-harm gate, sidecar resume), measure_quant_cost.py (derive_per_expert_activations, _measure_packed_experts, _packed_router_topk usage), export_native_compressed.py (both streaming and in-memory packed-expert paths, hard-fail gates, escape hatch, manifest), build_production_cache.py (expert fill call site, coverage gate), export_batched_gptq.py (batched GPTQ core), plus the downstream consumers: run-pipeline.sh (surrogate + validated-surrogate flows), production_recache.py, production_render_cost.py, perturbed_x_cache.py, weight_session.py, layer_state_cache.py, validate_assignments_kl.py, kl_measurement.py, build_rtn_cache.iter_quantizable_tensors, render_score.score_render_error.

Verified CORRECT (no findings): (1) One-cache rule for export bytes in the surrogate flow — both export paths read the 3-D GPTQ dequant from the same ProductionWeightCache (`_read_cached_packed_expert` → cache.get) and re-pack per expert with the same re-derive-from-dequant approximation the shipped 2-D path uses; the per-expert NVFP4 global scale is computed identically at fill (production_weight_cache.py:3158-3166, max over gate/up slices from source) and at export (per_expert_joint from the source proj_split), so render and pack use consistent globals. (2) The export hard-fail gates are present and symmetric in both streaming (:5429-5443) and in-memory (:5680-5692) paths, including the resume-scale trap (cached render without calibrated input_global_scale → RuntimeError for NVFP4). (3) Per-expert routing is correct: derive_per_expert_activations routes the captured module input through the block's own router (prefers route_tokens_to_experts), builds expert e's rows via one-hot expert_mask so the Hessian sees only tokens routed to e; down_proj gets the post-SwiGLU intermediate; subsampling is deterministically seeded; the function raises (never silently degrades) on missing router/act_fn; zero-row experts get source RTN, never the batched core's all-zero output. (4) Build-time fail-fast on missing activations: fill raises RuntimeError if a module's captured X is absent (production_weight_cache.py:3101-3107) — no silent RTN at cache build. (5) df12b76 OOM/leak fixes are sound: reservoirs stored CPU-side fp32; per-module derived activations + captured X freed at the module's last in-scope param via last_idx_by_module (in_scope ordering groups a module's params contiguously since it is built in one named_modules pass); empty_cache per iteration; activation tensors are explicitly `.to(device, float32)` before GPTQ and scoring (lines 3116, 3210), avoiding the CPU-resident-matmul landmine. Residual retention is bounded to ~one extra fp32 packed param + one module's act lists across a single loop iteration (src/render_acts persist until reassigned) — bounded, and a full 35B build/export/serve run validated it; not reported as a finding. (6) The escape hatch is env-gated and default-off ("0"), cannot affect cache builds, and the hard-fail correctly remains active when the flag is unset; the gate-weight² held-out objective matches served MoE semantics (output scaled by gate weight). (7) The batched GPTQ core's per-Linear Cholesky retry degrades only the failing expert, and dead columns are zeroed to match the per-Linear path. (8) Cache key naming is consistent end-to-end (fill stores under the live module path, export looks up the state-dict name, identical for Qwen3.x packed experts; cache._name_candidates covers aliases). (9) validate_assignments_kl inplace mode hard-fails (raise) on missing expert cache entries rather than silently degrading — only hooks mode has the silent fallback reported in findings.

Could NOT check (read-only audit, no GPU): any numeric claim (6.7-21x error reduction, the 0.0302 vs 0.0334 served A/B, ~13 min/35B timing), actual vLLM serving of a re-packed expert artifact, whether `.to('cpu', non_blocking=True)` in the collector hook has a D2H race on this torch build (CUDA makes pageable D2H host-synchronous; left unreported as unconfirmable here), and whether the priority reservoir's importance sampling biases the gate_up max_abs scale. The held-out 'do-no-harm' arithmetic was reviewed statically only.

### fused-coherence

VERIFIED CORRECT (this matters for the audit): (1) Both gates from 24b0f25 exist and work as advertised at their sites: the allocator gate is actually a HARD ERROR (SystemExit) on multi-format menu + resolved DefaultProfile (allocator.py:1213-1226), keyed off the RESOLVED profile so an unregistered-arch-with-path is also caught; menu de-duped by format name; single-format menus correctly exempt (trivially coherent, and fixed-format aux assignments — MTP/visual/pinned — are uniform within their own groups). The export warn on DefaultProfile exists (export main :6677-6691). (2) The export fused-sibling gate covers ALL group kinds the profile declares — q/k/v, gate/up, shared_expert gate/up, in_proj_qkvz, in_proj_ba — because it iterates packed_mapping from _fused_modules_mapping_for_profile; the packed-MoE gate (:6267-6280) is keyed per fused_qname (per layer), correctly allowing experts to differ ACROSS layers while forcing uniformity within one FusedMoE; the mixed classification (crash@load vs silent-corruption) matches the states logic. (3) base.py auto-derivation degrades safely for REGISTERED profiles even without vLLM: vLLM is NOT importable in the build venv (verified: ModuleNotFoundError in prismaquant-cu130), and all 7 structure specs declare fused_groups; live-ran Qwen3_5Profile without vLLM and it resolved qkv/gate_up/in_proj_qkvz/in_proj_ba groups and the full leaf mapping correctly, so the export gate is NOT blind on registered archs in the production venv. (4) Union-find promotion (allocator_solver.py:68-109) promotes each connected component to the single max-rank format; format_rank is index-in-sorted-by-effective_bits (unique per name, allocator.py:1264-1265) so uniformity holds by construction; legacy promote_fused has an explicit post-assert (:187-197). (5) Cost aggregation for fused super-items is a SUM of per-member predicted_dloss and a SUM of per-member memory bytes (allocator_candidates.py:499-549) — correct additive semantics; super-item candidate menu is the INTERSECTION of member format sets, which also blocks illegal BF16-passthrough on fused groups. (6) BF16/FP8_SOURCE passthrough legality IS re-checked after promotion on the final assignment and hard-fails (allocator.py:2073-2107); the check is skipped only when source_manifest is None (no model path), which the DefaultProfile gate already blocks for multi-format menus. (7) Pareto seed assignments for the validated-surrogate path are expanded AND promoted under the resolved profile before being written (allocator.py:1604-1609, 1654). (8) solve_with_promotion promotes INSIDE the budget loop and re-tightens on overshoot (allocator_solver.py:375-408) — promotion is budget-aware. (9) Packed-expert config_groups emit vLLM CANONICAL scheme names via _vllm_moe_scheme_projection_names (export :6216-6223), with per-layer regexes pinned to expert-0 projection names; tested in tests/test_moe_expert_projection_names.py (real ModelProfile base-class path). (10) Tests: test_incomplete_fused_ignore.py exercises the REAL build_quantization_config with the REAL Gemma4Profile (production path, asserts absent v_proj + fused qkv_proj land in ignore and that complete quantized layers do NOT); test_incomplete_fused_group.py exercises the real production function incomplete_fused_group_members (fake but contract-faithful profile — acceptable since the real-profile integration is covered by the ignore test); test_packed_moe_mixed_format_rejected covers the packed-MoE hard-fail with the real Qwen3_5Profile. (11) The in-repo probe (incremental_probe.py:735) writes meta['model']; the allocator reads it (:1085) and run-pipeline.sh's allocator invocation relies on it (no --model-override passed) — consistent with the commit claim. (12) The incomplete-fused-group BF16 pin (decision_units.py:82-118, wired at allocator.py:1143-1154) is profile-driven and runs before the DP. (13) MSE_PROMOTION rewrite defaults OFF (run-pipeline.sh:141 `: \"${MSE_PROMOTION:=0}\"`) and groups by serving_unit; noted (not filed) that its _group_key uses fused_sibling_group but not packed_expert_format_group, so if enabled with expert categories it could split packed-MoE units — gated behind an explicit opt-in, would be caught by the (late) export gate. (14) DefaultProfile DOES generically enforce packed-MoE format groups for standard `...experts...` names via the experts-token fallback (base.py:530-567, tested in test_allocator_sibling_aggregation.py:316), so the gate message slightly overstates the packed-MoE blind spot — conservative direction, not filed. COULD NOT CHECK (and why): resident_fisher_probe.py meta['model'] fix lives under /home/rob/dq-runs (outside this repo; file not found at the root path); actual vLLM load/serve behavior on mixed groups (read-only review, no GPU/serving allowed); behavior of build_quantization_config's secondary call sites with partial assignments (the stated reason for not failing on absent+quantized) — I found only the main() call site at :7269 in this file, but did not exhaustively audit external importers beyond tests.

### aura

VERIFIED CORRECT (read-only; no GPU runs, so all served-KL/Spearman/0.00%-vs-real-export claims in docstrings were treated as unverifiable external evidence, not re-confirmed):

KL-Fisher math (kl_fisher.py, read fully): hand-derived E[probe probe^T] for both gaussian and rademacher noise = diag(p) - p p^T (the categorical Fisher), with the 1/sqrt(token_count) factor giving F/T per selected position — exactly matching fisher_quadratic_form's normalization (0.5*variance.mean(), mean over positions, sum over vocab) and the 1/temp^2 factor consistent across both functions (kl_fisher.py:69-74, 97-124). test_kl_fisher.py is real (second-order match vs true forward-KL at 8% rtol; scope/centering invariants), though it does not cover the probe-covariance identity.

aura_cost.py estimator (read fully, 525 lines): predicted_dloss = 0.5 * (1/K) * sum_k <g_k, dW>^2 (lines 344-348, 394) is an unbiased estimator of the unary Fisher-KL quadratic; reductions over tokens match the gold-metric convention (token_scope='all', per-position mean); accumulation is fp32 (g.float(), dW upcast .float(), python-float i.e. fp64 running sums) and the CLI default loads the model fp32, satisfying the fp32-additivity requirement; bf16 dW storage is documented with an opt-out (--dw-dtype float32). No routing-probability division for experts — consistent with codex 2026-06-08's 'gradient already includes selected gate weights; weighting again double-counts'. No double counting across fused units: per-Linear unary costs are summed by union-find promotion (allocator_candidates.py:512), which is correct under fp32 additivity. Zero-cost passthrough rows (BF16/FP8_SOURCE) are correctly delegated: BF16 downcast hazard guarded by opt-in assert_bf16_passthrough; FP8_SOURCE legality left to the allocator's passthrough-integrity gate — no House-Rule-3 violation inside aura. Chunked execution argument (deterministic seeds; forward values independent of requires_grad partition) is sound in design, though untested (finding). _free_gib's MemAvailable gating self-documents its discrete-GPU wrongness (lines 169-173) — acceptable as a documented platform constraint.

Integration / defaults: AURA is strictly opt-in — zero references in prismaquant/run-pipeline.sh or pipeline.py; COST_MODE menu unchanged (local | production-render-score | production-render-staged; grouped-kl et al. still fail fast, run-pipeline.sh:205-232); SELECTION_MODE unchanged (surrogate | validated-surrogate). The --target-disk-gb byte-budget selector and VALIDATED_FRONTIER_PICK=saturation are opt-in; saturation auto-bumps calib-repeats to 4 (run-pipeline.sh:187-196), correctly defusing the stderr=0 degenerate. The DefaultProfile multi-format hard-fail from commit 24b0f25 is present and proactive (allocator.py:1213-1228) with --allow-default-profile as the explicit escape — the fused-coherence fix landed as described. allocator's _assignment_bits_total (1611-1619) uses memory_bytes_for_shape, so the --target-disk-gb path's bytes are consistent with footprint's body term; the ratchet (1924-1952) provably never ships below the proven-feasible grid pick.

footprint.py (read fully): residual-floor identity internally consistent; fp8-regime correctly removes the source weight_scale_inv from the floor (reencoded_source_bytes_for_shape), source_regime is robust to the large-vocab-fp8 trap that fools dominant_source_bytes_per_param, header-only reads (no torch/weight load). test_footprint.py is real (hand-computed totals, a regression test for the old scalar-src-bpp double-count, BF16 net-zero passthrough check). The '0.00% vs real exports' claim is explicitly delegated to an external verification pass; the uncounted NVFP4 per-tensor fp32 global scale (~4 B/tensor) is negligible at that precision. bpp/bytes convention: footprint counts only assignment (re-encoded) tensors; everything else stays at source bytes — consistent with the quantizable-only convention.

saturation_select.py (read fully): select_under_byte_budget verified against its docstring (largest-fits, inclusive <=, tie-breaks bytes -> lower loss -> higher bpp; below_floor/has_slack/rejected_next edge cases) — test_bitrate_select.py exercises all of them plus the zero-stderr degenerate and z-monotonicity; not tautological. bootstrap_mean_ci's inf stderr at n=1 would invert the degenerate (band=inf -> B* collapses to LOWEST bpp), but the only production caller sanitizes non-finite/<=0 stderr to 0.0 (select_validated_frontier.py:34-42), making the wired behavior safe (asymptote).

Codex docs vs code: 2026-06-08 (.out) recommended packed-expert AURA behind an opt-in flag with streaming capture + gates; 2026-06-09 (.out) declared the gate FAILED and pivoted to hybrid empirical expert costs — neither the packed-expert AURA nor the hybrid empirical expert cost exists in the tree (finding 1). The strict same-quantizer concern from 6-09 is partially addressed by the pre-existing --require-production-cache flag.

COULD NOT CHECK: GPU-dependent claims (bf16-dW Spearman 0.997, the -38%/-17.9% wins, footprint's 0.00% match vs the three real 27B artifacts, the 35B gate numbers) — read-only constraint; their artifacts live under /home/rob/dq-runs outside the repo. Whether any out-of-repo scratch implements the agreed hybrid expert cost was not searchable within scope. vLLM-side behavior of a quantized unpinned lm_head ('serving unverified' per the allocator help) was not verifiable without a serve.

### allocator-invariants

VERIFIED CORRECT (allocator core invariants, branch claude/fix-issues-4-6, read-only):

1. PASSTHROUGH integrity: PASSTHROUGH_SOURCE_REQUIREMENTS exists and covers both BF16 and FP8_SOURCE (allocator_candidates.py:18-21); per-candidate gating runs inside check_format_applicability (104-113) before profile/shape checks; the belt-and-suspenders post-allocation assertion exists (allocator.py:2081-2107), runs AFTER fused expansion + final serving-unit promotion, and raises SystemExit on violation — so promotion outputs ARE covered when a manifest exists. FP8_SOURCE is never allowed with source_kind None (line 34-35 returns True only for BF16). Pareto seed JSONs are written without this check, but I verified the consumer re-validates: kl_sensitivity_probe._seed_assignment_point calls check_format_applicability with source_kind=source_manifest.get(member) per member and resets illegal units to the floor (kl_sensitivity_probe.py:1004-1026), plus _assert_fused_assignment_coherent (1044-1049). Bit-attribution is read-only over the already-checked final assignment. MTP drift assertion (allocator.py:2060-2066) catches promotion changing MTP pins; visual stamping is uniform so trivially coherent.

2. bpp accounting convention: profile-pinned names (incl. lm_head) and incomplete fused groups are removed from stats/costs before the DP (allocator.py:1129-1164); MTP and visual Linears are pulled into a fixed/auxiliary pool excluded from body budget AND body Δloss, reported separately (fixed_total_bits/params/dloss, 'budget_scope': 'auxiliary_excluded_from_body_budget', lines 1465-1549); the Pareto sweep's achieved_bits (compute_achieved over body names only), the kneedle inputs, the seed records' bits_total (budget_expanded excludes aux; bits_total_with_aux is separate), and the bit-attribution report (_build_bit_attribution skips _is_visual_linear/_is_mtp_linear at line 605) all use the same body-only convention — no mismatch found. compute_achieved prefers chosen-candidate memory_bytes, then super-item _memory_bytes_by_format, then effective_bits_for_shape — consistent with how candidates priced bits. --allow-pinned is an explicit, announced opt-in convention shift (substring match could over-unpin, but it is operator-supplied and logged). Commit 0d1b76f's auxiliary exclusion is intact after the AURA/packed-MoE commits for the standard probe+cost path.

3. Multi-choice knapsack DP (allocator_solver.solve_allocation): infeasible target (target < min_bits - 1e-6) returns None and main() raises a clear SystemExit (allocator.py:1983-1986); per-layer option sets always retain the baseline (dbins=0, dgain=0) with an explicit fallback, so dp[0] >= 0 after every layer and dp.max() never selects an unreachable cell; bin rounding error is bounded and re-checked exactly via compute_achieved + the overshoot-tightening loop (tolerance 0.01); ties break deterministically to the earlier (cheaper) option via strict '>' masks; backtracking recomputes dbins with identical rounding so the cur<0 clamp and idx_chosen<0 fallback are dead defensive code. Two cosmetic notes (not findings): `np.isfinite(dp).any()` is always True since -1e30 is finite (the dp.max()==INF_NEG check is the real guard and works), and the INF_NEG sentinel relies on float64 absorption for dgain < ~1.4e14 — fine for realistic predicted_dloss magnitudes. solve_with_promotion's stall/infeasible-retry returns can exceed the budget tolerance, but the returned achieved is honest and printed/recorded, so no silent garbage.

4. Cost-source precedence (allocator_candidates.cost_entry_predicted_dloss / cost_entry_source): order is measured output_mse (optionally fisher_output_mse behind PRISMAQUANT_FISHER_OUTPUT_MSE_ALLOCATOR) > predicted_dloss > h_trace*weight_mse. Verified the producers are consistent: aura_cost writes predicted_dloss with output_mse_measured=False and cost_source 'aura'/'aura_passthrough_zero' (aura_cost.py:384-397); production_render_cost's score_sum entries carry output_mse but explicitly set output_mse_measured=False (production_render_cost.py:176-182) so they correctly route through predicted_dloss; the packed-expert output_mse=0.0 placeholder is demoted (allocator_candidates.py:204-219). The reported cost-source counter matches the value actually used. Stale-pickle precedence across FILES is orchestration-level (run-pipeline skip-if-exists) and out of these files' control; within a single pickle there is no silent override. Legacy entries with output_mse but no output_mse_measured key are treated as measured — matches the schema note that older artifacts omit the field, and those older producers did measure output_mse.

5. Fused/packed coherence: aggregate_fused_siblings prices super-items via the member-format INTERSECTION (so a member's masked format can never be promoted onto it via aggregation); promote_fused has a hard AssertionError post-check on residual mixed groups (allocator_solver.py:187-198); format_rank values are unique (enumerate over sorted specs) so the rank-tie next() in promote_fused cannot pick a foreign format; _promote_group_components promotes only to a format some member actually holds; compute_assignment_predicted_dloss raises AssertionError if promotion ever lands a format with no scored candidate, so mispricing fails loud. Probe stats for packed experts use packed param names (sensitivity_probe.py:667, '{qname}.{pn}' e.g. experts.gate_up_proj/down_proj per all current specs), which do NOT match the vLLM fused-sibling regex, so packed-MoE coupling is enforced IN-LOOP via _packed_groups_by_profile and the budget tightening sees it (finding 4 documents the unguarded precondition). Incomplete fused groups: incomplete_fused_group_members is generic/profile-driven and the allocator excludes them from the DP (export side forces BF16+ignore — confirmed mixed-and-incomplete handling exists at export_native_compressed.py:6352-6401, not deeply audited, out of scope). DefaultProfile multi-format gate (allocator.py:1213-1228) keys off the RESOLVED profile and is bypassable only via explicit --allow-default-profile — house-rule compliant.

6. House rule 1 scan: no hardcoded format bans or heuristic demotions found in the allocator production path. The MTP→BF16 and visual-format overrides are CLI-controlled auxiliary-sidecar pins with documented measurement gaps (text-only calibration gives zero visual Fisher), excluded from body accounting — consistent with rule 7/12. The format-family-coherence same-bit-tier check is warn-only by default (error only behind --enforce-family-coherence). The byte-budget selector (select_under_byte_budget + the ratchet bisection) is exact and objective-driven.

7. layer_config.py: single canonical parser; canonicalize_format mappings are self-consistent (FP8_SOURCE keyed on group_size 128 fp8_e4m3, MXFP8 on 32, FP8_E4M3 on 0/-1); load_assignment validates the payload schema. decision_units.py: block_id_from_qname, fused_group_key, discover_units (graph + fallback) check format applicability per member and take per-member memory bytes; incomplete_fused_group_members logic verified against the matcher's parent+leaf key shape.

NOT CHECKED / OUT OF SCOPE: footprint.py byte-exactness (relied on its '0.00% vs real exports' validation claim only for context, not for any finding); select_validated_frontier's measured-KL kneedle; export-side BF16+ignore handling of allocator-excluded names; whether run-pipeline.sh stage skip-if-exists can pair a stale cost pickle with a fresh probe (orchestration dimension, not these files); GPU-path behavior (read-only review, nothing executed beyond grep/python JSON reads). The fp16/fp32-source BF16-synthesis hole (finding 3) was confirmed in code but I could not enumerate a current production source it bites — rated accordingly.

### export-codecs

Scope: prismaquant/export_native_compressed.py (7558 lines, navigated by grep + targeted reads), collapse_config_groups.py (read fully), tests/test_prismaquant_export_native_compressed.py (test list + key tests), tests/test_incomplete_fused_ignore.py (read fully), plus supporting reads of production_weight_cache.py (is_uncached_packed_expert_qname, assignment_keys), build_production_cache.py (coverage check), model_profiles/base.py+structure.py (split_packed_experts_for_format), specs/*.json, and run-pipeline.sh export stage (to confirm which CLI args the production path passes). Read-only throughout; no pipeline/GPU execution, so vLLM load behavior is reasoned from the config/disk contract, not observed.

Verified CORRECT (notable):
- Mixed-scheme fused-group hard-fail (24b0f25): build_quantization_config:6381-6430 RAISES (not warns) for mixed present siblings, covering both >=2-quant-schemes and quantized+IGNORE ('silent-corruption') cases; leaf_state includes assignment + bf16_passthrough + extra_ignore, so a sibling demoted to ignore still triggers the raise. It runs inside write_config_with_quantization, which main() calls unconditionally AFTER shard writes — so the export-cache resume path (layers replayed at 4890-4902) cannot skip it; on violation no config.json is written, so no servable artifact ships. Packed-MoE mixed states raise separately at 6267-6280 (states include 'IGNORE'), tested at test 1496.
- Packed-MoE RTN-by-omission gate (9971c3e): hard RuntimeError in BOTH streaming (5429-5443) and inmemory (5680-5691) paths when a production cache is active but lacks the expert render; PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN escape hatch is explicitly research/A-B-only with a loud warning (1394-1399, 5444-5450). Missing calibrated input_global_scale for a cached NVFP4 expert also hard-fails (5459-5468, 5702-5709).
- Production-cache completeness: main() hard-fails upfront on missing 2-D recipe entries and missing backing files (6734-6747); fingerprint binds export cache to shard mtimes/levers/act scales (962-1019). The packed-expert exemption in the upfront check (is_uncached_packed_expert_qname, production_weight_cache.py:99-113 — its docstring is now stale post-9971c3e, saying experts are quantized 'by the packed exporter/validation fallback path') only defers to the mid-export hard fail.
- Packed-expert config emission: config_groups use vLLM CANONICAL gate_proj/up_proj/down_proj via _vllm_moe_scheme_projection_names (6216-6223), while on-disk tensor names keep profile projection names (w1/w3/w2 for LFM2.5) at 5470-5475; BF16 packed-expert ignore regexes are layer-pinned, canonical-named, and MTP/body namespace-separated (5902-5997); tests 1275, 1329, 1378, 1410, 1427 cover these. Per-expert enumerations collapse to per-(layer,proj) regexes; preformed 're:' targets pass through without double-wrapping (3154-3161); collapse_config_groups is idempotent and reuses the same _build_target_list.
- FP8_SOURCE verbatim copy (recipe-assigned case): fp8 bytes copied with no dequant/requant roundtrip (5034-5054), dtype asserted fp8_e4m3fn (5049), scale_inv renamed to weight_scale (both are dequant multipliers; scheme at 5821-5840 declares block [128,128] + per-tensor dynamic activations with the vLLM per-act-token assert documented); missing scale_inv hard-fails (5022-5031); head/packed FP8_SOURCE raise NotImplementedError rather than guess (4721-4728, 5370-5381).
- BF16 passthrough-only (rule 3) holds in the TENSOR path: _passthrough_tensor/_passthrough_dtype (4459-4488) preserve the source dtype and raise when unknown; on fp8 sources the BF16 view is never written (flipped to FP8_SOURCE) — the violation found is on the CONFIG side only (finding 1).
- ignore construction: lm_head et al. pinned via profile.pinned_names() default (6593-6599, 6917-6921); compute_extra_ignore covers every unmapped 2-D source weight incl. visual towers, and correctly EXCLUDES per-expert children whose packed parent is quantized (6520-6560, tests 2355/2394); incomplete fused groups force absent sibling + fused name into ignore when present members are BF16 (6349-6380, test_incomplete_fused_ignore.py), and the deliberate non-failure for quantized-present+absent-sibling is documented as the allocator's responsibility (6363-6374); final ignore is deduped via sorted(set()) (6488); no tensor can be in both a config group and ignore for the same name (BF16/ignore entries `continue` before by_fmt at 6110-6123; mixed parent cases raise).
- No 'Linear' class-name catch-all (6446-6481, test 1510); catch-all group is explicit names + profile per-expert regexes, canonical-rewritten only when on-disk names differ (6465-6481).
- Joint NVFP4 global scale across fused siblings (3451-3497) and input-scale unification using min() of reciprocals with the documented over-clip rationale (3391-3448); DefaultProfile fallback at export prints a loud blind-spot warning (6678-6692); runtime coercions are recorded in the manifest with a bf16_upgrade_audit that is explicitly audit-only, not a policy change (1106-1209, 7037-7055).
- /tmp landmine: offload folder defaults to a sibling of --output (6922-6923), not mkdtemp.

Could NOT check (and why): actual vLLM load/serve behavior of the disk-vs-config mismatches (no GPU/serving allowed — reasoned from the compressed-tensors contract and the file's own comments); whether any SHIPPED recipe actually assigns non-BF16 to mtp.* or visual Linears (recipes live in work dirs/HF, not the repo); numerical equivalence of export GPTQ vs production_weight_cache render code (both import shared codecs but full equivalence needs execution); export_batched_gptq.py internals (separate dimension); whether build_production_cache's streaming model instantiates MTP Linears under transformers v5 (if not, the upfront export check would have failed loudly on quantized-MTP recipes — either way finding 2's divergence stands whenever mtp.* is non-BF16).

### streaming-profiles

Scope: streaming + model-profile machinery (layer_streaming.py, streaming_model.py, weight_session.py, source_prefetch.py, autoscale.py, mtp_module.py, model_profiles/*), with git show on ce21000, 94093c4, a729c72, 1f96a14, 55f88cc, cceaee0, 8953514 and cross-checks against the installed production toolchain (prismaquant-cu130 venv, transformers 5.5.4) and real checkpoint configs in ~/.cache/huggingface. READ-ONLY; no pipeline/GPU runs and no test-suite execution.

Verified CORRECT:
- FP8 block dequant (the old MiniMax raw-codes bug): weight_scale_inv is applied inline on BOTH loader paths (_materialize at layer_streaming.py:415-416 and _read_layer_to_device at 599-600); FP8 tensors are kept compressed until on-device (591-593 skip the dtype cast for scaled tensors); the batched per-shape 5-D broadcast math (342-365) and the unaligned fallback (257-265) are algebraically right; dequant runs BEFORE pack_experts (599-606), so the per-expert→packed bridge sees true weights. DSv4's .scale siblings route via profile.fp8_scale_pairs override.
- Per-expert→packed loader (55f88cc): verified against installed transformers Lfm2MoeExperts — `gate, up = linear(x, gate_up_proj[e]).chunk(2, dim=-1)` means gate occupies the first output-row half; _pack_per_expert_into_packed cats spec order [w1, w3] along dim=0 then stacks experts, matching exactly; assembled shape is hard-checked against the live param (fails loud); missing experts/projections raise. Gemma4TextExperts uses the same chunk(2) convention. Export uses the SAME _build_expert_packer bridge (export_native_compressed.py:4654, 4911) — one loading mechanism, no out-of-band pre-pack (house rule 4 holds here).
- mtp_module._load_into_mtp gate/up offsets (start=0 gate, start=rows up) match the chunk(2) convention; down_proj per-expert copy indexed correctly.
- LayerCache: eviction while a layer is in use is safe — _fast_install rebinds tensors by reference and install() retains the cache entry (value-aware retention comment, streaming_model.py:476-486); eviction only drops the cache's reference while installed params keep storage alive; pressure shrink two-phase (priority preference, not contract) with post-empty_cache re-check; mark_done refusal counters; prepare_for_load pre-evicts before reads on both prefetch and cold paths.
- source_prefetch fail-fast gate: mode=require raises on no shards, missing files, budget shortfall, and incomplete reads (source_prefetch.py:103, 110, 137, 177); run-pipeline.sh:197 defaults VALIDATED_SOURCE_PREFETCH=require; wired into validate_assignments_kl.py:772, kl_sensitivity_probe.py:2579, and production_weight_cache.py:360 (assignment-file pages + hard error on missing cache entries under require). Probe/cost/export streaming deliberately use the LayerCache prefetch instead — consistent with the design.
- detect_profile DefaultProfile fallback is now gated where it matters: allocator hard-errors on a multi-format menu under a resolved DefaultProfile with --allow-default-profile escape (24b0f25, verified in allocator.py diff), export warns; registry order puts dense/MoE subset profiles first. Note: DefaultProfile's hand-coded fused fallback (default.py:62-66) covers in_proj_qkvz but not in_proj_ba — acceptable only because of the allocator gate. registry._resolve swallows matches()/register_vendored_modeling() exceptions (registry.py:118-125) — bounded by the same gate, flagged only here.
- Multi-layer-type rope (ce21000/94093c4): per-type dict construction is correct; Gemma3Attention and Gemma4TextAttention in the installed transformers both set self.layer_type, so _call_layer's self_attn.layer_type selection works for the supported archs; the DSv4-style no-layer_type-kwarg fallback computes the same rope per type (harmless); Gemma4Profile.init_rotaries re-runs the rotary's own __init__ on the real device (correct per-type init functions) and returns False for single-rope; 8953514 removed the exporter's stale duplicate _init_rotary_inplace with a guard test.
- fla lru-cache repriming (1f96a14): caches are cache_clear()+primed with CUDA visible BEFORE masking (streaming_model.py:134-145), escape hatch env honored, no-op when packages absent; mask is skipped entirely if CUDA already initialized.
- autoscale (cceaee0): _act_width = max(hidden, intermediate, moe_intermediate, ffn_dim, n_inner, shared_expert_intermediate) read from text_config-or-top-level; threaded into both pick_layers_per_shard and pick_cache_headroom_gb. Constants (ACT_MULT=12, etc.) are operational memory sizing with explicit env overrides, not allocation-quality heuristics — judged not a house-rule-2 violation.
- vllm_registry: fused-sibling matcher regex is leaf-boundary-anchored (`(.*\\.)?(?:alt)$` — no cross-leaf false matches like gate_proj~proj, verified by regex analysis); longest-sibling-first within group; prefix remapper is longest-prefix-wins.
- _classify_shard regexes correctly match the escaped shard-regex strings produced by _build_layer_shard_regexes.
- LFM2.5 conv.in_proj/out_proj pins are justified by vLLM reality (ShortConv built without quant_config → artifact fails to load with emitted scales), i.e. house rule 9, not a heuristic ban.
- WeightSession strict-production-cache default ON makes cache misses hard errors (house rule 4 on the polish path); RTN fallback only behind the explicit env.

Could NOT check (and why):
- Actual GPU behavior of any path (read-only constraint): whether Gemma4 attention .to(query.device) on captured CPU KV avoids a CPU matmul in phase-3 (it does move them, per the modeling source, but unexercised); real prefetch-thread contention; CUDA-graph interactions.
- transformers 5.8 (the version cited in issue #6 reports) — only 5.5.4 is installed in the production venv; if 5.8 re-keys shared_kv_states by layer_type, finding 1's crash mode changes, but the production toolchain is 5.5.4.
- Whether the shipped Gemma4-31B-IT-6bit probe session actually ran at SEQLEN=1024 (did not exhume run logs); the boundary-exactness argument (sliding_window=1024 == default SEQLEN) covers the default path.
- vLLM-side load behavior for the LFM2.5/Gemma4 exports beyond reading the installed modeling sources (no vLLM container runs).
- block_output_match / non-default levers' layer forwards (not in the default PRODUCTION_CACHE_LEVERS; perturbed_x_cache uses whole-model forwards via hooks so HF's own forward threads shared state correctly there).

### validation-selection

VERIFIED CORRECT: (1) KL math is full-vocab and fp32 — build_rtn_cache.py:361 `F.log_softmax(logits.float(), dim=-1)  # fp32!` for the teacher and :366 for the student; kl_divergence sums over the entire vocab dim (:368). (2) Spec-decode guard present and structural: validate_quantized_model.py:170-188 detects `vllm:spec_decode` in /metrics at config time and check_perplexity:290-301 hard-fails with a diagnostic rather than returning draft-model NLL; the two-serve workflow is documented in the module docstring. (3) select_validated_frontier kneedle math is correct for a convex-decreasing frontier: points sorted by increasing bpp (measured_frontier:192), normalization and `min(y_norm - (1 - x_norm))` (:103-106) picks the point furthest below the (0,1)-(1,0) diagonal; <3-point frontiers fall back to best-KL-then-lowest-bpp (:90-94, :110-111); degenerate xmax==xmin/ymax==ymin guarded (:101). (4) Dominance comparator correct: rows sorted (bpp, kl, label), kept only on strict KL improvement beyond the η floor (:196-199); frontier bpps are therefore unique, so _saturation_pick's dict-by-bpp cannot collide. (5) LOO stability diagnostic guarded (≥4 points), advisory-only under the default unstable_policy=keep-kneedle, and only rebinds idx under explicit policies (:411-432). (6) Saturation mode: pipeline auto-bumps VALIDATED_FRONTIER_CALIB_REPEATS to 4 (run-pipeline.sh:187-196) matching the stderr requirement; the no_noise_floor degenerate is surfaced loudly (:584-591); the metric=ucb double-counting warning exists (:494-501). (7) _kl_repeat_summary uses correct sample stddev/stderr/UCB (validate_assignments_kl.py:438-458). (8) One-cache rule holds in the measurement path: both the hooks path (measure_assignment_kl with production_weight_cache, kl_measurement.py:5370-5443 with strict-coverage RuntimeErrors on missing/skipped) and the in-place path (validate_assignments_kl.py:271-377, copies ProductionWeightCache tensors one at a time, hard-fails on missing_model/missing_cache/shape_mismatch:356-362) consume the same rendered cache the exporter ships; activation quant flows through PerturbedActivationCache only. (9) bpp in validation rows is computed over probe-stats names minus profile-pinned and passthrough-prefix names with quantizable_params reported (house rule 7 mechanics present, modulo the dead-code finding). (10) GPU-first enforced: require_cuda_hot_path (:742), device_map offload rejected (:744-747), source prefetch defaults to 'require' (:656-660) as does production-cache prefetch (:697-700). (11) BOS fix itself is tokenizer-mechanism-correct (explicit bos_token + add_special_tokens=False to avoid double-BOS, not a hardcoded model-name list), and changes nothing for non-BOS callers (default add_special_tokens=True preserves old behavior), so older recorded numbers stay comparable — the problem is solely that no entry point passes it. (12) measured_frontier surrogate_loss fallback to mse.predicted_dloss_sum matches what validate_assignments_kl actually emits (_assignment_cost_summary:242). (13) validation_harness WikiText PPL correctly uses split=test with a strided teacher-forced NLL (:351-420). COULD NOT CHECK (read-only, no GPU): actual served-artifact vLLM KL tooling (kl_tool.py lives in /home/rob/dq-runs, outside the repo); end-to-end behavior of CUDA-graph replay paths in kl_measurement.py (only the ~600 lines around measure_assignment_kl, kl_divergence, ref-logprob caching, and coverage gates were audited out of 5519 — the coord-descent lane-batching internals were skimmed via grep only); whether the shipped 27B's historical selection run used out-of-repo held-out text (the repo pipeline as written could not have). The /tmp-default work_root (validate_assignments_kl.py:751 mkdtemp without dir when --work-dir omitted; run-pipeline.sh does not pass --work-dir to the frontier validator) was confirmed but not promoted to a finding given the 8-finding cap — it holds only transient hook scratch, though it does brush the no-/tmp landmine.

### pipeline-contract

VERIFIED CORRECT — defaults (every CLAUDE.md-claimed default matches run-pipeline.sh): TARGET_BITS:=4.75 (L46), FORMATS:=NVFP4,FP8_DYNAMIC,BF16 (L45), NSAMPLES:=32/SEQLEN:=1024 (L52-53), PRODUCTION_CACHE_LEVERS:=gptq,static_act_order,joint_scale_opt (L117), COST_MODE:=production-render-score (L126), SELECTION_MODE:=surrogate (L181), TARGET_PROFILE:=vllm_packed_moe (L71), PRODUCTION_CACHE:=1/PRODUCTION_RECACHE:=1 (L105-106), VALIDATED_SOURCE_PREFETCH:=require (L197). Archived modes all fail fast with exit 2: grouped-kl (L213-216), fisher (L161-178, plus PRODUCTION_CACHE_LEVERS fisher_gptq guard L173-178), hdq (L240-252), multi-shot (L253-259); SELECTION_MODE whitelist (L263-269); EXPORT_GPTQ/EXPORT_SCALE_SWEEP value validation (L974-991). GPU-or-bust: shell check on DEVICE/EXPORT_DEVICE cuda* (L73-76) plus a torch.cuda.is_available() python probe (L77-84). gpu_guard.require_cuda_hot_path call sites confirmed in build_production_cache.py:251, production_recache.py:456, validate_assignments_kl.py:742, export_native_compressed.py:6907-6910, kl_sensitivity_probe.py:2555; incremental_probe/incremental_measure_quant_cost lack the in-module guard but receive --device "$DEVICE" from the already-guarded script, so the orchestration contract holds (direct CLI invocation could still run them on CPU — noted, not filed). Probe modality reuse-guard works and fails loud (exit 2) on unreadable/missing-modality pickles (L350-388). Export hard-fails on production-cache key misses AND missing backing files (export_native_compressed.py:6737-6747) — so stale assignment-scoped caches fail loud (though late and with a misleading message; folded into finding 2). Validated-mode recached cache IS digest-keyed (L845-852). validated-surrogate + PRODUCTION_CACHE=0 correctly fails fast (L671-674). VALIDATED_FRONTIER_CALIB_REPEATS auto-bumps to 4 for PICK=saturation with documented rationale (L186-196). PRISMAQUANT_NVFP4_SCALE_RULE banner default 'static_6' (L292) matches code defaults (incremental_measure_quant_cost.py:287-290, export_native_compressed.py:107-115/176). CACHE_HEADROOM_GB works via env passthrough when user-set (streaming_model.py:857; script default 'auto' is shell-local but equivalent to unset). validate_assignments_kl tempdirs go under work_root, not /tmp (validate_assignments_kl.py:487). set -euo pipefail (L41) + `cmd | tee log` correctly propagates stage failures. Half-written artifacts: no .tmp+rename anywhere (pickle.dump direct to final path in incremental_probe.py:889, incremental_measure_quant_cost.py multiple, build_production_cache.py:459), but a truncated pickle raises on load in every downstream consumer, so kills produce loud (if confusing) failures rather than silent acceptance — kept as context in finding 2 rather than a separate finding. pipeline.py contract machinery itself (gate math, ordering validation, owner whitelist, component composition) is internally sound and well-tested by tests/test_pipeline_contracts.py; tests/test_run_pipeline_defaults.py assertions all hold against the current script (they are string-presence checks only, so they would not catch the stale-reuse or in-sample issues). NOT CHECKED (out of dimension or read-only): actual behavior of validate_assignments_kl KL math beyond scope/sampling, allocator/export internals beyond cache-miss handling, GPU execution of any stage, and whether historical shipped runs manually overrode DATASET/KL scope (findings 1 and 3 are about the default wiring as committed).

### format-registry

READ FULLY: format_registry.py (771 lines), mx_formats.py, fp8_dynamic.py, serving_profiles.py, all 3 serving_profile_specs/*.json, tests/test_jso_scale_levels.py, tests/test_format_registry.py; targeted reads of export_native_compressed.py (scale-rule constants/aliases 107-196, codecs 199-455, _round_to_codebook 516-534, NVFP4 RTN 1935-2020, MX codecs 2124-2300, repack 1235-1400, main flag resolution 6760-6818), production_weight_cache.py (lever resolution 2053-2135, progressive NVFP4 render 1299-1625, non-NV render 1747-2007, packed-expert fill 2864-3320), export_batched_gptq.py (scale-rule call sites), production_render_cost.py, aura_cost.py:100-190, perturbed_x_cache.py fallback/activation paths, incremental_measure_quant_cost.py:290-400, registry parity tests in test_prismaquant_export_native_compressed.py:520-640.

VERIFIED CORRECT (no finding):
- Scale-rule routing is NOT conflated on the main path: fill_production_weight_cache sets levers['nvfp4_scale_rule']=joint_mse when joint_scale_opt is on and env unset (production_weight_cache.py:2096-2102), else defaults to resolve_nvfp4_scale_rule() (env, static_6); the progressive NVFP4 render uses JOINT_MSE for GPTQ candidates (:1403-1407) under _temporary_nvfp4_scale_rule; export main mirrors the same flip (export:6786-6791). static_6 governs only the pure-RTN baseline candidate and non-JSO renders, exactly as documented. four_over_six_mse is a distinct opt-in rule with its own gated candidate — the three rules are correctly separated in code.
- JSO grid: _NVFP4_JOINT_SCALE_LEVELS defaults to (6.0, 4.0) with env extension and garbage-fallback (export:258-279); GPTQ-time joint search evaluates candidates under FP8-snapped effective scales with optional act-weighting (export:411-455); tests/test_jso_scale_levels.py covers default, env grid, and joint==four_over_six equivalence. Matches commit cdb3022's claim.
- RTN A/B escape hatch (df12b76): PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN default '0', loudly commented 'NEVER a production path' (export:1394-1400), only bypasses the packed-expert hard-fail — house-rule-compliant debug gating.
- FP8_DYNAMIC emulation is faithful to vLLM: per-token dynamic scale with min_scale = 1/(448*512) matching vLLM's min_scaling_factor (fp8_dynamic.py:96-99); weight QDQ delegates to compressed-tensors FP8_DYNAMIC qparams; tests assert bit-exact equality vs compressed-tensors fake_quantize AND vs the export codec (test_format_registry.py:35-150). The '4B resident-emulation scare' does NOT apply to FP8 — only to NVFP4 (finding 1).
- E8M0 snap consistency: MXFP4 and MXFP8_E4M3 both route through compressed_tensors.generate_mx_scales (registry via _snap_scale_e8m0 num_bits∈{4,8} / mxfp8_e4m3_qdq; export via _mx_base_exponent_from_amax), so cost emulation and export packing share one rounding rule; tests assert byte-identical scales and exact registry==export dequant for MXFP8_E4M3. The legacy _mx_rounded_amax_power2 fallback is reached only for research E5M2/FP6.
- serving_profile_specs/vllm_packed_moe.json matches the de-menued-MXFP8 story exactly: MXFP8_E4M3 allowed for experts and not denied for dense; only MXFP4/MXFP8_E5M2/FP8_E5M2 denied for dense; FORMATS default NVFP4,FP8_DYNAMIC,BF16 (run-pipeline.sh:45) excludes MXFP8 by menu, not by ban. Expert regex correctly excludes shared_experts (word-boundary via (^|[.])). Profile merge (extends), alias resolution (_format_in via canonical_format_name/aliases_for), and shape rules (NVFP4 in%16, MXFP8 K>=128/N>=128/K%32 — kernel constraints, allowed constants) all check out.
- Passthrough-only invariant at format level: BF16/FP8_SOURCE quantize_dequantize are clone/identity; FP8_SOURCE effective_bits ≈8.002 via scale_block_shape (128,128); scale_count_for_shape/memory_bytes_for_shape arithmetic verified for grouped/per-channel/block cases. effective_bits' +0.02 fallback is only used for menu ordering/dedupe (allocator.py:1199,1242), not budget accounting.
- torch.compile hot path: group_size==0 forced eager with a documented Dynamo-specialization bug rationale (format_registry.py:458-465, tested in test_plain_fp8_rtn_uses_eager_path); dynamic=True + recompile_limit raise for grouped path; codebook device cache keyed on id() of persistent module-level tensors (safe); bucketize nearest-pick logic correct for non-ties, ties break to lower-magnitude consistently within the registry (export _round_to_codebook ties toward zero on abs codebook — opposite direction for negative exact midpoints only, measure-zero; noted inside finding 1).
- Default cost path coherence: COST_MODE=production-render-score derives allocator cost from the cache's render-score records (production_render_cost.py reads cache.metadata['render_scores']), i.e. the same rendered weights export re-packs — weight-side cost/render coherent for 2-D Linears (modulo findings 1-3). incremental_measure_quant_cost (L1) now renders non-BF16 weights through render_production_weight, not bare registry RTN.

COULD NOT CHECK (out of scope / needs GPU or other reviewers' dimension): actual vLLM CUTLASS NVFP4 kernel parity beyond emulation source review; whether packed-expert ALLOCATION costs come from registry-RTN measurements vs render scores (cost-pipeline dimension — fill_packed_expert_cache_entries writes no render_scores records, worth the cost reviewer's attention); whether the resident KL evaluator perturbs 3-D packed-expert params at all (perturbed_x_cache appears nn.Linear-only — cache/validation reviewer should confirm, I did not verify the full install path); runtime torch.compile behavior on Blackwell.

### doc-audit

VERIFIED CORRECT (doc==code): (1) run-pipeline.sh defaults match runtime_flags.md and CLAUDE.md for FORMATS/TARGET_BITS=4.75/NSAMPLES=32/SEQLEN=1024/COST_MODE=production-render-score/SELECTION_MODE=surrogate/PRODUCTION_CACHE_LEVERS=gptq,static_act_order,joint_scale_opt/PRODUCTION_CACHE_LRU_GB=64.0/PRODUCTION_CACHE_PREFETCH=require/VALIDATED_SOURCE_PREFETCH=require/VALIDATED_FRONTIER_PICK=kneedle (run-pipeline.sh:45-199). (2) grouped-KL and Fisher archive fail-fasts exist exactly as documented (run-pipeline.sh:175,214; archive/grouped_kl_2026-05-28 + archive/fisher_2026-05-15 exist). (3) The 3 doc-only *_MIN_CALLS flags are real — constructed at kl_measurement.py:165 f"{name}_MIN_CALLS". (4) PRISMAQUANT_NVFP4_SCALE_RULE default static_6 matches code ("" → static_6 alias, export_native_compressed.py:111-116). (5) docs/progressive_render_pipeline.md matches render_score.py: RenderMechanismSpec fields (name/operation/scope/phase/gate_metric/after/before/exclusive_group, :189-197), score_render_error/gate_render_candidate (:64,:101), registry + topo-order resolution; PRISMAQUANT_RENDER_GATE_MIN_GAIN default 0.0 confirmed in production_weight_cache.py. (6) Design doc §3.4 L3 implementation pointers all exist at HEAD (kl_measurement.py: select_formats_for_l3:528, select_l3_neighborhood:651, build_l3_candidates:854, solve_frozen_l3_neighborhood:1002); §3.3 grouped-KL ARCHIVED banner present. (7) audit_findings.md/audit_questions.md (both 2026-05-22, last touched cb2c076 2026-05-23): a quantization-correctness audit whose 5 findings are recorded as already-fixed; all cited tests exist at HEAD (test_registry_served_metadata_reconciliation...:528, test_registry_render_dequant...:549, test_mx_e8m0_scale_encoding...:725 in tests/test_prismaquant_export_native_compressed.py; test_mxfp8_activation_quantizer...:183 in test_format_registry.py; test_cost_surrogate_calibration_anchors.py exists) — line numbers have drifted ~100 lines and Finding 4's doc reference moved to archive/grouped_kl_2026-05-28/docs/ (the fixtures file already points at the archive path), but substance is current; audit_questions' explicit registry gaps (MXFP6_E3M2/E2M3, INT8_W8A16, INT4_W4A16_g128) still match the test's explicit_gaps dict (:541-547) — these remain open product questions, not stale. (8) Codex 2026-06-09 decision + blocker-fix docs vs HEAD: all claimed fixes landed — B1 held-out do-no-harm with rtn_fallbacks/heldout_reverts counters (production_weight_cache.py:3175-3305), B2 packed_expert_max_abs.json sidecar + _packed_expert_input_global_scale with hard-fail on missing scale (export_native_compressed.py:1364, 5458-5465), B3 hard-fail on BOTH export paths (:5426 streaming, :5678 in-memory), B4 re-pack via _read_cached_packed_expert (:1335); render_mode default "batched" (:2878); module_token_budget=32768 at HEAD (:2872) — the blocker doc's 65536 was deliberately superseded by df12b76 and the 2026-06-09 handover documents the change, so not a divergence; PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN escape hatch is loud-warn + explicitly "NOT a production path" (clearly gated, no house-rule-1 violation). (9) 2026-06-05 handovers' open items vs 2026-06-09: the "UNCOMMITTED" bit-rate-selection work (footprint.py, saturation_select.py, --target-disk-gb, --mode saturation, VALIDATED_FRONTIER_SAT_Z) all landed in 5db5a9e and VALIDATED_FRONTIER_SAT_Z=2.0 is wired (run-pipeline.sh:186-192); paper/main.tex is the canonical AURA paper with PrismaSCOUT preserved under paper/archive/ exactly as claimed; FP8-knee follow-up was pursued (served 27B FP8 measurements + ToolEvalBench per memory + the 2026-06-09 handover); tail-veto correctly described as "raised, not built" (no p99/tail logic in select_validated_frontier.py). Handovers self-label as superseded, so none filed as findings. (10) AGENTS.md and docs/design_guidelines.md are normative and consistent with code spot-checks (one-cache APPROVED_RESOURCE_OWNERS exists at pipeline.py:19; archive walls exist; bpp-quantizable-only rule restated). NOT CHECKED / OUT OF SCOPE: paper/main.tex numeric claims vs run artifacts (needs the dq-runs data, partly outside repo); the full 401KB codex-response-moe-gptq-render transcript (sampled via the brief + blocker docs + handover instead); HF model cards (outside repo; CLAUDE.md already flags the PrismaSCOUT HF README as historical); anything requiring GPU/pipeline execution (read-only constraint); the ~25 docs/*results*.md measurement logs (dated lab notebooks, not normative). Note: top-level README headline tables (27B PrismaSCOUT 5.31/0.0151, 35B 8-of-9) were left alone — they match CLAUDE.md's shipped-artifact record and are not code-checkable; the bpp-era caveat (public 5.31 ≈ 4.76 under current accounting) is a known framing issue already recorded in CLAUDE.md §4.12 rather than a new divergence.

### paper-audit

SCOPE: paper/main.tex (800 lines, read in full), paper/figures/ (all 14 files listed; fig_aura_rd_geometry.tex read in full), paper/archive/, paper/main.log/aux, plus cross-checks against the raw measurement artifacts in /home/rob/dq-runs/{aura-27b-flagship,aura-blocker-runs,aura-sat-validate-4b,xlayer_rung0} and prismaquant/aura_cost.py. Read-only; reproduction limited to re-running kl_ab.py compare on existing JSON dumps (pure JSON math) and line-overlap counting on corpora.

VERIFIED CORRECT:
- Attribution: author block is robert.tand@icloud.com (main.tex:68); grep for 'tenari' over all of paper/ returns nothing.
- Spine consistency post-5db5a9e: title, abstract, intro, contributions, and conclusion are uniformly AURA-spined; PrismaSCOUT appears only as the explicitly retired prior system (main.tex:114-116, 138-148, 351-369) with the old paper preserved at paper/archive/prismascout_paper_2026-06-05.tex. No section still builds on the L1/L2/L3 cascade as the contribution; the cascade appears only as the thing being retired, with the head-to-head (Table 2) as supporting evidence. No mixed-spine text found.
- Retracted/forbidden numbers: grep for '3.52', '0.0056', '0.0054', '5.39', '17 promotions', '4× lower' → zero hits in main.tex. The grouped-KL win, the polish headline, and the provisional 5.39/0.0054 number are all absent.
- Known-good numbers reproduced from raw dumps (kl_ab.py compare, exact): 27B small corpus 0.026464 vs 0.031931 → −17.1% (Table 3 row 1 ✓); 27B big corpus confident 0.055943 vs 0.068152 → −17.9% (Table 3 row 2 ✓); abstract's '17.9% replicated on two disjoint held-out corpora' is defensible (small corpus = synthetic encyclopedic text, verified disjoint from WikiText). 4B test-split −39.4% (0.245 vs 0.405) matches the cascade table (L1 0.405, AURA 0.245) and the L2 deltas check arithmetically: (0.405−0.399)/0.405=1.5%, (0.399−0.245)/0.399=38.6%≈38.5%, AURA-vs-L1 39.5%≈'39.4%'. 27B re-serve −33.5% (0.034375 reproduced) and all-position −22.1% match the run data. Table 1 precision-sweep values reproduced verbatim from xlayer_rung0/logs_fp_additivity.log (bf16 Σ/J=1.884, mean −5.27e-4 all-negative; fp32/fp64 0.919, −1.22e-5, fp64≡fp32 ✓); 27B joint 3.86e-3 reproduced in aura-27b-flagship/logs/additivity_27b.log ('joint: KL=0.003862855'); the 2.21× overshoot and ε≈4.2e-4≈overshoot/11 arithmetic checks. Proposition 1 (floor pseudo-coupling) and Proposition 2 (unbiasedness) algebra verified by hand. PPL nuance numbers (9.243/9.460/9.496, +2.35%/+2.74%, −0.38% relative, 0.39pp) all internally consistent and match auto-memory. W4A16/W4A4 attribution percentages check (0.0252/0.0559=45%, 0.0455/0.0559=81%). 4B Table tab:4b values (0.2268/0.2204/0.3645, PPL 3.582/3.965/4.834, −18%) match auto-memory and xlayer_rung0 artifacts exist; not re-derived from raw 4B dumps. Allocation counts 341/155 vs 314/182 match memory.
- Novelty scoping: claims explicitly scoped to ≥4 bits in abstract, intro, and Remark 7 (rem:scope); CoopQ correctly used as the <4-bit foil ('largest gains exactly there (2.0–2.5 bits)', main.tex:343-344); the AURA estimator is framed as 'a clean synthesis of known machinery' / 'not new sensitivity theory' (main.tex:180-184, 689-695) with Orr arXiv:2505.12988, FIMA-Q arXiv:2506.11543, KL-Lens arXiv:2604.13440 cited — all matching the lit-check memory; CoopQ correctly attributed to arXiv:2509.15455.
- Code claims in the paper verified against code: aura_cost.py _ZERO_COST_FORMATS={'BF16','FP8_SOURCE'} (line 64) matches 'BF16/FP8_SOURCE hardwired to zero (passthrough)'; the bf16-stored ΔW limitation (#2) matches code comments ('one bf16 delta per nonzero format'); the limitation honestly pre-empts what would otherwise be a finding.
- LaTeX hygiene: main.log shows no undefined/multiply-defined references or citations (only an OT1 font-shape warning); every \\ref target (sec:*, eq:mckp, eq:aura, prop:*, tab:*, fig:aura_rd_geometry) has a matching \\label; figure panels (a)/(b) referenced consistently.
- bpp accounting eras: the paper no longer quotes the public 5.31/0.0151/20.17GB flagship numbers or the RedHatAI comparison at all (flagship referenced only qualitatively at main.tex:360-361), so no era mixing is possible.
- Repro pointers in captions exist on disk: xlayer_rung0/fp64_additivity.py, xlayer_rung0/, aura-blocker-runs/, aura-27b-flagship/.
- Self-declared draft gaps (not findings): %TODO markers on fimaq2025/kllens2026 author lists (main.tex:772-777); 'DRAFT v0.1' banner and pending-CI caveats are explicit in the text.

COULD NOT FULLY CHECK: (a) the Gini 0.45/0.65, ρmax/ρmedian≈63, and 33%/24.5% chord-curvature computations — no computing script located within budget; values match the aura_rd_convex_geometry auto-memory note but were not re-derived from cost pickles; (b) bf16-resident-vs-fp32 cost fidelity (Spearman 0.9967, median ratio 0.990) — matches memory, not re-derived; (c) whether figure (b)'s 4.50/4.85/5.00/5.25/5.50/7.00 points come from the same resident vak/w4a4 family as the four verified points (frontier_saturation.json) — the verified four are decisive for the mislabeling finding regardless; (d) the 4B 'two independent calibrations' tight-error-bar claim — single-source (memory) but consistent across notes. No GPU/pipeline/serve runs were performed per the read-only constraint.

### hygiene-landmines

CHECKED AND FOUND CORRECT: (1) /tmp sweep — kl_measurement.py's six context-cache mkdtemps all take dir=tmp_parent (work_root) and are only /tmp-exposed via validate_assignments_kl's default (finding 1); perturbed_x_cache.py:1127 mkdtemp correctly uses dir=str(root); kl_measurement.py:1724 tempfile.gettempdir() write is a CUDA-graph .dot debug dump gated behind _verbose_enabled() (called from line 1985 inside the verbose block at 1979) — debug-only, tiny, acceptable; __init__.py:35 NamedTemporaryFile is a self-deleting writability probe into the Triton cache dir with an XDG-cache fallback, never /tmp-resident state; validate_assignments_kl rmtree-cleans its work_root on exit (lines 1031-1032) — only its location is wrong. run-pipeline.sh and all examples/launchers/*.sh never set TMPDIR, confirming mkdtemp defaults really land in /tmp. (2) NVINT removal — prismaquant/kernels/ ships only nvfp4_fused.py; the three test references (tests/test_layer_config.py:53, test_prismaquant_native_math.py:46, test_prismaquant_export_native_compressed.py:376-382) all assert rejection via pytest.raises/assertRaises ('test_low_bit_custom_kernel_formats_are_not_registered'), so no collection errors and the ban is pinned by tests. (3) PrismaClip/PrismaFisherClip dead helpers (memory note of 2026-05-15) — no longer present: production_weight_cache.py 'clip' hits are all live act-clip plumbing; kl_sensitivity_probe.py has zero clip helpers; the auto-memory note is stale in the good direction. (4) CPU-activation landmine — export_batched_gptq.py:88-96 documents the _LazyActivationCache CPU-residency trap and explicitly moves activations to device before the H=X^T X matmul (the v23 fix is in place). (5) No bare 'except:' anywhere in prismaquant/*.py. (6) Gate-bypass env flags — PRISMAQUANT_ALLOW_PACKED_EXPERT_RTN (df12b76 escape hatch) is correctly loud: without it export hard-fails with a RuntimeError banning RTN packed experts (export_native_compressed.py:5432-5443), with it a WARNING prints per expert (5444-5450) naming it research/A-B only; PRISMAQUANT_VALIDATION_SKIP_END_KL records NaN in metrics rather than fabricating a pass (validation_harness.py:122-128, 236-238); PRISMAQUANT_ALLOW_PYTORCH_FALLBACK is a documented debug-only bypass of the fast-kernel startup gate (_fast_kernel_guard.py:39-43) — silent when set but explicit opt-in; PRISMAQUANT_DISABLE_RTN_COMPILE is perf-only. (7) .gitignore correctly covers __pycache__, *.pyc, *.log (texput.log), *.pkl, tmp/, data/, scratch/, references/, .claude/, *.egg-info/, .venv — none of the heavy local dirs (tmp/ 992K, data/benchmarks.sqlite 684K, scratch/ 1.2M) are tracked except the one pre-existing scratch file in finding 6. (8) The recache/validate PerturbedActivationCache instances use input_rows=0, and finalize() (perturbed_x_cache.py:966-976) only torch.saves rows when snaps are non-empty, so the /tmp dirs hold manifests rather than GB-scale shards — reflected in severity choices. (9) The DO_NO_HARM revert logic itself is objective-derived (activation-weighted MSE comparison, picks the min), not a heuristic ban, and the same _quantize_2d is called by production_weight_cache.py:1992-1995, so cache/export byte identity is preserved when the env doesn't change between stages — only its silent exception swallow is the finding. COULD NOT CHECK (read-only audit, no GPU): actual runtime behavior of the staged loads, whether transformers holds the staged /tmp config open across the whole load, and live /tmp contents under real memory pressure; also did not audit dimensions owned by other reviewers (allocator math, export packing numerics, bpp accounting, held-out split disjointness) beyond what the /tmp and except sweeps touched.

### test-quality

Scope: all 17 new/changed test files on claude/fix-issues-4-6 vs main (git diff main...HEAD --stat -- tests/), each read alongside the production code it claims to cover; all 102 of those tests executed and passing under the cu130 venv (CPU-only, read-only review respected).

Verified CORRECT (no finding): (1) No test mocks its unit under test anywhere in the new set — test_allocator_kneedle, test_bitrate_select, test_footprint, test_bit_attribution exercise the real allocator/saturation_select/footprint functions on synthetic curves, and test_allocator_kneedle's golden-section test even cross-checks convergence against a 2001-point brute force; test_select_validated_frontier additionally runs the real CLI via subprocess and pins the layer_config schema. (2) test_incomplete_fused_ignore uses the REAL Gemma4Profile and the REAL build_quantization_config and would fail if the absent-v_proj/fused-qkv ignore emission were removed (export_native_compressed.py:6375-6380 verified). (3) test_measure_quant_cost_packed_experts genuinely pins the mean-field-vs-×N dloss fix with an independently computed oracle AND an explicit anti-regression bound (got < 0.5×product_of_sums), exercises the real _measure_packed_experts/_finalize_results, and its seemingly weak `.get(\"output_mse_measured\", True)` assertion is actually consistent with production semantics — measure_quant_cost.py:127-128 only writes the key when False, and allocator_candidates._has_measured_output_mse (lines 204-219) fails CLOSED for packed-expert placeholder zeros, which the test also exercises through the real gate function. (4) test_per_expert_packing pins exact tensor layout (gate_up[i]==cat(w1,w3)), bystander preservation, and both loud-fail paths (shape mismatch, missing expert) of the real layer_streaming._pack_per_expert_into_packed with the real Lfm2MoeProfile. (5) test_meta_init_fla_priming is well-constructed: it proves prime-before-mask ordering via identity comparison against the original torch.cuda.is_available and verifies cache_clear, and the restore test pins mask cleanup. (6) test_jso_scale_levels reloads the real export module per env value, pins the {6,4} default, garbage-fallback, and the superset-MSE property (assertion holds by construction, so the unseeded randn cannot flake it). (7) test_gemma4_rotary_init exercises the real Gemma4Profile.init_rotaries (the fake is only the rotary module, mimicking the transformers class shape — appropriate since the newer transformers isn't installed) and structurally guards the exporter-duplicate regression. (8) test_multilayer_rope_forward and test_autoscale_act_width call real layer_streaming/autoscale functions and pin both the regression and the non-regression (single-rope tuple passthrough; wide-vs-narrow layers_per_shard ordering). (9) test_moe_expert_projection_names includes the crucial negative control (alternate-named experts NOT detected under the w1/w2/w3 profile), proving the profile names gate detection. (10) The only fixture change (tests/fixtures/cost_surrogate_calibration_anchors.json) is source_doc path updates to the grouped-KL archive — no stale config-format assumptions. (11) test_run_pipeline_defaults parses the real run-pipeline.sh, so the archive fail-fast assertions (grouped-kl, multi-shot, HDQ) are genuine.

Could NOT check (out of scope / read-only): numerical correctness of fill_packed_expert_cache_entries itself (no oracle exists — that absence is finding #2); whether any gate actually fires correctly on a real model/vLLM load (would require GPU/serving runs, prohibited); behavior of the export-stream RTN gate's three branches at runtime (only code-read, finding #1). The deleted tests/test_grouped_kl_cost.py matches the archived grouped_kl_cost.py module removal — consistent, not a finding.
