# Architecture re-vet — 2026-07-30

> **Frozen audit snapshot.** This document records the repository as it existed
> on 2026-07-30. On 2026-07-31/08-01 the gfx1151 work was canceled and removed,
> and the two-tree Gridbook design was retired: Gridbook now has one external
> canonical repository consumed through one immutable pin and packaged runtime
> contract. Paths under `plugins/gridbook/` below are historical and do not exist.

**Date** 2026-07-30 · **HEAD** `ef50495` (`claude/docs-consolidation`; docs consolidation complete, tests 1563/0) · **Input** `docs/ARCHITECTURE.md` + six lens reviews in `scratch/doc-consolidation-2026-07-30/revet/`.

**What this is.** The re-vet Robert asked for after the docs consolidation: *"re-vet the architecture and see if there are any fundamental changes we want to make; make sure we're using our plugin architecture right and using configurations for supporting individual models."* Six lenses (cost/allocation, pipeline, plugins, lanes, validation, de-scope) verified every claim in code at `ef50495` and wrote proposals, not patches. **No code and no docs were changed.**

## Executive summary

The architecture is **sound**. Four of six lenses returned an explicit null on their own core: the cost family, the solver, the orchestrator, the metric hierarchy, the three-registry plugin design and the format menu need no fundamental change, and cross-layer modelling stays buried. Every real finding sits in one of three places — **selection** (the ship decision still runs on the surrogate), **provenance** (artifacts do not record what produced them), and **wiring debt of one repeating shape** (a declarative extension point defined, parsed, documented, with no production caller). The de-scope lens found the inverse of over-engineering: ~15k lines (21% of `prismaquant/`) implement two cost-cascade levels and a contract layer the pipeline never runs. The decisions that matter most: **(1)** make the byte budget the constraint and measured KL the objective, retiring kneedle to diagnostic (R1); **(2)** flip `COST_MODE=aura` to default, gated on a cost-provenance stamp that stops silent stale-table reuse (R2); **(3)** decide whether CB is *a* lane or *the* lane by funding the `col_weights` render identity that lifts its cost/selection gates (R3); **(4)** retire the L1/L2/L3 cascade framing from the spine — it has no executor, no consumer and a null result (R4); **(5)** rule on `pipeline.py` — settings-hash authority or linter-or-gone, where two lenses disagree (R5).

## Decisions requested

Ranked by leverage-per-risk, fundamentals first. "Lens" credits the arguing reviewer(s); merged rows credit both.

| id | proposal | class | cost | risk | lens |
|---|---|---|---|---|---|
| **R1** | Byte budget becomes the *constraint*, measured KL the *objective*; kneedle → diagnostic — **IMPLEMENTED 2026-07-30** (both verdicts YES; D12 closed) | FUNDAMENTAL | M | none while opt-in | L1 |
| **R2** | Flip `COST_MODE=aura` to default, gated on a cost-provenance stamp + wiring the additivity gate — **IMPLEMENTED 2026-07-30**; default flipped, both preconditions landed, re-solve 0/614 + 0/500 | FUNDAMENTAL | S+S+S | mismatch rebuilds, loudly | L1 |
| **R3** | Split `COST_MODE` into (render source)×(cost objective); `col_weights` on the CB render closes Milestone C and lifts the CB gates — **IMPLEMENTED 2026-07-30**; native renders pinned bit-identical, AURA-on-CB opt-in only | FUNDAMENTAL | M–L | med — shared render path | L2 + L4 |
| **R4** | Retire L2/L3 from the spine; archive-wall the L3 probe — **IMPLEMENTED 2026-07-30** (measured: 97 symbols / ~4.3k lines out of `kl_measurement.py`, not the ~15 functions estimated) | FUNDAMENTAL | M | near-zero | L6 |
| **R5** | `pipeline.py`: settings-hash authority — **RESOLVED for Lens 2, IMPLEMENTED 2026-07-30** (D6 + D10 closed; 16 guard sites / 15 artifacts) | FUNDAMENTAL | M | low | L2 vs L6 |
| **R6** | Lane eligibility becomes model configuration — **IMPLEMENTED 2026-07-30** (spec side wave 2, `run-pipeline.sh` preflight wave 3) | FUNDAMENTAL | M | low, additive | L4 |
| **R7** | Strix Halo enters **serving-only**, GGUF-first; no ROCm build stack in scope — **ACCEPTED 2026-07-30**, then **PARTLY SUPERSEDED the same day**: Robert funded the CB kernel port, a gfx1151 box arrived, and option B's entry test is passed (HIP kernels authored + parity-verified + benchmarked in `plugins/gridbook/gridbook/csrc_hip/`, still unserved). Quantization-stays-on-the-Spark is unchanged; A still unrun | FUNDAMENTAL | S | none | L4 |
| **R8** | `spec.match`: implement the reader (`SpecMatchProfile`, zero-Python Tier A) **or** delete the dead field | FUNDAMENTAL | M / S | med — detection order | L3 |
| **R9** | D1 tail-veto: emit `kl_p99`/`kl_max`/`nll_p99` from tensors already in hand; veto in `_frontier_from_rows` | STRUCTURAL | M | zero default-off | L1 + L5 |
| **R10** | Gridbook per-arch CB loader list → data + serve-time "every CB expert tensor consumed" assertion (D3) | STRUCTURAL | M | low | L3 + L4 |
| **R11** | `spec.default_serving_profile` authoritative; allocator stamps resolved profile into `layer_config.json` — **IMPLEMENTED 2026-07-30**, D4 closed, non-regression 0/614 + 0/500 | STRUCTURAL | S | low | L3 |
| **R12** | Route MTP through the profile; add `mtp_source_prefix()`; delete `mtp_module.py` (D2) | STRUCTURAL | M | med — probe + export | L3 |
| **R13** | `exported/shipcard.json` — a refusal contract the build lane opens and the serve lane must close — **IMPLEMENTED 2026-07-30** (run-pipeline echo deferred) | STRUCTURAL | M | none | L5 |
| **R14** | Mechanize held-out disjointness by `calib_hash` intersection, hard-error; fix the wikitext `--calib-skip-first` no-op | STRUCTURAL | S | none | L5 |
| **R15** | Serve fingerprint from the **server's** `/proc/<pid>/maps`; `kl_ab.py` refuses cross-fingerprint deltas — **IMPLEMENTED 2026-07-30** | STRUCTURAL | M | none | L5 |
| **R16** | `LaneSpec`: one lane-uniform ship gate across native/CB/GGUF (closes D26's measurement half) — **IMPLEMENTED 2026-07-30**; gates ADVISORY, blocking deferred to Robert | STRUCTURAL | M–L | low, additive | L4 |
| **R17** | `COST_MODE=production-render-staged` → the eighth `exit 2` gate — **IMPLEMENTED 2026-07-30** | STRUCTURAL | S | none | L6 |
| **R18** | Archive-wall `PRODUCTION_CACHE_UNION` + `MSE_PROMOTION` — **IMPLEMENTED 2026-07-30**; the MSE-promotion ledger check came back CLEAR | STRUCTURAL | S | low | L6 |
| **R19** | Wall the orphan modules + tools — **IMPLEMENTED 2026-07-30**; `aura_additivity_gate.py` kept live (R2 wins), `_fast_kernel_guard` walled + booked as §12 **D28** | STRUCTURAL | S | low | L6 vs L1 |
| **R20** | CB ladder law: unify dense/expert chains, derive `_CB_LADDER_TOL` from anchor noise; **no served A/B** | STRUCTURAL | S–M | none (default OFF) | L1 |
| **R21** | Persist the MoE auto-tuner's timings; **defer λ** until two boxes' cost tables disagree in ranking — **IMPLEMENTED 2026-07-30**; λ ruling pinned by a test | STRUCTURAL | S | none | L4 |
| **R22** | Author the `minimax_m2` structure spec + `deepseek_v4` `default_serving_profile` (D13) | STRUCTURAL | M+S | low | L3 |
| **R23** | **Decision only:** do *not* port `run-pipeline.sh` to a `pipeline.py` executor — **ACCEPTED 2026-07-30**, recorded so it is not re-litigated | STRUCTURAL | free | none | L2 |
| **R24** | Hardening pass: D8 export prefetch `require` + D9 `require_cuda_hot_path` on 7 stages — **IMPLEMENTED 2026-07-30** as one branch; D8/D9 closed | HYGIENE | S | D8 can newly fail | L2 |
| **R25** | Archive-wall block-output-match; close D16 as *unreachable*, not *unmeasured* — do not A/B it — **IMPLEMENTED 2026-07-30**, D16 closed as *unreachable* | HYGIENE | S | none on shipping recipe | L5 |
| **R26** | Conformance test: every declared spec field has a reader (the pattern behind 4 of 5 plugin leaks) | HYGIENE | S | none | L3 |
| **R27** | Two accessor moves: `streaming_model.py:98-104` arch predicate → spec bool; probe's `MiniMaxM2Experts` → accessor | HYGIENE | S | ~zero | L3 |
| **R28** | Phantom flags (`PRISMAQUANT_FISHER_CAP_MULTIPLIER` has zero readers), S13–S16 label inversion, `last_token_kl` rename — **D27 (gridbook version skew) resolved 2026-07-30 under R10**, the rest open | HYGIENE | S | none | L6+L5+L1 |

---

## Lens 1 — cost models, allocation, selection

*§2, §4, D1/D12.* **Null where it matters:** the *math* needs no fundamental change. The cost family (additive KL-Fisher × rendered dW), the collapse of L1/L2/L3 to one level, and the solver (serving-atomic DP unit, feasible-only termination, memoized ratchet) are sound and code-backed. **Every real gap is in the selection layer and in cost provenance, not in the estimator.**

### R1 (L1 P1) — Byte budget = constraint, measured KL = objective; kneedle → diagnostic · FUNDAMENTAL

**What.** Two disjoint ship selectors exist. (i) `allocator.py --target-disk-gb` (`:1202`, impl `:2276-2605`) uses exact footprint as the feasibility gate then picks by **predicted** Δloss — objective stamped `min_predicted_dloss__ties_to_larger_footprint` (`:2288`). (ii) `select_validated_frontier.py` picks by **measured** KL but is byte-blind: `grep -c 'bytes\|disk_gb'` → 0, and `measured_rows` (`:170-207`) whitelists only kl/bpp/format_counts/changed_vs_base/mse/surrogate_loss. The selector that owns the ship decision runs on the surrogate; the one that measures cannot see the card — §2.2 inverted exactly where it is load-bearing. Proposal: one stage. Plumb `TARGET_DISK_GB` into `run-pipeline.sh` (closes D12); convert budget → max body-bpp via `footprint.floor_bytes_for_model` (bytes are affine in body bpp at fixed model — exact, not a fit); add a `budget` pick = **min measured KL among rows that fit**; keep kneedle as the diagnostic `_rd_curve_diagnostic` (`allocator.py:285-338`) already says it is. Side effect that pays for the whole thing: the budget bracket collapses the Pareto set from 11 rungs (`run-pipeline.sh:47`) to the ~3 that can fit.

**Why now.** The surrogate-knee failure is on record (§11: 27B surrogate 5.857/0.056 vs validated 5.31/0.015) *and* the RD diagnostic already prints "select ship bpp by byte budget or measured saturation, not curvature" (`allocator.py:329-334`) — code telling the operator to do what the pipeline cannot. Every byte-budget ship to date is a manual CLI invocation (D12).

**Cost** M. **Risk** none while `VALIDATED_FRONTIER_PICK=kneedle` and `TARGET_DISK_GB` unset — both paths opt-in, byte-identical when off; the risk lives entirely in the default flip.

**Verdict-request.** (a) Should `TARGET_DISK_GB`, when set, override `TARGET_BITS` in the pipeline as it does in the CLI? (b) Should `validated-surrogate` default **on only under a byte budget** (~3 KL evals), staying opt-in for open-ended `TARGET_BITS` runs? Lens recommends yes to both. *Corollary:* do **not** promote `validated-surrogate` unconditionally — alone it turns the assignment-scope render (`run-pipeline.sh:177`) into a format-menu render (`:1153-1170`), ~2× render on a 3-format menu, plus 11 KL evals, to decide a bpp a byte budget fixes for free.


**Outcome: ACCEPTED, implemented 2026-07-30 — both verdict-requests answered YES by Robert.**
(a) `TARGET_DISK_GB`, when set, **overrides `TARGET_BITS`** in the pipeline exactly as it does
in the CLI. (b) `validated-surrogate` defaults **on only under a byte budget**, staying opt-in
for open-ended `TARGET_BITS` runs — an explicit `SELECTION_MODE` still wins. Mechanism: the
allocator prices every Pareto candidate with `footprint.assignment_artifact_bytes` (the same
accounting its own byte-budget selector uses) and stamps `artifact_bytes` into each assignment
payload + the manifest; under a card it narrows the Pareto set to the largest fitting rung ±1
(~3 of 11) with a log line — a computed narrowing, skipped loudly if any candidate is unpriced;
`select_validated_frontier --mode budget --target-disk-gb` picks **min measured KL among the
rows that fit**, reading the bytes column from the allocator payload. Unpriced rows and an
infeasible card are hard errors, never a silent fall-back to another pick. Kneedle stays the
default without a card and stays the diagnostic `_rd_curve_diagnostic` already calls it.
Byte-identical when `TARGET_DISK_GB` is unset (pinned by a test). Closes **D12**.

### R2 (L1 P2) — Flip `COST_MODE=aura`, gated on two S-sized preconditions · FUNDAMENTAL

**What.** AURA's evidence clears the §2.4 default-on bar: −38%/−39.5% served confident-KL at the 4B knee across two independent calibrations with PPL agreeing (−18% vs `output_mse`), −17.9% at 27B, both flagships (regen-27B, 35B arm-E) produced with it, MoE hole closed by the M4 hybrid, automated at `[2d]` (`run-pipeline.sh:905-956`). What is *not* ready is the WORK_DIR contract.

**Why now — the concrete hazard.** `COST_MODE=aura` writes its final table to `${WORK_DIR}/artifacts/cost.pkl` (`:364`) — the **same path** as `local` (`:316`) and `production-render-score` (`:346`). `[2d]`'s reuse test is a bare `[[ ! -f "$COST_PATH" ]]` (`:909`), as are `[2b]`/`[2e]` (`:690`, `:800`); none is under `require_stage_settings`. And `grep -c 'cost_mode\|estimator\|provenance' allocator.py` → 0. Net: re-running an existing WORK_DIR under the new default silently allocates on the **old** `output_mse` table while the log says AURA — D6's silent-reuse class landing precisely on the flip. The fix pattern is already in-tree: `[2d-CB]` decides reuse by reading `provenance["expert_empirical_cost"]` out of the payload (`:971-982`), not by path existence. Preconditions: **(i)** stamp `provenance["cost_mode"]`, make `[2b]/[2d]/[2e]` reuse conditional on it, put `cost.pkl` under `require_stage_settings`; **(ii)** wire `aura_additivity_gate.py` — it exists, is tested (`tests/test_aura_cost.py:193,222`) and is invoked by nothing (`grep aura_additivity_gate run-pipeline.sh` → 0). That gate **is** the small-scale gate: it records `residual = measured_end_KL − Σ predicted_dloss_i` with an honest per-probe stderr, turning AURA's one structural assumption into a per-artifact number instead of a two-model memory.

**Blast radius is smaller than it looks.** CB and GGUF hard-`exit 2` unless `COST_MODE=local` (`:99`, `:121` — the imatrix-weighted-export rendering confound), so a default flip touches only the compressed-tensors lane. AURA is structurally unavailable on CB/GGUF for that same confound reason (see R3); §4.3 should say so, since CB is where the frontier work lives.

**Cost** S+S+S. **Risk** with (i) in place a stale WORK_DIR *fails* instead of silently mis-allocating — a deliberate, loud break; historical reproduction stays available via explicit `COST_MODE=production-render-score`.

**Verdict-request.** Accept "stale cost tables hard-fail on a mode change" as the back-compat story (lens recommendation — silence is worse than a stop), or add `--allow-cost-mode-mismatch` mirroring `--allow-legacy-fisher-norm`? And: flip on 4B + 27B + 35B, or require one more architecture first?

> **DISAGREEMENT (R2 vs R19).** Precondition (ii) wires `aura_additivity_gate.py`; Lens 6 lists the same file among orphans to wall ("AURA additivity cleared"). Robert must pick: wire it as a standing per-artifact trust-region readout, or wall it as a closed thread.


**Outcome: precondition (i) IMPLEMENTED 2026-07-30; the default flip is NOT done.**
Every producer — `incremental_measure_quant_cost`, `production_render_cost`, `aura_cost`,
`expert_empirical_cost`, and the inline sidecar-backfill finalize — now takes `--cost-mode` and
stamps `provenance["cost_mode"]`. `run-pipeline.sh`'s `cost_table_reusable()` makes reuse of the
**allocator's** table conditional on that stamp matching `$COST_MODE`, and `cost.pkl` /
`cost_aura.pkl` are under the new R5 guard. Behaviour on a mismatch is **rebuild with a loud
line naming both modes** (the in-tree CB-hybrid provenance-probe pattern the proposal points
at), not a hard stop; an unstamped pre-R2 table warns and is reused, never invalidated. Under
`COST_MODE=local` the baseline *is* the allocator table and carries the same gate; under the
other modes `cost_baseline.pkl` is mode-agnostic and is deliberately shared across mode
changes. Precondition (ii) (`aura_additivity_gate` wiring) and the `COST_MODE=aura` default
flip remain open — the flip waits on the R3 axis split.

> **OUTCOME (wave 4, 2026-07-30) — BOTH REMAINING HALVES DONE; `COST_MODE` DEFAULT IS NOW `aura`.**
>
> - **Precondition (ii) — `aura_additivity_gate.py` is WIRED**, which also resolves the **R2-vs-R19 disagreement in favour of R2**: it is a standing per-artifact trust-region readout, not a closed thread. Stage `[3c]` computes `residual = measured_end_KL − Σ predicted_dloss` with the module's honest stderr (exact per-probe when the rows carry `x2_per_probe`, else the independence lower bound, and it records which) and stamps the result into `cost.pkl`'s `provenance["additivity"]`, so every artifact derived from that table carries the number. It is a **report**: non-blocking, never touching an allocation.
> - **The cost of always having a residual, and the choice made.** A residual needs a *measured* end-KL, which under `SELECTION_MODE=surrogate` the pipeline does not otherwise produce. `AURA_ADDITIVITY_GATE=auto` (default) therefore reports from a measurement the run already made (validated-surrogate's frontier JSON) and otherwise records the predicted sum with `measured_kl: null` and a status naming why; `measure` opts into one bounded KL eval against the same dW cache AURA costed on. Adding an unrequested GPU stage to the *default* cost mode was judged the worse failure — this is flagged for Robert as the one place the wiring is weaker than "every artifact carries a residual". **Robert ruled 2026-07-30: `measure` is the default.** Every `COST_MODE=aura` run now performs the one bounded end-KL eval and every artifact carries a real residual; `auto` (report only from a measurement the run already made) and `0` stay selectable. The report stays non-blocking and never touches an allocation.
> - **The flip.** `COST_MODE` defaults to `aura`. Evidence per §2.4: −38%/−39.5% served confident-KL at the 4B knee across two independent calibrations with PPL agreeing, −17.9% at 27B, both flagships produced with it, MoE hole closed by the M4 hybrid. `production-render-score` stays fully supported as the explicit/legacy spelling. Back-compat story: **accepted as the lens recommended** — a stale `WORK_DIR` rebuilds its cost table with a loud line naming both modes; no `--allow-cost-mode-mismatch` escape hatch was added, because the thing it would allow is the silent mis-allocation the stamp exists to stop.
> - **Non-regression measured, not asserted.** Allocator re-solve on the two shipped CB artifacts under `/home/rob/dq-runs` (`prod-27b-nvfp4cb-5p5`, `prod-35b-nvfp4cb-4p75`), same probe/cost/formats/target as their pipeline specs: **0/614 (27B)** and **0/500 (35B)** changed vs the pre-wave-4 HEAD (`5131297`, materialised with `git archive`). Both were `COST_MODE=local` runs, so the flip cannot reach them — this measures that nothing *else* in wave 4 did. The 32/500 delta of the 27B-era re-solve against the 35B's *shipped* `layer_config.json` reproduces identically at `5131297` and is the pre-existing Fisher-renorm delta wave 3 already recorded.
> - Docs updated in the same change: `ARCHITECTURE.md` §3.3 / §4.2 / §4.3 / §4.7 + the stage table, `docs/design/runtime_flags.md`, `CLAUDE.md` §6 + §8, and `tests/test_architecture_doc.py` (which derives the pinned default from `run-pipeline.sh`, plus three new tests pinning the flip, the axes and the D15 CB defaults).

### R9 (L1 P3 + L5 P3) — D1 tail-veto: one reduction away, not a second eval pass · STRUCTURAL

**What.** Every KL site forms `kl_per_pos` `[rows, L]` and immediately discards it with `.mean(...)` — `kl_measurement.py:2367-2373`, `:3904-3909`, `:4275-4279`, `:4446-4451` (line numbers as of this snapshot's HEAD `ef50495`; post-split the surviving site is `kl_measurement.py:850-857` in `_replay_lane_kl_totals` and the other three went into the wall at `archive/l3_propagated_2026-07-30/prismaquant/kl_measurement_l3.py:3183-3188`, `:3554-3559`, `:3725-3730`). On the selection side `_measure_inplace_assignment_kl` (`validate_assignments_kl.py:664`) accumulates per-sequence KL in `values` (`:716,758`) and discards it at `:770` (`return sum(values)/max(len(values),1)`); `measure_assignment_kl` (`kl_measurement.py:5441`; now `:1051`) is the same shape. Return `(mean, per_seq, stats)` and emit at **zero extra forward cost** `kl_p95`/`kl_p99`/`kl_max` — deliberately the **same key names the gold lane already emits** (`tools/measure_vllm_full_kl.py:401-406,444-445`), so a selection row and a served row become directly comparable for the first time. `_kl_repeat_summary` (`:640-660`) gains `kl_per_sample` concatenated across repeats.

**A true rung-2 term, also free.** Student logits and `calib_ids` (the labels) are both in hand at `:751-758`; per-sequence token NLL is one `gather`, no extra forward. Emit `nll_mean`, `nll_p99` — a PPL-family statistic in a selector that today has only KL, and §2.3 says rung 2 can veto rung 1.

**Where the veto binds.** `select_validated_frontier._frontier_from_rows:214-231`. Today admission is one line: `row["kl"] < best_kl - floor`. Change to admit only if that **and** `row[tail] <= incumbent[tail] * (1 + tail_eta)`, with `tail` selected by `--tail-veto {none,kl_p99,nll_p99}` and `--tail-eta` (default `none`/`inf` ⇒ byte-identical). Vetoed rows retained with a `veto_reason` so a rejection is visible, never silent. `measured_rows` (`:180-210`) gains passthrough columns; `practical_knee`/`saturation` need no change. `kl_repeats` (`validate_assignments_kl.py:654`) already supplies the between-seed spread to set the floor from data rather than taste.

**Why now.** D1 is HIGH, raised 2026-06-05, never built — and it stalled on an assumed cost (a second eval pass) that does not exist. **Cost** M. **Risk** zero by construction while `--tail-veto=none`; a regression test asserting frontier identity on a stored results JSON pins it.

**Verdict-request.** Which statistic is the contract — `kl_p99` (positions), `kl_max`/max-over-sequences (the statistic that would have caught the broken 27B where the mean passed while 80% of prompts were bad), or `nll_p99`? §7.2's ship gate already uses p99 per-prompt NLL, which argues for max-over-sequences as primary. And: ship default-off (recommended), or default-on once a 27B run shows the two picks agree?

**Outcome: ACCEPTED, implemented 2026-07-30** — both selection paths now return `(mean, per_seq, stats)` and emit `kl_p95/kl_p99/kl_max` + the free rung-2 `nll_mean/nll_p99` under the gold lane's key names (`kl_tail_domain: "sequence"` records that the sample unit is a sequence, not a position); `_frontier_from_rows` gained the `--tail-veto {none,kl_p99,kl_max,nll_p99}` / `--tail-eta` admission term with vetoed rows retained under `vetoed_rows` + `veto_reason`, shipped **default-off and pinned byte-identical** by a frontier-identity test.

> **Robert ruled 2026-07-30: enabled, `kl_max`, repeat-derived eta.** The veto is **DEFAULT-ON** with `kl_max` (max over sequences) as the **contract statistic** — the one that would have caught the broken 27B that passed on the mean while 80% of its prompts were bad; `nll_p99` keeps being recorded on every row, so changing the contract later is a flag, not a re-measurement. `--tail-eta` defaults to **`auto`**: the slack is *derived* (house rule 2) as the incumbent row's relative stderr of the tail statistic across calibration repeats (`std/sqrt(n) / mean`, floored at 0), which required `validate_assignments_kl` to keep the per-sequence tails **per repeat** as well as pooled — free, same forwards. One repeat has no spread, so `auto` degrades to a strict `0` **and prints a warning** naming the single-seed history (+10% flipping to -5.2%). An explicit numeric `--tail-eta` still wins; the derivation is documented at `select_validated_frontier.tail_eta_auto`. Two consequences of turning it on were handled rather than argued: the `--tail-veto` help text now states the asymmetry (a spurious veto only makes the pick MORE conservative, and stays visible via `veto_reason`), and a pre-R9 validation JSON — no tail column on any row — makes the veto go **inert with a loud warning** instead of vetoing every row and handing back an empty frontier. Tests: the default-on case is now the pinned one (CLI default `kl_max`, the inert path, the derived-eta behaviour), with the `none`-mode identity test kept.

### R20 (L1 P4) — CB ladder law: no served A/B warranted; unify the chains, derive the tolerance · STRUCTURAL

**What.** The split-aware floored law is the right family, and "+14.2% on exact-exponential data" is not an argument against it. (1) **The holdout gate arbitrates accept/reject of one law, not a bake-off.** `_ladder_metric_fit` (`measure_quant_cost.py:1621-1671`) returns from branch 1 (floored linear in `R(k)`) whenever `C > 0`; branches 2/3 are reached only on non-monotone/degenerate anchors. On the unit test's free-rate exponential the fit misses the holdout by 31% against `_CB_LADDER_TOL = 0.10`, the tensor is rejected, and the predicted rungs are **measured** (`:2002-2007`; test `tests/test_measure_quant_cost_packed_experts.py:377-381`). The law's only material cost is encode wall-time, not shipped cost fidelity. (2) **The two chains have diverged.** The expert path (`expert_empirical_cost._cb_ladder_fit`, `:442-478`) is floor-law → log-linear with **no `R(k)` term at all**. The ceil-first sawtooth argument that motivated the dense change (commit `5184892`; the (28,38,48)+k=39 phase mismatch behind 8–12% rejections on the 2026-07-21 27B run) applies identically to unit-KL rungs and never landed there. (3) **`_CB_LADDER_TOL = 0.10` (`:1557`) is a bare constant where an explicit exists** — `encode_tiers.md` §B states the rule ("trust the fit only where the holdout error clears the between-seed cost noise") and that noise is measurable from the same anchors. House rule 2 applies.

**Cost** S–M. **Risk** none — `PRISMAQUANT_CB_LADDER_INTERP` is default OFF (`measure_quant_cost.py:1587`); only the 35B CB run set it explicitly. **Verdict-request.** Agree the law needs no served A/B and the work is chain unification + derived tolerance + a visible accept/reject rate? Or freeze the ladder until the CB lane needs it again?

> **OUTCOME — ACCEPTED and IMPLEMENTED 2026-07-30** (Robert; no served A/B, all three pieces). Default stays OFF.
> - **One chain.** `expert_empirical_cost._cb_ladder_law` (`:482`) is now THE law — floored linear in the exact ceil-first `R(k)` → smooth floor law → log-linear — behind one shared gate `_cb_ladder_gate` (`:627`). `measure_quant_cost._ladder_metric_fit` / `_ladder_rate_factor` are thin delegates; the dense call site (`:1940`) gates through the shared helper, which also fixes a latent `TypeError` (`rel_err` was computed from `pred_h` *before* its `None` check). The expert chain gains `R(k)`: on the (28,38,48)+k=39 phase case a log-linear fit through those anchors misses by **14.9%**, the shared law is exact.
> - **Derived tolerance, and the estimator that does NOT work.** `_cb_ladder_holdout_tol` (`:559`) implements `encode_tiers.md` §B literally: refit the law on each calibration window, take the standard error of the *paired* holdout residual. Free on the expert path — `_unit_kl(per_window=True)` returns the per-window KLs the aggregate is already built from. The obvious cheaper estimator, **anchor fit residuals, was tried and rejected**: it cannot separate measurement noise from law misfit, so a systematically wrong law inflates its own tolerance — measured, it returned a **252% tolerance against a 127% holdout miss**, i.e. it opens the gate exactly where the gate must close. The bare `0.10` therefore survives as the value used where the datum is genuinely absent — every dense-path fit, since that path measures each `(tensor, format)` exactly once (`_count == 1`). Where the datum exists the derived value *replaces* it and can tighten: a 2.9% miss that `0.10` waved through is rejected at the measured 1.29%.
> - **Visible rate.** Both paths print `accepted/total (pct), N rejected -> measured` at the end of the stage, and the dense per-chunk reject line now carries the running accept rate; the expert `_ladder` metadata records `holdout_tol` alongside `holdout_rel_err`.
> - **Tests.** `tests/test_measure_quant_cost_packed_experts.py` +2 (shared-chain identity incl. the k=39 sawtooth vs log-linear; derived-tolerance behaviour: absent → floor, zero-spread → floor, real spread → 1.29% and it bites). `tests/test_expert_empirical_cost.py` / `tests/test_nvfp4_cb_formats.py` updated for the 3-tuple return and the new leading branch — note the free-rate-exponential fixture the old expert chain fitted *exactly* is now **rejected by the gate**, which is the point of the unification. 172/172 green across the three files.

**L1 hygiene (→ R28).** `--kl-ucb-z` / `PRISMAQUANT_COST_UCB_Z` are both dead in the pipeline — correctly so at production calibration (6/252 churn, served parity) — but §4.3 documents two knobs no driver sets; name the thin-calib regime where z=2 won −8.0%, or mark them research-only in `runtime_flags.md`. `_RD_LOG_LINEAR_R2_THRESHOLD` is honestly labelled a diagnostic threshold (`allocator.py:322-326`) — leave it; noted so no future reader re-files it as a heuristic.

---

## Lens 2 — pipeline & orchestration

*§3, D6/D8/D9/D10.* **No fundamental change to the orchestrator is warranted.** Measured shape, since "1700 lines of bash" overstates the bash: 1516 bash lines + 177 across 14 python heredocs; 25 subprocess invocations of 14 modules; 80 `if [[`; 29 `exit 2`; exactly **one** shell function (`require_stage_settings`, `:492`). A straight-line script with a gate preamble, not a control-flow thicket. The 60 commits since 2026-05-01 are the GGUF/CB lanes being *added*, not bash being *fought* — none of the last 15 is a bash bugfix.

### R3 (L2 P1 + L4 P2) — Split `COST_MODE` into (render source) × (cost objective); gate lanes on the render · FUNDAMENTAL

**What.** `COST_MODE` silently decides two independent things: which *render* produces the per-`(Linear,format)` error, and which *objective* maps it to `predicted_dloss`. Split into `COST_RENDER ∈ {inline, cached-menu}` × `COST_OBJECTIVE ∈ {weight-recon, render-score(M6), aura-adjoint}`; the lane gates then assert only that the render is exporter-faithful.

**Why now.** The gates name the wrong invariant: `run-pipeline.sh:98-101` and `:130-133` reject non-`local` because "production-render-score scores UNWEIGHTED registry renders" — a property of the *render*, not the objective. And `local` is already not one thing: `measure_quant_cost.py:1442-1449` (`_cost_render_uses_imatrix`) returns True unconditionally for `_CB_COST_FAMILIES = ("nvfp4_cb","fp8_cb")` (`:1439`) and for GGUF under `PRISMAQUANT_GGUF_IMATRIX`, so under `COST_MODE=local` the CB lane measures the exporter-faithful imatrix-weighted VQ encode (`:716-720`, `:1516-1520`) while the compressed-tensors lane measures unweighted RTN qdq — same enum value, two renders, keyed correctly off the format family. The right key exists; the gate does not use it. The lane already strains against it: `CB_EXPERT_EMPIRICAL=1` (`:966-1035`) bolts measured empirical expert unit-KL onto the "local" payload via `--replace-experts` — a hybrid objective inside a gate that claims to forbid anything but `local`.

**The missing piece is shared with CB Milestone C.** `aura_cost.py` consumes rendered dW through a duck-typed `.get(name,fmt)` (`:214-226`; `--production-cache` `:758`) — no CB-specific work; `production_weight_cache.py:1376` already knows CB group semantics and `format_registry.py:913-973` gives every CB rung a registry `quantize_dequantize`. **The one missing piece is a `col_weights` argument on the production render path** so a CB format-menu cache renders imatrix-weighted (the vector is already on disk: `artifacts/cb_col_weights.pkl`, stage 2a-CB `:617-643`). That is exactly CB Milestone C (`run-pipeline.sh:1655-1662`, deferred per `format-pipeline.md §6`): cost == KL == shipped bytes through `ProductionWeightCache`, letting the `COST_MODE=local` / `PRODUCTION_CACHE=0` gates lift. Only then does CB get AURA cost, validated-frontier selection, and the render identity principle 8 exists to protect — and only then is a per-arch CB default defensible.

**Lens 4's framing of the same decision.** The measured case for CB is as strong as it will get: 27B matched-byte (16.713 vs 16.707 GB) ALL-KL −56/−58%; 35B conf-KL −53%; Hy3 prefill 2.1–2.6× the GGUF-IQ path. If that does not fund the integration, nothing will, and CB should be documented as permanently manual. The counter-argument belongs in Robert's terms: **the native lane's promise is that its correctness depends on nothing we maintain.** CB replaces that with 12 `csrc` files including a CUTLASS fork with sm_120-specific smem budgeting, a PyPI package at in-tree `0.1.0` (`gb/__init__.py:13`) with unverified index skew (D27), single-GPU only, and a materially wrong README (D14). That is a product, and products need owners.

**Cost** M–L: `col_weights` through `render_production_weight` + `build_production_cache` for the CB/GGUF families; enum rename with back-compat aliases; rewrite the two gates as a render-faithfulness assertion. Excludes measuring whether AURA beats weight-recon on CB — a separate served A/B, the only thing that justifies shipping it. **Risk** low if aliased (`local|production-render-score|aura` keep today's meaning); medium on the shared render path, so it must be additive (CB-only branch) with native/GGUF renders bit-identical. The real risk is using the new freedom before the A/B — land AURA-on-CB opt-in and non-default.

**Verdict-request.** (1) Split the axes, or is `COST_MODE`-as-lane-shorthand fine? (2) Fund Milestone C as the prerequisite to per-arch CB defaults, or freeze CB as a premium manual lane and stop carrying the gate as debt? Lens 4's recommendation absent an emphatic yes: **fund Milestone C for its own sake — it removes the rendering confound the gates were written to avoid — while leaving the default at native.** (3) Given that, is AURA-on-CB worth an A/B slot, since AURA's −38%/−17.9% wins were on the native lane and CB's error surface (VQ, route-flip floor) is a different animal?

> **OUTCOME — ACCEPTED and IMPLEMENTED 2026-07-30 (wave 4).** All three verdict-requests answered as the lenses recommended: **fund Milestone C, keep the default at native, land AURA-on-CB opt-in.**
>
> - **Milestone C closed.** `render_production_weight` and `build_production_cache` take `col_weights`, applied to the **weighted-render families only** (`production_weight_cache.WEIGHTED_RENDER_FAMILIES` = the two CB families + `gguf`) through **one** definition — `emu_forward_kl.weighted_quantize_dequantize`, which the emulation path and the inline cost render already used. The CB lane's cost, KL and shipped bytes can now come from ONE `ProductionWeightCache` render. `_format_supports_render_mechanism` gained the `gguf` family on `weighted_vq` and `weighted_vq` is now a **declared** render mechanism (`render_score.py`) rather than a bare `if`, per R26's pattern. The additivity risk the proposal named is pinned rather than argued: `tests/test_col_weights_render_identity.py` asserts NVFP4 / FP8_E4M3 / MXFP4 / MXFP8_E4M3 / BF16 renders are **bit-identical** with and without the argument, that the weighted families equal the shared definition exactly, and — the guard against a vacuous pass — that they actually move under a real imatrix. `build_production_cache` refuses both confounding combinations (weighted family in the menu with no vector; a vector supplied for a menu with no weighted family).
> - **Axes split, aliases exact.** `COST_RENDER ∈ {inline, cached-menu}` × `COST_OBJECTIVE ∈ {weight-recon, render-score, aura-adjoint}`; `COST_MODE=local|production-render-score|aura` keep today's meanings and remain the documented spellings. Setting both spellings is `exit 2`; the two unimplemented pairs stop with the reason rather than falling through. ARCHITECTURE.md **§4.7** is the reference.
> - **Gates rewritten as a render-faithfulness assertion.** The CB/GGUF `COST_MODE=local` requirement is **gone**. What replaces it asserts the invariant the old gate was proxying — the render that produces the allocator's cost must be the render the exporter ships — keyed off the format family exactly as `_cost_render_uses_imatrix` already was. On a `cached-menu` render the pipeline harvests the imatrix and passes `--col-weights`; the harvest itself became ONE shell function (`harvest_cb_col_weights`) replacing three near-copies, now with four call sites. `PRODUCTION_CACHE=0` stays required on those lanes for its own unchanged reason (their exporters never read the export cache).
> - **Deliberately NOT promoted.** render-score / aura objectives on CB are reachable and print an OPT-IN line when selected. No CB served objective A/B was run, and none was attempted. **AURA-on-CB served A/B DEFERRED by Robert 2026-07-30:** queued behind the NVFP4-vs-CB-FP8@4.5 criteria work; CB stays weight-recon until it runs. The same ruling flipped `AURA_ADDITIVITY_GATE` to `measure` (see the R2 outcome), closing the R3c/R2 residue.
> - **(1c) The additivity gate is WIRED** — see the R2 outcome; that also settles the R2-vs-R19 disagreement in favour of wiring.
>
> **Deviation, recorded.** The additivity report was specified as "a stage after the aura `[2d]` finalize". It is stage **`[3c]`** instead: the residual needs a concrete assignment and none exists at `[2d]` (the allocator has not run, and under validated-surrogate the assignment is not final until the frontier is selected). `[3c]` is the first point where `layer_config.json` is final under both selection modes.

### R5 (L2 P2 vs L6 P2) — `pipeline.py`: settings-hash authority, or linter-or-gone · FUNDAMENTAL / DISAGREEMENT

**Lens 2 (promote, narrowly).** `pipeline.py` owns the one thing the shell provably gets wrong: which settings each artifact's identity is keyed on. It emits, per artifact, that key set and its values; `require_stage_settings` reads it instead of hand-passed `k=v` pairs. Execution stays in bash. Evidence: D6 holds — the guard (`:492-522`) is invoked at 6 sites (`:539,606,666,851,1372,1455`); 10 artifacts are bare `[[ ! -f ]]`, each verified: `:690` (`cost.pkl`, under *every* mode), `:800`, `:879` (`cost_aura.pkl`), `:909`, `:981`, `:1134` (frontier cache), `:1239`, `:1329` (frontier recache), `:1421`, `:1556`. Enumerating ten more call sites is a fix that decays — the eleventh stage arrives without one, exactly as the CB lane added four. Meanwhile `pipeline.py` already receives every setting a hash needs (`run-pipeline.sh:462-481` passes 11, incl. formats/target-bits/target-profile/selection-mode/render-mechanisms) and declares 14 artifacts and 9 stages (`pipeline.py:821-1001`) — it has the data and no consumer. This closes D6 by *mechanism* rather than enumeration, and D10 by giving the contract layer one real job. D10's other half is bookkeeping: delete `StreamingActivationCache` and `StreamingModelPrefetch` from `APPROVED_RESOURCE_OWNERS` (`pipeline.py:23,25`) — verified absent from the whole tree — and name `layer_streaming.LayerCache`; `kl_measurement.QuantWeightCache` (`:89-107`) is a second `.get(name,fmt)` rendered-weight holder, L3-only so not a one-cache violation, but it belongs in the owners map as research-scoped rather than unmodelled. **Cost** M (2d). **Risk** low and asymmetric — the only new failure mode is a hard stop on a reuse that would previously have silently produced a confounded artifact; the real risk is over-keying (hashing a setting an artifact does not depend on forces spurious 90 GB rebuilds), mitigated by declaring key sets per artifact and reviewing them once.

**Lens 6 (demote or delete).** (a) delete `pipeline.py` (1,178 L) + the `run-pipeline.sh:462-481` invocation and let the runtime strict-cache gates carry the one-cache claim; or (b) keep it, rename its role to "spec linter", and stop citing `APPROVED_RESOURCE_OWNERS` as *the* declarative contract in CLAUDE.md §6. Validation is tautological — it validates the spec `default_production_pipeline_spec()` just generated from its own hardcoded contracts, and `run-pipeline.sh` never passes `--input` (`pipeline.py:501-563,890-980`). Two of three approved owner names exist nowhere else in the tree; nothing reads the emitted JSON back; 10+ executed stages unmodelled; `validate.vllm_smoke` always stripped; last touched 2026-06-12. **A contract that cannot fail is not a contract.** **Cost** S for (b), M for (a). **Risk** (b) zero; (a) loses a structural sketch — but ARCHITECTURE §3.2's stage table now *is* that map, and it is code-cited. Lens 6 recommends (b) now, (a) never.

**The fork.** Both lenses agree `pipeline.py` is not enforcement today and must not become the executor. They disagree on whether it should be *given* enforcement (L2: it is the only place holding the data to key artifact identity) or *stripped of the claim* (L6: give the shell guard the ten missing sites and stop pretending). **Dependency:** if Robert wants the python port regardless (R23), say so before R5 lands — L2's helper would be thrown away.

**Verdict-request.** Make `pipeline.py` load-bearing this narrowly, or delete its validation and keep the shell guard hand-written? Under the first option: accept forced rebuilds on any live `WORK_DIR` when key sets land?


**Outcome: RESOLVED in favour of Lens 2's narrow promotion — implemented 2026-07-30.**
`pipeline.py` is now the settings-hash authority and nothing more: `STAGE_SETTINGS_KEYS`
declares, per artifact, which settings its identity is keyed on; `run-pipeline.sh` supplies
every value once (`STAGE_SETTINGS_ENV`) and `--write-stage-settings` projects them into
`artifacts/stage_settings.json`; `require_stage_settings <artifact> <stage> [LATE=v …]` calls
`--check-stage-settings` instead of hand-passing `k=v`. Execution stays in bash (R23 confirmed).
Coverage is now **every** skip-if-exists artifact — 16 call sites over 15 declared artifacts,
including all ten the audit enumerated. The manifest is keyed by stage, so the two stages that
legitimately share one path (AURA dW cache == frontier cache under `validated-surrogate`)
coexist; pre-R5 flat manifests are read as a `legacy` block and still guard the stage whose key
set they match, so **no live `WORK_DIR` is invalidated** — the named over-keying risk was
handled by preserving historical manifest key names and by keying the probe on
model/corpus/windows/modality but **not** on `FORMATS` (it is format-blind). Missing manifest,
and a stage that never recorded one, stay WARN. D10 bookkeeping done: the two phantom owner
names are deleted, `streaming_model_weights` names `layer_streaming.LayerCache`, a test asserts
every approved owner has a class behind it, and `QuantWeightCache` needed no entry — it went to
the archive wall with L3 in wave 2. Closes **D6** and **D10**.

### R23 (L2 P3) — Adjudication: do **not** move stage execution into `pipeline.py`-as-executor · STRUCTURAL

An explicit "no", recorded so it is not re-litigated. The complexity the rewrite case rests on could not be evidenced. Against: (i) ~60 lines of env plumbing per subprocess does not shrink in python — it becomes `subprocess` arg lists of the same arity; (ii) the graveyard's lesson is that rewrites need measured need, and our actual failure class (D6 silent reuse) is language-orthogonal and fixed by R5 in 2 days; (iii) the three lanes are near-disjoint straight lines, not a matrix — GGUF `exit 0`s at `:1523`, CB at `:1661`, so lane × mode interaction is bounded by two preflight gates; (iv) `run-pipeline.sh` is the operational muscle memory of every launcher and handover, and a port invalidates it as the Strix Halo work starts. Honest cost if you want it anyway: **L, 1.5–2 weeks**, plus end-to-end re-validation of one 27B-class artifact proving byte-identical output — that validation, not the port, is the real bill. **What would flip this:** if lanes stop `exit 0`-ing and start composing (one run exporting both compressed-tensors and CB from one cache), the straight line breaks and bash stops being the right host. **Verdict-request.** Confirm "no port", and confirm which side of R5 is the accepted D6 fix.

> **OUTCOME — ACCEPTED 2026-07-30 (wave 4). No port. Decision only; zero code.**
> `run-pipeline.sh` stays the executor and `pipeline.py` stays the settings-hash
> authority and nothing more (the R5 side that landed in wave 3 is the accepted
> D6 fix). Recorded here so it is not re-litigated: the case for the rewrite
> rests on a complexity that could not be evidenced, the honest bill is **L,
> 1.5–2 weeks plus a byte-identical 27B re-validation**, and the flip condition
> is unchanged — lanes composing rather than `exit 0`-ing. Wave 4 moved slightly
> *toward* that condition in one respect (the CB lane can now legitimately use
> the cached-menu cost render), which is worth watching but does not meet it:
> the three lanes still terminate in disjoint `exit 0` branches.

### R24 (L2 P4) — One hardening pass: D8 + D9 + the guard rollout · HYGIENE

**D8** — `export_native_compressed._production_cache_prefetch_assignment` (`:2090-2110`) returns `0` on every failure path (no cache / no `prefetch` attr / no keys) and its sole caller (`:6237-6246`) prints only when `prefetched > 0`, so a total miss is invisible and the export goes NVMe-bound; add a `require` mode mirroring `production_weight_cache.py:461-478`, plumb `--production-cache-prefetch`, default `require` on the native lane to match `VALIDATED_SOURCE_PREFETCH=require` (`:282`). **D9** — `require_cuda_hot_path` has 6 callers (`build_production_cache`, `expert_empirical_cost`, `production_recache`, `validate_assignments_kl`, `kl_sensitivity_probe`, `export_native_compressed`) and is missing from `incremental_probe`, `incremental_measure_quant_cost`, `aura_cost`, `production_render_cost`, `export_nvfp4_cb[_streaming]`, `export_gguf`, `select_validated_frontier` — exactly the stages the CB/GGUF ladder work invokes directly, bypassing the shell preflight (`:134-145`); one line each in `main()`. **Guard rollout** — the 10 sites from R5, on R5's mechanism. **Cost** S (<1d). **Risk** D9 ≈ nil (confirm no CPU-only test imports those `main()`s); D8's only risk is newly failing an export whose cache was legitimately absent — ship `require` on the native lane, `warn` elsewhere. **Verdict-request.** One branch, or split D8 out (the only item that can newly fail a run that used to pass)?

---

## Lens 3 — plugin architecture & per-model configuration

**No structural flaw. The residue is wiring debt — but of one repeating shape, and that shape is the real finding.** The three-registry design is load-bearing: allocator, solver, caches, exporter and `pipeline.py` carry zero arch conditionals, the three-tier precedence has absorbed 9 of 11 architectures, and the `DefaultProfile` fused-coherence gate (`allocator.py:1550-1554`) fails proactively rather than guessing. What §8.5 does not yet name is that **all four leaks, plus one more, are the same failure: a declarative extension point is defined, parsed, documented — and has no production caller.** `spec.default_serving_profile` (L1), `profile.build_mtp_module` (L2), `spec.unpacked_expert_projection_names` (§8.4), and — new here — **`spec.match`, which all 9 spec files declare, `structure.py:185` parses into `ModelStructureSpec.match`, and nothing in the tree reads** (grep: zero consumers outside the parser). Dead config decays: `qwen3_5_dense.json`'s `match` is **not equivalent** to `qwen3_5_dense.py:23`, which additionally requires `not any("Moe" in arch)`.


**Outcome: ACCEPTED, implemented 2026-07-30 as ONE branch (D8 + D9 + the guard rollout).**
**D8:** `_production_cache_prefetch_assignment` gained a `require` mode mirroring
`production_weight_cache.prefetch_assignment(require=…)`, exposed as
`--production-cache-prefetch {require,warn}`; `run-pipeline.sh` passes `require` on the native
lane (matching `VALIDATED_SOURCE_PREFETCH=require`) and the CB/GGUF lanes read no production
cache at all, so `warn` elsewhere is vacuous. Every silent-zero path — no cache, no `prefetch`
attribute, quantized entries resolving to zero keys, keys loading nothing — is now a named
failure under `require`; an all-BF16 prefix still legitimately prefetches nothing.
**D9:** the guard is at `main()` entry (not import time) in all seven stages, after confirming
no CPU-only test imports those `main()`s (`tests/test_prismaquant_visual_phase2.py` only reads
`incremental_probe.main`'s source; `alloc.main()` is the allocator, which is not in scope). A
parametrized test pins all twelve `require_cuda_hot_path` callers.
**Guard rollout:** landed on R5's mechanism.

### R8 (L3 P6) — Spec-expressible `matches()`: under-engineering today, not over- · FUNDAMENTAL

**What.** Consume `spec.match`. Add `SpecMatchProfile`, a concrete `ModelProfile` evaluating `spec.match` with `name` = spec `id`; `registry._resolve` builds one per spec file no Python profile claims. Vocabulary needed: `model_type` (exact list), `architectures` (glob list), one negative `architectures_exclude`, and a `priority` int replacing the load-bearing order comment at `registry.py:46-57`.

**Why now — argued from the Python that exists.** All 10 `matches()` were read. **Nine are pure declarative predicates** over `(model_type ∈ set, arch startswith prefix)` and are already transcribed into their spec `match` blocks. The two that are not — `qwen3.py:25` (exact `Qwen3ForCausalLM`, no prefix) and `qwen3_5_dense.py:23` (`not any("Moe" in arch)`) — need exactly the two vocabulary items above. That is the whole delta. Not speculative generality: **the field, the data and the parser all already exist (`structure.py:185`); only the reader is missing**, and one transcription has already drifted wrong precisely because nothing reads it. On the behaviour-vs-naming split: the per-arch Python that genuinely must stay Python is small and well-identified — MTP construction (R12 keeps it Python, correctly: it materialises modules), the seven streaming-probe adapters (`base.py:823-947`), Gemma4 cross-layer forward state (`:949+`), `register_vendored_modeling()`. Those are forward-pass behaviour and §8.2 is right that JSON is the wrong medium. `matches()` is not in that set. Tier A has never happened for exactly one reason: `matches()` is abstract (`base.py:57-61`). Remove that obstacle and a new dense arch with a vLLM `packed_modules_mapping` is genuinely zero-Python.

**Cost** M. **Risk** medium, and worth naming: detection order governs which profile a shipped model resolves to; getting it wrong on the Qwen family silently re-routes every future 27B/35B run. Mitigation is strict — land `SpecMatchProfile` **alongside** the Python profiles, plus a test that for every registered profile, on its real `config.json`'s `(model_type, architectures)`, the spec-match verdict equals the Python verdict. Only after that is green for a release do the Python bodies get deleted, one arch at a time. Its first act fixes `qwen3_5_dense.json`.

**Verdict-request.** (1) Is zero-Python Tier A worth this, or is the honest position "Tier B is the contract — delete the dead `spec.match` field instead"? Both are defensible and the second is cheaper; what is *not* defensible is leaving a decayed, unread `match` block in 9 files. (2) If yes, approve `priority` replacing comment-encoded registry order.

**Outcome: ACCEPTED, implemented 2026-07-30** — `spec.match` now has a reader. `SpecMatch`
(`structure.py`) parses the three-key vocabulary (`model_type` exact / `architectures` globs /
`architectures_exclude` veto) and **raises on any other key**; `SpecMatchProfile`
(`model_profiles/spec_profile.py`) binds one spec and answers `claims()`, and
`registry.detection_order()` builds one per spec file no Python profile claims. `priority`
replaced the comment-encoded `_REGISTERED` order (lower first; built-ins 100–190 in the
historical order; `ModelProfile.priority = 0` keeps `register_profile`'s insert-at-front
override intact) and is declared **both** on the class and in the spec, with a test asserting
they agree. Its first act was the fix the proposal predicted: `qwen3_5_dense.json` gained the
missing `architectures_exclude: ["*Moe*"]`. `qwen3.json` needed nothing — a bare class name is
already an exact glob. The strict gate is `tests/test_spec_match_profile.py`: every registered
profile × 32 representative configs, spec verdict == Python verdict, plus whole-registry
resolution equivalence and priority-order equivalence (331 cases). No `matches()` body was
deleted, and no shipped model's resolution changed (all ten specs are claimed by a Python
profile, so the live detection order contains zero `SpecMatchProfile`s).

### R11 (L3 P1) — Make `spec.default_serving_profile` authoritative; close the allocator↔export split · STRUCTURAL

**What.** (i) `run-pipeline.sh:91` `: "${TARGET_PROFILE:=vllm_packed_moe}"` → leave unset; pass `--target-profile` (`:471`, `:1081`) only when non-empty, and add `--target-profile-default vllm_packed_moe` so `resolve_target_profile(profile, requested, default=)` (`serving_profiles.py:611-633`) falls back to today's value — **never** to `research`, whose format menu is unbounded. (ii) Close the export half: `export_native_compressed` has **no `--target-profile` flag at all** (verified); its only channel is `PRISMAQUANT_TARGET_PROFILE` (`:1959`), which `run-pipeline.sh` never exports. Have the allocator stamp its resolved profile into `layer_config.json` — today it lands only in the side report `format_applicability.json` (`allocator.py:1917`), which export never reads — and have the exporter read it there, env var as override. Allocator and export then cannot disagree.

**Why now.** Measured cost: 226 dense FP8 Linears silently coerced to BF16 on the Hy3 CT export, 2026-07-11 (§8.5 L1 / D4), because the allocator resolved `vllm_packed_moe` from the shell while export re-resolved `gguf` from `hy_v3.json`. **Non-regression.** All 8 in-tree launch scripts already `export TARGET_PROFILE=nvfp4_cb` (`scripts/run_{laguna_s21,27b_prod_nvfp4cb,hy3_prod_joint,hy3_prod_nvfp4cb,27b_cb_20gb,35b_prod_nvfp4cb,0p8b_s_rung_headtohead}.sh`, `smoke_nvfp4_cb_pipeline.sh`) — explicit request still wins, so **every existing script is bit-identical**. Spec defaults: 7 archs declare `vllm_packed_moe` (no change); `hy_v3`→`gguf` and `laguna`→`nvfp4_cb` are always script-driven; `minimax_m2`/`deepseek_v4` declare nothing and the new default holds them at `vllm_packed_moe`. Gate: re-run the allocator on the shipped 27B/35B probes and diff `layer_config.json` — expect 0/614 and 0/500, the same check the #13–#21 stack used.

**Cost** S. **Risk** low — the only changed behaviour is an unset-`TARGET_PROFILE` run on hy_v3/laguna, which today hard-errors at `run-pipeline.sh:106/:124` anyway. **Verdict-request.** Approve the order; decide whether export's channel is `layer_config.json` metadata (lens recommendation — it travels with the artifact) or a CLI flag.


**Outcome: ACCEPTED, implemented 2026-07-30. Export's channel is `layer_config.json` metadata**
(the lens recommendation — it travels with the artifact), with `PRISMAQUANT_TARGET_PROFILE` kept
as the override for direct exporter invocations. `run-pipeline.sh` leaves `TARGET_PROFILE`
unset, passes `--target-profile` only when non-empty, and always passes
`--target-profile-default vllm_packed_moe`. The allocator stamps its resolved profile into a
reserved `__prismaquant__` block (`layer_config.LAYER_CONFIG_META_KEY`) that the schema and
every assignment parser skip; `select_validated_frontier` carries the block forward when it
overwrites the layer config. **Non-regression measured, not assumed:** re-solving the shipped
27B (`prod-27b-nvfp4cb-5p5`, 5.5 bpp) and 35B (`prod-35b-nvfp4cb-4p75`, 4.75 bpp) from their
stored `probe.pkl`/`cost.pkl` changed **0/614** and **0/500** against the same allocator without
this change. Against the *shipped* configs the 27B is also 0/614; the 35B differs by 32/500 for
an unrelated pre-existing reason — the Fisher renormalization fix that landed after that
artifact shipped, reproduced identically at HEAD. `tests/test_architecture_doc.py` was updated
in the same change: `TARGET_PROFILE` leaves the `:=`-defaults list and is replaced by a test
asserting the *absence* of a shell default plus the doc's spec-resolution statement.
Closes **D4** / §8.5 L1.

### R12 (L3 P2) — Route MTP through the profile; it needs a fourth accessor · STRUCTURAL

**What.** The three sites (`incremental_probe.py:2786`, `incremental_measure_quant_cost.py:604`, `export_native_compressed.py:8877`) import the same triple from the Qwen3.5-specific `mtp_module.py`: `MtpModule(text_config)` `:58`, `_load_mtp_state_dict(model_path)` `:123`, `_load_into_mtp` `:153`. Two map onto existing extension points (`base.py:248-256`, `:258-272`); **the third does not, and this is what the design doc misses** — `_load_mtp_state_dict` hardcodes the `mtp.` checkpoint prefix, but Hy3's MTP is `model.layers.80.*` and DSv4's nextn block is likewise a layer index. Add `mtp_source_prefix() -> str | None` (spec-expressible plain string, default `"mtp."`) and make the reader a generic base implementation keyed on it. `build_mtp_module` must return a module whose names, once wrapped in a parent named `mtp`, match the recipe names the allocator assigned (`mtp.fc.*`, `mtp.layers.0.*`) — state that as the contract, since today it is only implied by `MtpModule`'s layout. Then delete `mtp_module.py`; the near-duplicate already sits dead at `qwen3_5.py:78-126`.

**Why now.** `deepseek_v4.py:83` returns `has_mtp → True` while `:86` returns `build_mtp_module → None`, so production hands DSv4 a Qwen3.5 decoder layer. Blocks the DSv4 ship, and validator check 5 (`validate.py:246`) is the one check the new CI suite deliberately skips (it materialises a full decoder layer), so nothing catches it.

**Cost** M. **Risk** medium — touches probe and export for every shipped Qwen artifact. Mitigation: the Qwen body moves verbatim; regression test is a byte-diff of exported `mtp.*` tensors on a small Qwen3.5 checkpoint. Cheap CI surrogate worth adding regardless (closes D11's residual): assert `build_mtp_module` is not `None` whenever `has_mtp()`. **Verdict-request.** Approve `mtp_source_prefix`; decide whether DSv4 gets a real `build_mtp_module` now or declares `has_mtp → False` + `passthrough_prefixes` (the hy_v3 route) until its MTP is actually quantized.

**Outcome: ACCEPTED, implemented 2026-07-30** — `mtp_source_prefix()` + generic `read_mtp_source_state_dict()` / packed-expert-aware `load_mtp_state_dict()` added to `ModelProfile`, all three call sites repointed, the Qwen `MtpModule` moved verbatim into `qwen3_5.py` (absorbing both dead near-copies), `prismaquant/mtp_module.py` deleted, DSv4 on the hy_v3 route (`has_mtp → False` + `"mtp."` passthrough); gated by parameter-name-set equality against the pre-move layout plus the conformance ratchet (no local Qwen3.5 checkpoint was available for the byte-diff).

### R10 (L3 P3 + L4 hygiene) — Gridbook registry as data + a serve-time assertion that cannot be bypassed · STRUCTURAL

**What.** (i) Replace `plugin.py:66-118`'s `try/except ImportError` chain with a module-path tuple fed to `_install_on_module_classes` (`:38-63`) — already the version-robust, name-agnostic, inert-for-non-CB form; the three hardcoded class imports become `("…models.hy_v3", "…hy_v3_mtp", "…laguna", "…qwen3_5", "…qwen3_5_mtp")`. Gridbook ships into the vLLM container and cannot import `prismaquant.model_profiles`, so it needs its own registry — JSON sidecar or in-code tuple, either is data. Over-installing is explicitly harmless (`:44-46`). (ii) The assertion is the part that matters and **cannot live in the wrapper** — the failure mode is that the wrapper was never installed. Anchor it on `process_weights_after_loading` (`moe.py:200`), which vLLM calls for every CB MoE layer on every load path: set `_pq_cb_filled = False` on `w13_cb_qweight`/`w2_cb_qweight` at `create_weights` (`moe.py:120-142`), set True in both fill paths (the instance hook in `moe.py`, and `moe_toplevel_loader.load_weights` at its `loaded.add(mapped)` site), and raise naming the model class and missing module path if still False. Zero extra memory, arch-independent, fires once per layer.

**Why now.** Current failure mode is *coherent-looking garbage generation* — the FusedMoE serves uninitialised memory (auto-memory `gridbook_new_moe_arch_checklist`; commit `9a79963`, Laguna, 93% of params). No detection, no test. DSv4 is an unwired TODO at `plugin.py:117-118`, Laguna's DFlash drafter at `:94-95`. Every new CB-lane arch pays a debug session. **Lens 4 adds:** this is the prerequisite to R6 and to any CB-as-default or PyPI hand-off claim — silent-garbage-on-a-new-arch is the one failure mode that makes both unsafe.

**Cost** M. **Risk** low to shipped behaviour (the wrap is idempotent and inert); the assertion could false-positive on an EP/PP-absent expert shard — scope it to params the local rank registered, which `params_dict` already gives. **Verdict-request.** Approve the assertion location; JSON sidecar (third-party extensible) vs in-code tuple (simpler).

> **OUTCOME — ACCEPTED and IMPLEMENTED 2026-07-30** (Robert; assertion location approved, **in-code tuple** chosen, JSON sidecar left as the documented upgrade path). Closes **D3**; the D27 version item was folded into the same change (below).
> - **(i) Registry as data.** `_CB_TOPLEVEL_MODULE_PATHS` (`plugins/gridbook/gridbook/plugin.py:91-118`) — five module paths (`hy_v3`, `hy_v3_mtp`, `laguna`, `qwen3_5`, `qwen3_5_mtp`), each fed to `_install_on_module_classes` (`:38-63`); the three hardcoded class imports are gone. DSv4 is now a **commented candidate line naming the module path to check** (`:114-117`), not a bare TODO. A comment invites the JSON sidecar if third parties ever need to extend it.
> - **(ii) The unbypassable assertion.** `gridbook/cb_fill_guard.py` (new, import-light — no torch, no vLLM at module scope). `create_weights` stamps `_pq_cb_filled = False` on `w13/w2_cb_qweight` (`moe.py:147`, `:154`); **both** fill paths stamp `True` (per-layer instance hook `moe.py:199`; `moe_toplevel_loader.py:583`, at the `loaded.add` site); `process_weights_after_loading` (`moe.py:213-217`) raises, naming the model class (best-effort via `get_current_vllm_config` + vLLM's model registry, all failure-tolerant), the module paths the wrap *did* install on, and the registry line to add. No env bypass. Scoped per the EP/PP caveat: absent or zero-element stacks are skipped; a registered non-empty stack with no sentinel counts as unfilled.
> - **Tests.** `plugins/gridbook/tests/test_cb_fill_guard.py` — 7 tests, torch-only, no vLLM: unwired synthetic top-level arch raises (and the message names `_CB_TOPLEVEL_MODULE_PATHS`), wired passes, per-layer path passes, partial fill raises, EP/PP scoping, untracked-param strictness, plus an AST check that the registry is a literal tuple and no `vllm.model_executor.models.*` class import remains in `plugin.py`.
> - **Not covered by test:** the full `process_weights_after_loading` call requires vLLM (absent from the build venv); the guard is exercised at its contract boundary instead. First CB serve on any arch is the end-to-end check.
> - **Docs updated:** `ARCHITECTURE.md` §8.5 L3 (FIXED + mechanism), §9.2 per-arch wiring, §8.3 Tier D (7)/(8), §8.4 matrix, DIAGRAM-3 GBPLUG node, §12 D3/D27; `plugins/gridbook/README.md` per-arch section + version block.

### R22 / R27 / R26 (L3 P5, P4, P7) — authoring and the pattern fix

**R22 — `minimax_m2` spec + `deepseek_v4` default: pure authoring, nothing blocking.** `minimax_m2.py`'s 8 overrides (`:69,:86,:91,:101,:104,:110,:133,:137`) are all expressible in `prismaquant.model_structure.v1` today — `fused_sibling_group` → `fused_groups`; `packed_expert_param_names`/`per_expert_moe_regex` → `packed_experts` + `moe`; the three name maps → `naming.{live_to_recipe,recipe_to_source,recipe_to_vllm}` regex rules, which `base.py:314-318` gives precedence over the vLLM mapper and `lfm2_moe.json` already exercises. `deepseek_v4.json` needs one key; the only judgement is *which value* — its lane is undecided (vLLM 0.20 native vs CB) and its current fallback `research` carries **no format allow-list**, so `vllm_packed_moe` is the conservative, provably-tighter choice and a prerequisite for R11's default to mean anything. **Cost** M+S. **Risk** minimax is unshipped since M2.7; the check is that spec-derived accessors return values identical to the deleted Python on the M2.7 checkpoint before the Python goes. **Verdict-request.** DSv4's serving profile value.

**Outcome (R22): ACCEPTED, implemented 2026-07-30** — `specs/minimax_m2.json` expresses all
eight overrides: `fused_groups` (qkv only), `packed_experts` (`param_names` = the unfused vLLM
leafs, `module_class_names`, `projection_splits` for the gate_up/down role buckets,
`format_groups` for the FusedMoE coupling), `moe.per_expert_regex`, and three
`naming.recipe_to_vllm` regex rules for the `block_sparse_moe.experts.N.w{1,2,3}` →
`mlp.experts.N.{gate,down,up}_proj` rename; `has_mtp`/`per_expert_mtp_regex`/`source_tensor_name`/
`live_to_recipe_name` are the spec defaults. `tests/test_minimax_m2_spec.py` compares a bare
`SpecMatchProfile` against the pre-spec Python behaviour accessor by accessor; the Python stays
until that has held for a release. **It closed a latent unservable-artifact bug**: with no spec,
`packed_expert_format_group` fell through to the legacy fallback whose first group is
`(gate_up_proj, down_proj)`, so MiniMax's `down_proj` got a *different* coupling key than its
`gate_proj`/`up_proj` — one expert bank in two format groups. DSv4's value: **`vllm_packed_moe`**
(the approved conservative choice), also now declared in `deepseek_v4.json`.

**Outcome (R27): ACCEPTED, implemented 2026-07-30** — `streaming_model.py`'s arch half became
`staging.bypass_hf_fp8_module_rewrite` behind `profile.bypass_hf_fp8_module_rewrite()`, with the
per-checkpoint FP8/block-scale half left as a config read; `incremental_probe.py`'s
`MiniMaxM2Experts` literal became `profile.packed_expert_module_class_names()` — the declared
class stays **required** rather than being replaced by a pure structural test, because the
replacement forward implements one specific expert-loop signature and applying it to a lookalike
container would silently change a forward pass. `unpacked_expert_projection_names` is declared
for the first time — which required adding it as a real `ModelStructureSpec` field: `base.py`
had been reading it off the spec with `getattr`, so no declaration could ever have taken effect.
Core-stack architecture literals in control flow: **3 → 0**.

**R27 — the two accessor moves.** `streaming_model.py:98-104` is **already half config-derived** (`qc["quant_method"] == "fp8"` and `"weight_block_size" in qc`); only the arch half is hardcoded. That is a static architecture property, not forward-pass behaviour, so it belongs in the **spec** as a boolean (`staging.bypass_hf_fp8_module_rewrite`, default false) behind a base accessor — not a Python predicate. `incremental_probe.py:113-122` (`type(module).__name__ == "MiniMaxM2Experts"`) → `packed_expert_module_class_names()` (`base.py:182-192`); its sibling `unpacked_expert_projection_names` (`base.py:470-495`) is the field no spec declares. **Cost** S each, **risk** ~zero. Fold in once R22 gives minimax a spec to declare them in.

**R26 — the pattern fix.** One conformance test: enumerate `ModelStructureSpec`'s fields and every key present in any `specs/*.json`, assert each is read by at least one `base.py` accessor or named consumer (explicit allow-list for descriptive fields like `_verified_source_layout`). Same for `serving_profile_specs/`. Four of five findings in this lens are one bug class, and `tests/test_model_profile_conformance.py` (new, 2026-07-30) is the natural home; its ratchet idiom already encodes known gaps as red-when-closed. **Cost** S, **risk** none. Highest value-per-line in this lens; fold into whichever of R8/R10/R11/R12 lands first.

> **OUTCOME — ACCEPTED and IMPLEMENTED 2026-07-30** (Robert). `tests/test_model_profile_conformance.py`, +194 cases in the default CPU lane, split in two because a declaration can rot at either end: **(a) key-is-parsed** — every key in `model_profiles/specs/*.json` (66) and `serving_profile_specs/*.json` (46), top level plus one level into each destructured container and into list-element dicts, must be named as a string literal by its parser; **(b) field-has-a-reader** — every `ModelStructureSpec` dataclass field (29) must be read outside `structure.py`, every serving-spec dataclass field (49, auto-discovered via `from_dict`) inside `serving_profiles.py` or a module importing it. A third test makes the parametrization non-vacuous so a moved spec dir cannot collapse it to green.
> - **Allow-list is a convention, not a name list.** Both spec families already used a leading-underscore prose convention across several authors (`_verified_source_layout`, `_default_serving_profile_rationale`, `_naming_rationale`, …), so `DOC_ONLY_KEY_PREFIX` gates it — three such keys landed *during* the work — with the named inventory kept as documentation and `test_doc_only_allowlists_are_live` failing when an entry outlives its key. The reverse direction is enforced too: a parser may never name a `_` key, or prose becomes load-bearing.
> - **One real unread field, ratcheted:** `RuntimePackageSpec.module` — `vllm_packed_moe.json` declares `module: "flashinfer"` but the sole consumer reads `version`/`pip_packages`/`env` and then hardcodes `import flashinfer`, so the declared import name drives nothing. Assert-then-xfail, so wiring it turns the test red. `ModelStructureSpec.match` was a second finding mid-session and was closed by the concurrent declarative-detection work before the test landed; its history is kept in the comment.

---

## Lens 4 — serving lanes & the Strix Halo horizon

**No fundamental change to the *native* lane's default status is warranted — but for a reason the accuracy A/B does not state.** CB wins every matched-byte pair measured (§9.2), yet the CB lane is hard-gated out of the platform's own cost/selection machinery (`run-pipeline.sh:119-131`: `COST_MODE=local`, `PRODUCTION_CACHE=0`), has no in-lane serving smoke (`:1649-1654`), no validated-frontier selection, no TP, and a hand-maintained per-arch loader list whose failure mode is coherent garbage (`gb/plugin.py:66-118`). The blocker to CB-as-default is **not** quality evidence; it is that CB is a second-class citizen of the pipeline. *(The CB integration decision itself is folded into R3, jointly with Lens 2.)*

### R6 (L4 P1) — Lane eligibility becomes model configuration, not an operator env var · FUNDAMENTAL

**What.** Add `supported_lanes` / `preferred_lane` to the model-profile spec (mirrored in `serving_profile_specs/*.json`), plus a preflight refusing an `EXPORT_CONTAINER` the arch has not declared; `run-pipeline.sh` defaults to the arch's preferred lane, not hardcoded `compressed-tensors`. **Why now.** Lane choice is already a per-arch fact, currently tribal knowledge in `scripts/run_*_prod_nvfp4cb.sh`. Nothing stops `EXPORT_CONTAINER=nvfp4_cb` on DSv4, whose CB expert loader is an explicit TODO (`gb/plugin.py:117-118`) — the run completes and serves garbage (precedent `9a79963`: Laguna, 93% of params). The honest CB-eligible set is four archs and nothing in the tree says so. Same declarative move §8 already made for structure and serving constraints; lanes are the one axis still imperative. **Cost** M. **Risk** low — undeclared archs keep `compressed-tensors`, strictly additive. **Verdict-request.** Is lane eligibility a profile property, or do lanes stay operator-chosen with a warning only? (R10 is a prerequisite either way.)

**Outcome: ACCEPTED — spec side implemented 2026-07-30 (wave 2); pipeline preflight wired 2026-07-30 (wave 3).**
`supported_lanes` / `preferred_lane` are `ModelStructureSpec` fields read by
`ModelProfile.supported_export_lanes()` / `preferred_export_lane()`, defaulting to
`["compressed-tensors"]` / `"compressed-tensors"` when absent — strictly additive, so no run
that is legal today became illegal (a test enumerates every in-tree launch script's
`(arch, EXPORT_CONTAINER)` pair). The honest CB set is declared as exactly the four wired
archs — `qwen3_5`, `qwen3_5_dense`, `hy_v3`, `laguna` — with `hy_v3` also GGUF and preferring
it, and `laguna` preferring `nvfp4_cb`; a conformance test pins that declaration against the
gridbook loader list so over-declaring fails. `require_lane_supported(profile,
export_container)` (`serving_profiles.py`) is the preflight and refuses with the declared set
and the failure mode named. **`run-pipeline.sh` is NOT wired** — that was wave 3.

**Pipeline preflight landed 2026-07-30 (wave 3).** `run-pipeline.sh` runs one preflight block
before the lane gates: `detect_profile(MODEL_PATH)` -> `require_lane_supported(profile,
EXPORT_CONTAINER)` -> `resolve_target_profile(...)`, refusing an undeclared lane with `exit 2`
naming the declared set and the failure mode. The same block resolves the serving profile once
(`TARGET_PROFILE_RESOLVED`), which the GGUF/CB consistency gates, the pipeline spec and the
allocator now all read — so lane eligibility and profile resolution can no longer disagree.

### R7 (L4 P3) — Strix Halo: enter as a *serving-only* target, GGUF first · FUNDAMENTAL

Quantization stays on the Spark in all three options; Strix is a serving box. That decoupling is load-bearing — the probe/cost/render/export stack (all CUDA, `gpu_guard.require_cuda_hot_path`) then needs **zero** ROCm work.

| Entry | Buys | Blocks / costs | First milestone |
|---|---|---|---|
| **A. GGUF-first (recommended, S)** | A real Strix readout with zero new code: `rdtand/Hy3-…-2.8bit-gguf-vllm` exists and llama.cpp serves ROCm/gfx1151. | Nothing. Caps at the IQ prefill tax (42 tok/s on Spark) — the number we would be measuring. | Serve the shipped 2.8 bpp Hy3 GGUF on Strix; record prefill/decode/TEB. No port, no build. |
| **B. HIP/CK port of CB kernels (L+)** | The lane's thesis (tensor-core CB removes the IQ dequant tax) on the new box. | `gb/csrc/cutlass_fork/*` is CUTLASS + sm_120 (R6 smem LUT, TileM feasibility, TMA). A rewrite, not a port — and it doubles the kernel surface we maintain forever. | Port **one** kernel — dense decode GEMV (`gb/linear.py:46-53`) — and bit-exactness-check it against CUDA on the same artifact. If that one is uneconomic, B is uneconomic. |
| **C. Wait for upstream vLLM-ROCm (S, passive)** | The zero-maintained-code promise, on Strix, free. | Depends on upstream W4A4/W8A8 ROCm maturity; unknowable schedule; the weakest accuracy lane per §9.2. | A dated tracking note: which vLLM version first routes NVFP4/FP8 to a performant gfx1151 kernel. |

**Ordering.** **A now; C in parallel as passive tracking; B only if A's measured Strix prefill tax is both large and load-bearing for a real workload.** A also answers what nothing else does: does the box hold a 100 GB-class artifact at all? **Cost** A: S. **Risk** none. **Verdict-request.** Confirm serving-only framing (no ROCm build stack in scope) and approve A.

> **OUTCOME — ACCEPTED 2026-07-30 (wave 4). Serving-only framing confirmed; option A approved; no ROCm build stack in scope.**
> Quantization stays on the Spark in every option, so the probe/cost/render/export stack needs **zero** ROCm work — that decoupling is the ruling's load-bearing half. **Milestone (A):** serve the shipped Hy3 2.8 bpp GGUF on Strix and record prefill / decode / ToolEvalBench, *when the box exists* — nothing here is blocked on code, so the item's state is "blocked: no box", not "in progress". **C is tracked passively** as a dated row (which vLLM version first routes NVFP4/FP8 to a *performant* gfx1151 kernel — registry support is not enough, per core principle 9); nothing observed yet. **B is not funded** and its entry test is unchanged: one kernel (dense decode GEMV) bit-exactness-checked against CUDA, and if that one is uneconomic B is uneconomic.
> Recorded as a dated table in `docs/lanes/gguf.md` ("Hardware targets — Strix Halo tracking"), which is where the readout will land. §10's "Strix Halo — planned, nothing designed or built" stays accurate until A produces a measurement. **Still blocked on hardware per Robert 2026-07-30** — no box, so nothing to measure; the item stays "blocked: no box", not "in progress".

> **⚠ SUPERSEDED IN PART — 2026-07-30, later the same day.** Robert directly commissioned the
> thing this ruling declined: *"build fp8 vllm kernels that target strix halo and support codebook
> formats"*, and a gfx1151 box arrived. **Option B is now funded and its entry test is PASSED.**
> The test as written above was "port one kernel — dense decode GEMV — and bit-exactness-check it";
> what landed is broader (decode GEMV for fp8-CB *and* NVFP4_CB two-tier, plus a bf16 WMMA
> prefill GEMM, the expander and the fused activation QDQ, in
> `plugins/gridbook/gridbook/csrc_hip/`), and it is checked **not** against CUDA but against an
> fp64 torch reference at a 1-bf16-ULP gate — 34 pytest + 44 standalone cases green on the device.
> Bit-exactness against CUDA is not an available gate and the entry test should be read as amended:
> this device's WMMA unit is measurably not exactly-rounded (~0.5 f32 ULP), so a relative gate is
> the tightest defensible one. B's stated cost — "it doubles the kernel surface we maintain
> forever" — **stands and is now real**; nothing here retires it.
>
> **What did NOT change.** (a) Quantization stays on the Spark: probe/cost/render/export are CUDA
> and need zero ROCm work — the ruling's load-bearing half survives intact. (b) The concern that
> `gb/csrc/cutlass_fork/*` is "a rewrite, not a port" was **correct**: none of the CUTLASS/sm_120
> code was ported; the HIP kernels are independently derived against the format spec, sharing only
> `LAYOUT.md`. (c) A is still unrun — no GGUF serve on Strix, no vLLM-ROCm serve of anything, so
> there is **no serving metric on this hardware yet** and §10's readout must not be read as one.
> (d) C stays passive tracking. **New state:** B = "kernels authored, parity-verified and
> benchmarked standalone; unserved". `docs/lanes/gguf.md`'s tracking table still owns A.

### R21 (L4 P4) — Persist the auto-tuner's timings; defer λ · STRUCTURAL

**What.** Give `gb/moe_autotune.py`'s per-candidate timings a durable sink instead of a print (`moe.py:1400-1405` is `print()`; the `log=` callback at `moe_autotune.py:111,138-150` has no persistent consumer). Emit a per-(format, shape-regime, box) ms table. Do **not** add λ yet. **Why not λ.** §9.2 confirms it is unimplemented, and `Candidate` (`allocator_solver.py:40-46`) carries `fmt / bits_per_param / memory_bytes / predicted_dloss` — no time term anywhere. Implementing λ today optimizes against a cost table that does not exist, on a lane that is not default, against a tax the kernel campaign moved 12×→1.75× in one week: a heuristic in an objective's clothes. **Strix is not the forcing function** *unless* R7-B is funded — under R7-A the only Strix lane is GGUF, one compute path per rung, nothing to trade. The real forcing function is two measured serving-cost tables that **disagree in ranking**; persisting the tuner output is what makes that observable, costs an afternoon, and pays for itself as kernel-campaign regression data regardless. **Cost** S, **risk** none. **Verdict-request.** Agree λ stays specified-not-implemented until two boxes' tables disagree?

> **OUTCOME — ACCEPTED and IMPLEMENTED 2026-07-30 (wave 4). Timings persisted; λ stays specified-not-implemented.**
> `moe_autotune.py` gained a durable append-only JSONL sink: one row per tuned layer carrying the three keys that make a serving-cost table comparable — **(format, shape regime, box)** — plus `{candidate: ms}`, the chosen path and whether it was forced. Box identity is the CUDA device name (a ms number without it cannot answer "do two boxes disagree"); the token count is bucketed by power of two because the crossover this table exists to locate is a property of the M tile, while expert count and intermediate size are recorded exactly because the round-2 taxes scale with expert *size*. Path is `PRISMAQUANT_CB_AUTOTUNE_LOG`, else `<PRISMAQUANT_CB_ARTIFACT_DIR>/cb_autotune_timings.jsonl`, else off; an unwritable path degrades to a stderr warning and **never fails a serve**, like every other gate on that path. The stderr line is unchanged, and the `log=` callback stays compatible with its pre-sink three-positional-argument form.
> **The λ ruling is recorded in code, not only in prose:** a test asserts `allocator_solver.Candidate` carries no time field, so "persist the timings" cannot be mistaken for "the allocator now optimizes latency". The forcing function is unchanged — **two measured tables that disagree in RANKING** — and this sink is what would make that observable. Tests: `plugins/gridbook/tests/test_moe_autotune_sink.py` (torch-only, no vLLM, no GPU).

### R16 (L4 P5) — One lane-uniform ship gate: `LaneSpec` · STRUCTURAL

**What.** Declare per lane `{serve command, endpoint, gate set, KL evaluator}` and run the same numeric ship gate on all three. Two facts make this far cheaper than §9.3 implies: `validate_quantized_model.py` is **already runtime-agnostic** — it drives an OpenAI-compatible endpoint over HTTP (`--base-url` `:507`, `--model-name` `:511`, health `:163`, `/metrics` `:171-189`) and knows nothing about compressed-tensors; and `grep 'gguf\|nvfp4_cb'` across all `validate_*.py` returns **zero hits**. The asymmetry is wiring, not capability. CB serves through vLLM+plugin (same endpoint, gate unchanged); GGUF through llama-server or `vllm-gguf-plugin`. For selection, GGUF's missing frontier evaluator is `llama-perplexity --kl-divergence-base` (already the lane's harness, `docs/lanes/gguf.md:80-84`) behind the `validate_assignments_kl` interface. **Why now.** §9.3's "no validated-frontier evaluator" and §9.2's "no in-lane smoke" are one gap seen from two sides with a shared fix; it also closes D26. Worth stating plainly: per §7.1 **nothing in the pipeline blocks on a quality number on any lane, native included** — so this is less "raise CB/GGUF to native's bar" than "define the bar once". **Cost** M for the CB half (all four `scripts/serve_*.sh` already exist); M–L for the GGUF KL adapter. **Risk** low, additive. **Verdict-request.** Approve `LaneSpec` as the shape, and decide whether gates stay advisory or become blocking — the latter changes every lane's run. *(Interacts with R13: the shipcard is the record, `LaneSpec` is the runner.)*

> **OUTCOME — ACCEPTED and IMPLEMENTED 2026-07-30 (wave 4). Shape approved; gates stay ADVISORY — the blocking question is DEFERRED to Robert.**
> `prismaquant/lane_spec.py` + `prismaquant/lane_specs/{compressed_tensors,nvfp4_cb,gguf}.json` declare, per lane, the four things the proposal named: **serve command / scripts, endpoint, gate set, KL evaluator** — same idiom as `serving_profile_specs/` (JSON + `from_dict`), so a fourth lane is a data file, not a branch. Each gate names the `shipcard.json` slot its record closes, which is the R13 interlock: the shipcard refuses, `LaneSpec` describes what it will refuse on. A test pins every declared slot against `shipcard.REQUIRED_SLOTS` and every declared serve script against the filesystem, so the declaration cannot rot into fiction — the exact failure mode §8.5 catalogued five times.
> **The CB half was wiring, as predicted**, and one assertion states it: native and CB declare the *same* ship-gate runner on the *same* endpoint kind.
> **The GGUF KL evaluator is implemented** (`prismaquant/gguf_kl_evaluator.py`): `llama-perplexity --kl-divergence-base` behind the `validate_assignments_kl` interface, returning `(mean, per_sequence, stats)` with the **gold lane's key names** (`kl_mean`/`kl_p99`/`kl_max`/`nll_mean`, the latter = `ln(PPL)` by definition, free). Two things are recorded rather than papered over: `per_sequence` is **empty** and `kl_tail_domain="aggregate"`, because llama-perplexity reports token-domain quantiles, not per-sequence values — so an R9 tail-veto reading `kl_p99` on this lane reads a different domain than on the resident one. Parsing is a pure function pinned against **canned** harness output in both spellings llama.cpp has shipped (`Mean    KLD:` and `Mean    KL divergence         :`, `±` and `+/-`); **no server, no GPU, no binary was run** — the live path is integration and is marked as such.
> **Deferred, explicitly:** whether gates become blocking. `LaneSpec.advisory_gates` is `True` on every lane and a test pins it, so a flip is a deliberate edit. Nothing in `run-pipeline.sh` was made to block, and the GGUF frontier loop was **not** wired to the new evaluator — the evaluator exists; using it for selection is the next decision, not this one.
>
> **ADDENDUM — Robert delegated; ruling 2026-07-30: publish-blocking, pipeline advisory.** The deferred question is answered without touching a single lane's run: pipeline gates **stay advisory** (the build/serve boundary is physical — `vllm` is not importable in the build venv), and the **blocking point is publication**. `tools/publish_artifact.py <artifact_dir> --repo-id rdtand/<name>` wraps `huggingface_hub.upload_folder` (imported **lazily**, so the build venv degrades to printing the exact `hf upload ...` line rather than failing), and before it uploads anything — or prints anything copy-pasteable — it runs `prismaquant.shipcard.verify` as a **library call** and REFUSES on failure, listing every unfilled or failing slot. `--force-unverified` requires the artifact directory's basename **re-typed** (interactive prompt, or `--confirm-name` for scripts) and stamps `forced_unverified: true` plus the overridden problems into the shipcard, so an ungated ship is visible on the artifact forever. Tests: `tests/test_publish_artifact.py` (10) — verified pass, unfilled/failed/spec-tainted/missing-card refusals with the assertion that **no upload command is printed on a refusal**, the wrong-basename usage refusal, and the forced path's stamp. Docs: ARCHITECTURE §7.1 (new row in the what-runs-where table + the ruling), CLAUDE.md §5 gates.

**L4 hygiene (→ R28).** **D27 is CLOSED** (2026-07-30, folded into R10's landing): the skew was real but benign — releases are cut from the standalone `/home/rob/gridbook` repo (PyPI 0.1.1), the in-tree copy is the *development head* and is ahead of the release in kernel work, and it now says so (`__version__ = "0.2.0.dev0"`, plus a version block in `plugins/gridbook/README.md` naming the release source). **D15** (`CB_SCALE_CODING=v1`, `CB_EXPERT_EMPIRICAL=1` defaults contradict every shipped driver) and **D27** (version skew) are cheap and load-bearing for any "CB is the product" claim; **D14** (gridbook README materially wrong) is the lane's most-read file. **D21:** resolve the two Hy3 CB repo ids against the Hub before either is cited again. Fold the R7-A Strix readout into `docs/lanes/gguf.md` as a hardware row; §10's "Strix Halo — planned, nothing designed or built" stays accurate until then.

---

## Lens 5 — validation & ship gates

**(1) "Nothing blocks on a quality number" is a feature, not a gap — with one real gap.** It is a *physical lane boundary*, not laziness: `vllm` is not importable in the build venv (`/home/rob/dq-runs/venvs/prismaquant-cu130/bin/python -c "import vllm"` → `ModuleNotFoundError`), and `validate_native_export.py:160` / `validate_quantized_model.py` both need a live vLLM (the latter an already-running `--base-url` serve, `:507`). Embedding a docker serve inside `run-pipeline.sh` would make the build tool own the serving stack — worse than the status quo. What is missing is not CI; it is a **ship record**. **(2) D16 is misfiled and needs no A/B** (R25). **(3) D1's tail column already exists two layers away and is thrown on the floor** (R9). **No FUNDAMENTAL change is warranted for this lens** — the metric hierarchy, the two-lane split and the thresholds are right.

### R13 (L5 P1) — The ship record (`shipcard.json`): a refusal contract, not pipeline-embedded CI · STRUCTURAL

**What.** `export_native_compressed` writes `exported/shipcard.json` with the build-lane facts it already has (git commit, `assignment_hash`, `layer_config_sha`, achieved bpp, footprint bytes, format histogram, render-lever provenance from `export_native_compressed.py:6160-6172`) and *empty, required* serve-lane slots: `native_export.eager`, `native_export.graph`, `ship_gate` (the `validate_quantized_model` verdict block), `gold.kl`, `gold.ppl`. The packaged `python -m prismaquant.shipcard_cli verify` command exits non-zero unless every slot is filled by a record whose `model_sha` matches. Each serve-lane tool gains `--shipcard <path>` and appends its own verdict. `run-pipeline.sh`'s closing echo (`:1703-1705`) prints the *remaining unfilled slots* instead of a copy-pasteable command.

**Why now — per-escape coverage, all three.** *Broken-27B mean-pass*: the worst-per-prompt NLL guard exists (`validate_quantized_model.py:20-23,116-120,275-278`) but the stage is never run **and never echoed** (§7.1); the shipcard makes "we didn't run it" a refusal instead of an omission. *Spec-decode draft-NLL trap*: `_spec_decode_on` (`:171-189`) + the refusal (`:292-302`) exist — **but only in `validate_quantized_model`.** `grep -n "spec_decode\|/metrics" tools/measure_vllm_full_kl.py tools/measure_vllm_wikitext_ppl.py` → **0 hits**. The gold lane, the rung-1 authority, has no such guard. Port it and make the shipcard require `spec_decode_detected: false` on the record that produced the PPL/KL number. *In-sample validation*: see R14. It also collapses the two-arm eager/graph smoke from protocol-in-help-text (`validate_native_export.py:139-140`) to two named slots — a `--both-arms` flag fills both in one invocation.

**Cost** M. **Risk** none to shipped behavior; additive, no existing stage's exit code changes. **Verdict-request.** Approve the shipcard as the ship contract, and approve porting the spec-decode refusal into the two gold-lane tools?

> **OUTCOME — ACCEPTED and IMPLEMENTED 2026-07-30** (Robert; both halves). No shipped behaviour changes: every stage's exit code is unchanged except `validate_native_export`, which now returns 1 on a failed arm instead of raising.
> - **The record.** `prismaquant/shipcard.py` (stdlib-only, importable without torch's world) defines the schema, `compute_model_sha` (config sha + per-shard byte sizes — content-hashing a 90 GB artifact would make `verify` unusable, and the failure guarded against is "this record belongs to another build", not tampering), `make_record`/`fill_slot`/`verify`. `export_native_compressed._write_shipcard` (`:8111`) opens it after `mixed_native_manifest.json` with git commit, `assignment_hash`, `layer_config_sha`, exact `artifact_bytes`, format histogram, the render-lever echo and the D24 `PRISMAQUANT_ALLOW_KV_SHARED_FISHER` / `PRISMAQUANT_KV_COTANGENT` state (`unvalidated_kv_fisher_correction` is one boolean an artifact now carries).
> - **Achieved bpp is quoted, not recomputed.** The exporter is handed a recipe, not a bpp, and `footprint.assignment_artifact_bytes` needs probe stats it does not have. It reads the allocator's own number from the sibling `pareto.knees.json` and records *where it came from* (`{"value": 4.7513, "source": "pareto.knees.json:log_error"}`), or `null` — inventing a number under a different accounting convention is the CLAUDE.md-§12 trap.
> - **The render-lever echo is shared, not copied.** `_render_lever_provenance()` is now the single definition; the export cache's `manifest.json` fingerprint calls it (key set byte-identical, so no in-flight export cache is invalidated) and the shipcard echoes the same dict. Two stores of the same fact was the thing to avoid.
> - **Slots and refusal.** `python -m prismaquant.shipcard_cli verify` exits 1 unless all five slots hold a *passing* record whose `model_sha` matches the artifact **on disk** (so re-exporting after the gate ran is caught), and both `gold.*` records report `spec_decode_detected: false`. `show` prints what is missing; `fill --slot gold.kl --record <json>` closes the gold slots and **refuses at fill time** on a tainted or unknown spec-decode state (`--allow-spec-decode` records it anyway; `verify` still refuses).
> - **Spec-decode ported to rung 1.** `tools/spec_decode_guard.py` holds both detections — `/metrics` scrape (the §7.2 form) and live-engine `speculative_config` (the form the gold tools need, since they build their own in-process `LLM`). Both gold tools raise with the draft-NLL diagnostic and stamp `spec_decode_detected` on every result dict; `None` (could not inspect) is refused rather than read as "off".
> - **Two arms in one invocation.** `validate_native_export --both-arms` runs eager then graph, tearing each engine down between (`del` + `gc` + `empty_cache`) so the second load does not profile against the first's pool, and fills `native_export.eager` / `native_export.graph`.
> - **Tests.** `tests/test_shipcard.py` — 15, covering all four refusal modes (unfilled, foreign `model_sha`, artifact edited after the card was opened, failed check), both spec-decode-taint states, the exporter's build block, and the CLI's exit codes. Green.
> - **Not done at the time (closed in 0.5.0):** `run-pipeline.sh:1703-1705`'s closing echo still printed a copy-pasteable command instead of the unfilled slots. It now calls the packaged `python -m prismaquant.shipcard_cli show` command, so the installed wheel has the same refusal surface as a checkout.

### R14 (L5 P2) — Mechanize held-out disjointness by hash, not by flag · STRUCTURAL

**What.** (i) Cost/probe stages stamp `calib_hash` (the existing `perturbed_x_cache.calibration_data_hash`) into their output artifacts — today `grep -n "calib_hash\|cal_hash" production_render_cost.py aura_cost.py incremental_probe.py` → **0 hits**, so disjointness is unverifiable after the fact and rests entirely on `run-pipeline.sh:1201` passing the right integer. (ii) `validate_assignments_kl` reads the cost artifact's hash set and **hard-errors** if its own `calib_repeat_hashes` (`:307-320`) intersect it. (iii) Fix the silent-skip: `_load_calibration_repeats:503-563` honors `--calib-skip-first` **only on the `args.dataset` branch** — on the wikitext path (`args.dataset` empty) `skip` is computed at `:506` and then never used, so a manual `--calib-skip-first 32` on wikitext is a silent no-op. Raise instead.

**Why now.** This is the one escape of the three that needs no serving stack, and the class of bug (in-sample "validation") has already regressed once (§7.5). A hash intersection is an explicit, not a heuristic. **Cost** S. **Risk** the pipeline default (`DATASET=/home/rob/dq-runs/calibration/diverse-v1.jsonl`, `run-pipeline.sh:82`; `VALIDATED_FRONTIER_SKIP_CALIB=$NSAMPLES`, `:260`) already takes the correct branch, so shipped selections are unaffected; the new error only fires on misconfiguration. **Verdict-request.** Approve as a hard error (not a warning)?

**Outcome: ACCEPTED, implemented 2026-07-30** — `calibration_data_hash` is stamped by `incremental_probe` (per shard, unioned into `calib_hashes` at merge), `aura_cost.provenance`, and `build_production_cache` onto `cache.metadata` (inherited by `production_render_cost`, which sees no calibration text of its own); `validate_assignments_kl` hard-errors on any intersection with its own `calib_repeat_hashes` and stays inert on pre-R14 artifacts that stamp nothing, and the `--calib-skip-first` wikitext silent no-op now raises.

### R15 (L5 P4) — Mechanize §7.4: a serve fingerprint the comparator refuses to cross · STRUCTURAL

**What.** §7.4's rule ("identical extension residency, ±20% is not evidence") is prose with no enforcement. Mechanize the enforceable subset: (1) the serve wrapper writes `serve_manifest.json` next to the artifact — full launch argv, image tag, `vllm`/`torch`/driver versions, GPU name, `enforce_eager`, `--quantization`, and the resident-extension list read from the **server's** `/proc/<pid>/maps` (basenames of `.so` matching `gridbook|prismaquant|flashinfer|causal_conv1d|fla`). This must be server-side — the measuring client cannot see the server's address space, which is exactly why the ±17% drift was invisible for so long. (2) `serve_fingerprint = sha256(canonical_json(manifest_minus_argv_paths))`, stamped into every gold-lane result dict (`measure_vllm_full_kl.py:388-406`, `:430-446`; `measure_vllm_wikitext_ppl.py`) alongside a `git_commit` (which those tools do not record today — `grep -n "git\|commit"` → 0 hits, so gold-lane numbers are currently *less* provenanced than the surrogate KL JSONs at `validate_assignments_kl.py:280-305`). (3) `tools/kl_ab.py A.json B.json` refuses when fingerprints differ; `--allow-cross-fingerprint` downgrades the output from a delta to a **range**, printing the §7.4 ±20% band and the differing manifest keys, so an unavoidable cross-stack comparison is still quoted honestly.

**Why now.** The mechanism is understood and dated (2026-07-19, conf-KL 0.01134 vs 0.01328 on the same 27B keyed only on `.so` residency). The next CB-vs-native A/B is exactly the shape that trips it. **Cost** M. **Risk** none — refusal only fires on new-format JSONs; legacy JSONs lacking a fingerprint compare as today with a printed warning. **Verdict-request.** Is `/proc/<pid>/maps` acceptable as the residency proxy, or should the fingerprint also include a one-shot kernel-selection probe (a fixed GEMM whose chosen cuBLAS algo id is recorded)?

> **OUTCOME — ACCEPTED and IMPLEMENTED 2026-07-30** (Robert; `/proc/<pid>/maps` accepted as the residency proxy — the GEMM-probe extension is **not** built, and is left as the upgrade path if a fingerprint match is ever observed alongside a numeric split).
> - **Server-side manifest.** `tools/serve_fingerprint.py write` runs *inside* the container (`docker exec`, via `write_serve_manifest` in the new `scripts/lib/serve_manifest.sh`, called from all four `scripts/serve_*.sh` after READY). It unions `/proc/<pid>/maps` over the vLLM **API-server and EngineCore** processes — the engine is the one holding the kernels, and a front-end-only read would have missed the mechanism entirely. Stdlib only by construction: versions come from `importlib.metadata` and the GPU from NVML via `nvidia-smi`, so the writer never imports torch and never creates a CUDA context on a box whose OOM discipline is measured in single-digit GiB of slack. Failure prints a warning and never tears the serve down.
> - **Fingerprint = sha256(canonical JSON minus argv paths).** Eliding path-like argv tokens is what makes an A/B possible at all: two arms name different artifact directories and must share a fingerprint, while image, resident extensions, `enforce_eager`, `--quantization`, package versions, GPU and every non-path launch flag must move it. Pinned by test, in both directions.
> - **Gold-lane stamping.** Both measure tools build their own in-process `LLM`, so they fingerprint *themselves* from `/proc/self/maps` (`self_manifest`) — the measuring process is the server in that lane. Every result dict gained `serve_fingerprint`, the embedded `serve_manifest`, and `git_commit` (previously absent: gold numbers were less provenanced than the surrogate KL JSONs).
> - **`tools/kl_ab.py`.** Same fingerprint → delta. Different → **exit 3, no delta printed at all** (a refusal that still quotes the number is not a refusal) plus the differing manifest keys; `--allow-cross-fingerprint` prints a range with the ±20% band and states whether the difference clears it. Legacy fingerprint-less JSONs compare with a warning.
> - **Tests.** `tests/test_kl_ab.py` — 19, including the dated 27B case (0.01134 vs 0.01328 differing only in `resident_extensions` → refused, then quoted as INSIDE the band and NOT EVIDENCE) and a parametrized sweep over the seven stack keys that must move the fingerprint. Green.
> - **Not run:** no serve script was executed (a production serve owns the box); the shell changes are `bash -n`-checked and the manifest writer is exercised in-process against `/proc/self/maps`. First CB serve is the end-to-end check.

### R25 (L5 P5) — D16: block-output-match is dead code on the shipping recipe. Delete it; do not A/B it. · HYGIENE

**What.** Retire `PRISMAQUANT_BLOCK_OUTPUT_MATCH` (default `"1"`, `export_native_compressed.py:6169,6321,6467`) and `block_output_match.py` to a dated archive wall, and close D16 as *unreachable*, not *unmeasured*.

**Why now — three independent findings, none of which a gold-lane A/B would surface.** (1) **It never runs.** In the per-Linear emit loop the production-cache pack fires **first** and `continue`s (`:6426-6440`), the batched-NVFP4 path fires **second** (`:6448-6455`), and the block-match branch is **third** (`:6465-6486`). With `PRODUCTION_CACHE=1` — the shipping default — every dense NVFP4 Linear is emitted from the cache and never reaches the branch. Empirical confirmation on two real production exports: `grep -c "block-output-match" /home/rob/dq-runs/ornith-35b-aura-20260703/logs/export.log` → **0**, same on `prod-27b-nvfp4cb-5p5/logs/export.log`; the ornith histogram reads `NVFP4_PRODUCTION_CACHE`. (2) **If it did run, it would re-introduce M19.** `_finalize_compute_only:5270-5299` calls `quantize_dequantize_nvfp4(w, group_size=16, global_real_override=...)` **outside** any `_temporary_export_nvfp4_scale_rule` context — unlike the cache path (`_pack_production_cached_2d:2206-2212`) and the joint-global pre-pass (`:6280-6285`), both of which wrap in `_export_match_render_scale_rule`. So the block-match path would re-derive per-group scales under the default env rule and discard the render's `joint_mse` scales — precisely the −6.6% KL defect M19 fixed everywhere else. (3) **Its mechanism is subsumed and its failure mode is silent.** It multiplies the whole dequantized weight by a scalar from `{0.95, 1.0, 1.05}` (`:6598`, `:6669`) — a per-tensor gain re-search run *after* JSO already solved for the scale by activation-weighted MSE inside the GPTQ loop, the same argument that retired the clip solvers and `scale_sweep`. The entire block is wrapped in `except Exception → print WARN` (`:6695-6697`), so any failure is an invisible no-op. Its docstring's "~0.05-0.10 PPL gain" (`block_output_match.py:33`) is a pre-JSO *expectation*, never a measurement.

**Cost** S. **Risk** none on the shipping recipe; a nonzero-but-known behaviour change only for an export with `PRODUCTION_CACHE=0` **and** `PRISMAQUANT_BATCHED_NVFP4_EXPORT=0`, which no shipped artifact used. **Verdict-request.** Archive-wall it outright, or flip the default to `0` and leave the code one cycle? (Lens recommends the wall — leaving a path that silently bypasses M19 is the band-aid.)

**L5 hygiene (→ R28).** *(The `kl_mean` rename shipped with R9 on 2026-07-30: `kl_mean` is canonical, `last_token_kl` is emitted as a deprecated alias for one cycle, and `select_validated_frontier._row_metric` resolves either.)* `_kl_repeat_summary` emits the mean under the key `last_token_kl` even when the pipeline ran `--kl-scope full_sequence` (`:653`, acknowledged in the docstring) — rename to `kl_mean` with the old key aliased one cycle; the current name has already misled at least one doc. `validation_harness.validate_artifact:77-153` raises only on non-finite metrics (`:156`) and otherwise records everything — that is correct, it is a *measurement* registry, not a gate; keep it that way and add one line to §7.1 so the next reader stops reading "no thresholds at all" as a defect. Add the `PRISMAQUANT_ALLOW_KV_SHARED_FISHER` / D24 caveat to the shipcard schema as a build-lane flag echo, so an allocation that rode an unvalidated correction is visible on the record rather than only in a probe log.


> **OUTCOME — ACCEPTED and IMPLEMENTED 2026-07-30** (wave 2). Archive-walled outright, per the lens recommendation; the "flip the default to `0` for one cycle" option was declined because a path that silently bypasses M19 is the band-aid.
> - `block_output_match.py` + `tests/test_block_output_match.py` → `archive/block_output_match_2026-07-30/`, with `export_branches.py` holding the three excised export sites and `_finalize_compute_only` verbatim.
> - **The refusal is loud, not silent.** `export_native_compressed.main()` now calls `_refuse_archived_block_output_match()`, a `SystemExit` when `PRISMAQUANT_BLOCK_OUTPUT_MATCH` is set truthy — an old launcher fails with the reason rather than exporting differently in silence. `=0` and unset both pass (they already asked for what now always happens).
> - `_quantize_2d(..., compute_only=True)` is **kept**: it is a no-pack introspection hook the export lever-threading tests use. Its production consumer is gone and the call site says so.
> - `_render_lever_provenance()` records the constant `"archived_2026-07-30"` rather than echoing the dead env var. **This moves the export-cache fingerprint once** — deliberate, and safe: no production export was in flight.
> - Docs: `runtime_flags.md` row rewritten as archived; `docs/README.md`'s "default-ON with no table row" drift note removed; **D16 closed as *unreachable*, not unmeasured** (§12), with the lesson recorded — *before funding a measurement, check the code under test executes on the recipe you ship*.
> - Suite: 172 export tests green.

---

## Lens 6 — the de-scoper

**~15k lines (21% of `prismaquant/`) implement two cost-cascade levels and one contract layer the shipping pipeline never executes.** Nothing here should go because it is *wrong* — only because it is *unexecuted*. The system is not over-built; it is under-pruned. Every DELETE means "dated `archive/` wall + banner", never `rm` — archived levers stay replay-reachable.

### R4 (L6 P1) — Retire L2/L3 from the spine; archive-wall the L3 probe · FUNDAMENTAL

**What.** Stop describing PrismaQuant as a three-level cascade. Wall `kl_sensitivity_probe.py` (3,678 L), `propagated_sensitivity_costs.py` (293 L), `sensitivity_response.py` (416 L), the `--propagated-sensitivity-*` surface (`allocator.py:1325-1337`, `run-pipeline.sh:218-222,1065-1072`) and the L3 half of `kl_measurement.py` (`select_l3_neighborhood`, `build_global_l3_neighborhood`, `solve_frozen_l3_neighborhood`, `_l3_prequant_*` — ~15 of 164 functions). Rewrite CLAUDE.md §3 / ARCHITECTURE §2.2, §4.4 as one faithful cost + real-KL select.

**Why now.** L2 is already not a cost stage — no `COST_MODE` runs the fixed point (accepted set `run-pipeline.sh:314-385`); `perturbed_x_cache.py` survives only as an activation/model-loading utility. L3 is opt-in and OFF: `ALLOC_PROPAGATED_SENSITIVITY_REPORT:=` empty at `:218`, and `kl_sensitivity_probe` has **zero** references in `run-pipeline.sh`. The retiring measurements already exist: L2 cascade beats L1 by −1.5% while AURA beats it −38.5% (`aura_cascade_headtohead`); cross-layer residual +5–12% diffuse, 3/1180 pairs significant (`xlayer_sensitivity_2026_06_09`); L3-polish-DP is in the graveyard for non-additivity (§11), and its consumer is *already* archived (`archive/polish_2026-05-15/`; D19 walled 10 dead launcher targets). The spine's top-billed structure has no executor, no consumer and a null result — yet every new reader of CLAUDE.md §3 learns it first. *(Lens 1 independently corroborates: "the collapse of L1/L2/L3 to one level … sound and code-backed".)*

**Cost** M — the wall is mechanical; splitting `kl_measurement.py` is the work (`validate_assignments_kl`, `validation_harness`, `mse_promotion` import it). **Risk** near-zero on default and validated-surrogate paths (neither touches L3). Real risks: an unnoticed import edge (mitigated by the 123-file/36.6k-line suite) and losing cheap reachability for a future cross-layer question (mitigated by the banner README).

**Verdict-request.** Let docs+code say the cascade framing is retired? Or is L3 deliberately held for the cross-layer redesign you called "could radically change all quantization"? If held, the honest fix is to **relabel it research**, not to keep calling it level 3 of the spine.


> **OUTCOME — ACCEPTED and IMPLEMENTED 2026-07-30** (wave 2). Robert's ruling on the verdict-request: **retire the framing**, do not hold L3 as "level 3 of the spine". The cross-layer redesign remains a live ambition; the wall's banner is where it must start, and it opens with the three measurements that closed the question.
> - **Walled** → `archive/l3_propagated_2026-07-30/`: `kl_sensitivity_probe.py` (3,678 L), `propagated_sensitivity_costs.py`, `sensitivity_response.py`, three L3 tools, four L3 test files, and `examples/launchers/proto-production-prismascout.sh` (the launcher wall's "pending a per-stage decision" item — this is that decision).
> - **`kl_measurement.py` split: 5,731 → 1,206 lines.** The split was computed by AST reachability from the live entrypoints, not by eye: 97 top-level symbols reachable *only* from the L3 roots moved to `kl_measurement_l3.py`; the 22 shared with `measure_assignment_kl` stayed and are re-imported by the archived module, which **was verified importable against the live tree**. The per-sequence tail machinery wave 1 added (`sequence_token_nll`, `summarize_per_sequence_kl`, `return_per_sequence`) is live and untouched. `QuantWeightCache` proved L3-only and went to the wall.
> - **Allocator + orchestrator**: the five `--propagated-sensitivity-*` arguments, the cost-folding body and the two `propagated_sensitivity_costs` metadata keys removed (verbatim record in `allocator_propagated_surface.py`); `ALLOC_PROPAGATED_SENSITIVITY_REPORT` non-empty is now `exit 2`.
> - **Live tests repointed rather than deleted**, because both assertions still pin live contracts: `test_production_weight_cache.py` carries the D5 damp-provenance delegation as a local shim (sweep OFF by default; sweep-off renders must record their fixed damp), and `test_format_menu_expansion.py` keeps the non-BF16 format-menu rule as a local helper — that rule is still what `run-pipeline.sh` computes for `CACHE_FORMATS`. `tools/smoke_graph_memory.py` stays live with only its L3 phase removed.
> - **Spine rewritten**: `CLAUDE.md` §3 is now one faithful cost + real-KL selection, with a "History: the retired cascade" subsection carrying the three measurements and pointing at the wall; the governing sentence is unchanged. `ARCHITECTURE.md` §2.2 aligned, §4.4 rewritten as the wall record, §11 gains the row, §3.5's gate table renumbered.
> - **One correction to this proposal's own accounting:** it estimated "~15 of 164 functions" of `kl_measurement.py` as L3. The measured answer is **97 of 164** and ~4.3k of 5.7k lines — the L3 half was much larger than the lens could see from the entrypoint names, because the lane-batched / override-set / paired KL engines and the whole tail-CUDA-graph cache hang off it.

### R17 (L6 P3) — `COST_MODE=production-render-staged` → the eighth `exit 2` gate · STRUCTURAL

**What.** Wall the staged path (`run-pipeline.sh:717-822`, ~109 shell lines; `select_tail_from_render_scores` + 8 CLI args, `production_render_cost.py:347-505`) and fail fast like the other seven, message carrying its own measurement. **Why now.** Its own result doc is a refusal: last-token-KL screen improved (0.0232 vs 0.0280), direct WikiText PPL **regressed 10.83 vs 8.33 — "Do not ship"** (`docs/results/production_render_staged_27b_results_2026-05-21.md`; HISTORICAL at `docs/README.md:86`). Untouched since `8e071bf`. The canonical instance of the failure the graveyard records, yet still a live, selectable, bare-skip-unguarded (§3.4) cost mode with a `PROMOTE_FRACTION` knob — inconsistent with grouped-kl being `exit 2` for the same class. **Cost** S. **Risk** none — no shipped artifact used it. **Verdict-request.** Does the graveyard convention apply to a mode that lost on PPL as much as one that lost a vLLM A/B?


> **OUTCOME — ACCEPTED and IMPLEMENTED 2026-07-30** (wave 2). Verdict on the request — **yes, the graveyard convention applies**: losing a direct PPL comparison in your own result doc is the same class as losing a vLLM A/B, and the gate text carries the numbers (0.0232 vs 0.0280 screen, 10.83 vs 8.33 PPL, "Do not ship").
> - `COST_MODE=production-render-staged|production-render-tail` is now the **eighth** `exit 2` gate; the catch-all reads *"COST_MODE must be local, production-render-score, or aura"*.
> - The 108-line `[2b/4]`…`[2e/4]` block, the three `PRODUCTION_RENDER_COST_*PROMOT*` knobs and `PRODUCTION_RENDER_COST_TAIL_QNAMES` are gone → `archive/production_render_staged_2026-07-30/run-pipeline-staged-block.sh`.
> - `production_render_cost.py`: **the default path is behaviourally byte-identical.** Removed only the staged-only surface — `select_tail_from_render_scores`, `load_qnames_file`, the three staged keyword parameters (`missing_render_score_policy`, `promotion_qnames`, `bf16_policy`) with their branches and `meta` keys, and **ten** CLI arguments (the proposal said 8; the exact list is in the wall README). `--require-render-scores` already covered the strict behaviour `--missing-render-score-policy=error` duplicated.
> - Two tests lifted to the wall as records; `docs/README.md`'s row now names the gate it feeds.

### R18 (L6 P4) — Delete `PRODUCTION_CACHE_UNION` and `MSE_PROMOTION` · STRUCTURAL

**What.** Wall `tools/build_union_cache.py` (282 L) + `tools/patch_union_from_pareto.py` (97 L, zero refs) + `run-pipeline.sh:180-186,1135-1150`; and `mse_promotion.py` (633 L) + `tools/build_mse_promotion_assignment.py` (144 L) + `tests/test_mse_promotion.py` (564 L) + `run-pipeline.sh:211-217,417-430,1292-1313`. **Why now.** Union cache: default 0, untouched since `7ac2d98` (2026-05-28), smoke doc filed HISTORICAL. MSE promotion: default 0, superseded by AURA-on-MoE (`docs/README.md:89`), gated behind three conditions with a documented silent-no-op outside them (CODEX M31), and backed by **564 lines of test** — the largest test file behind a default-off superseded lever. Together ~1.7k lines and two guard branches in the orchestrator's most delicate arm. **Cost** S each. **Risk** low; both stay reachable post-wall. **Verdict-request.** Approve both — or hold MSE promotion if any shipped run's artifacts carry a `layer_config_before_mse_promotion.json`. That ledger check is the one thing the lens could not close from the tree.


> **OUTCOME — BOTH ACCEPTED and IMPLEMENTED 2026-07-30** (wave 2). **The ledger check the lens could not close is CLEAR**: no shipped run directory carries a `layer_config_before_mse_promotion.json`, so no artifact rode MSE promotion. Both walled.
> - `archive/union_cache_2026-07-30/` — `tools/build_union_cache.py` + `tools/patch_union_from_pareto.py`. `PRODUCTION_CACHE_UNION` truthy is `exit 2`; the two defaults and the frontier-cache dispatch branch are gone. Lesson recorded: a render-budget percentile that pre-decides which Linears may be *offered* an FP8 rung is a constraint on the allocator (principle 1), not an optimization.
> - `archive/mse_promotion_2026-07-30/` — `prismaquant/mse_promotion.py`, `tools/build_mse_promotion_assignment.py`, `tests/test_mse_promotion.py`. `MSE_PROMOTION` truthy is `exit 2`; the seven shell defaults, the two-condition precondition gate and the stage-4/4-C′ rewrite are gone. Lesson recorded: a post-allocator rewrite ranked by a local surrogate cannot beat the allocator that already solved the global problem against a faithful cost — AURA put those bits inside the DP.
> - `tests/test_run_pipeline_defaults.py` gains `test_mse_promotion_is_archived_and_blocked` and `test_production_cache_union_is_archived_and_blocked` in place of the old inertness test.
> - Note: `mse_promotion.build_promotion_candidate_report` had one live consumer, `tools/sensitivity_propagated_group_report.py` — itself walled the same day under R4, so no live import edge was broken.

### R19 (L6 P5) — Wall 4 orphan modules + 8 orphan tools · STRUCTURAL

`cross_layer_residual.py` (360 L), `aura_additivity_gate.py` (138 L), `collapse_config_groups.py` (83 L, last touched 2026-04-22), `_fast_kernel_guard.py` (92 L — verify no dynamic import first); `tools/{analyze_damp_sweep_log, collect_col_importance, lift_gptq_ablation, measure_vllm_ppl_from_ids, patch_union_from_pareto, remeasure_cost_post_lever, sensitivity_coverage_summary, summarize_kl_sensitivity_response}.py` (~1.2k L). All have **zero** references across `prismaquant/ tools/ scripts/ examples/ tests/ docs/`; three belong to closed threads (damp-sweep OFF-final, xlayer null, AURA additivity cleared). **Cost** S, **risk** low. **Verdict-request.** Blanket orphan-walling, or file-by-file? — **and see the R2 disagreement: Lens 1 wants `aura_additivity_gate.py` wired, not walled.**

> **OUTCOME — ACCEPTED and IMPLEMENTED 2026-07-30** (wave 2), blanket-walled to `archive/orphans_2026-07-30/` **with one carve-out and one resolved disagreement**.
> - **`aura_additivity_gate.py` is NOT walled.** The R2-vs-R19 disagreement is settled in R2's favour: it stays live, to be wired as AURA's standing per-artifact trust-region readout in a later wave.
> - **`_fast_kernel_guard.py` — R19 said wall, the orphan report said REWIRE; resolved by doing both.** Walled (with `tests/test_fast_kernel_guard.py`) after verifying **no dynamic import** resolves it anywhere in the tree — its own `importlib.import_module` is the kernel *probe*, not an import of the guard, and its sole caller (`polish_from_assignment`) has been archived since 2026-05-15. The gap is **booked, not buried**: new `ARCHITECTURE.md` §12 row **D28** (LOW) records that serve-time fast-kernel enforcement has had no caller since 2026-05-15 and that **core principle 9's kernel-performance gate is manual today**. Whoever funds it should start by moving the file back — the mechanism is written and tested; only the call site is missing.
> - Also walled: `cross_layer_residual.py` (+ its test), `collapse_config_groups.py`, and the six remaining orphan tools (`analyze_damp_sweep_log`, `collect_col_importance`, `lift_gptq_ablation`, `measure_vllm_ppl_from_ids`, `remeasure_cost_post_lever`, `sensitivity_coverage_summary`). `patch_union_from_pareto.py` went to the R18 union wall instead, next to the tool it patches.
> - The banner groups them by closed thread with each thread's verdict, and carries the durable lesson: **a tool outlives the question it was built to answer, and there is no signal when that happens** — reachability is the only honest liveness signal, and it must be checked.

**L6 hygiene (→ R28).** **Phantom flags:** `PRISMAQUANT_FISHER_CAP_MULTIPLIER` is named in **CLAUDE.md:423** as the robust-Fisher-clip lever *pending promotion* — accurately labelled as unbuilt, but pending since 2026-05-19 with **zero code readers**; promote it or strike the entry. Same for `PRISMAQUANT_CB_SCALE_RECOMPOSE` (`two-tier-scale-spec.md:261`) + the two D18 entries. Implement or strike. (~15 other "zero-reader" flags are false positives — they live in `csrc/cb_gemv.cu` or are composed as `f"{name}_MIN_CALLS"`.) **S13–S16: the label is inverted, not the code.** §9.2 calls the signed CB rungs "research-only", but they are allow-listed in the **served** profile (`nvfp4_cb.json:30-33,93-96`), emitted (`export_nvfp4_cb.py:77,278`) and CUDA-served (`gb/linear.py:301`). **KEEP** — the only 2.125–2.5 bpw fp4 ladder — and fix the label. If they really are research-only they must come out of the served allow-list: the allocator can pick them today.

> **OUTCOME — R28 hygiene batch ACCEPTED and IMPLEMENTED 2026-07-30** (Robert). Five items:
> - **S13–S16 label** fixed in `ARCHITECTURE.md` §9.2 after re-verifying all three claims (served allow-list `serving_profile_specs/nvfp4_cb.json:30-33` + `:93-96`; emitted `export_nvfp4_cb.py:77,278`; CUDA-served as the `n_sub == 1` path in `plugins/gridbook/gridbook/linear.py:295-305`). Rungs **KEPT**, relabelled production. One correction to this audit's wording: they are *not* "the only 2.125–2.5 bpw fp4 ladder" — `NVFP4_CB_K13..K16` occupy the same nominal band at 0.125 bpw steps; the distinction is the codebook family (sign-magnitude), not the rate.
> - **UCB knobs** marked RESEARCH-ONLY in `runtime_flags.md:42-43` with the thin-calib regime named (z=2, −8.0%) against the production-calib decision (z=0, 6/252 churn, served parity), plus a §4.3 pointer. Confirmed no driver sets either (zero `UCB` occurrences in `run-pipeline.sh`; only test files set them). A stale cite in the row was corrected (`allocator_candidates.py:305` → `_cost_ucb_z` at `:450-453`).
> - **`PRISMAQUANT_FISHER_CAP_MULTIPLIER` — the "promote it or strike it" fork resolved by PROMOTING.** Implemented as an opt-in lever: `allocator.clip_probe_fisher_outliers` (`:1166-1280`), called at `:1579` immediately *after* `renormalize_probe_fisher` (it clips the finalized scalar, not the raw accumulator). Ports the reference tool's role regex verbatim, so MoE experts and sidecars are skipped exactly as in `/home/rob/dq-runs/robust_fisher_clip.py` and `h_trace_per_expert` is never touched. Unset ⇒ byte-identical no-op; a malformed value is a hard `SystemExit`, not a silent skip. 7 tests in `tests/test_fisher_normalization.py:390-495`. **Still research**: the 2026-05-19 ~5% PPL win at 6.0 bpp on 4B is a small-scale screen with no served A/B — `CLAUDE.md` now says "wired, still research" rather than "pending promotion".
> - **`PRISMAQUANT_CB_SCALE_RECOMPOSE`** annotated NOT IMPLEMENTED at `two-tier-scale-spec.md:259-265` (zero readers tree-wide) rather than struck — it is the M0 bullet of a forward-looking spec, so marking it unbuilt is the honest fix; deliberately absent from `runtime_flags.md`.
> - **D21 resolved.** Canonical id is `rdtand/Hy3-295B-A21B-prismaquant-gridbook-2.9bit-vllm`; **both** older ids 307-redirect to it, so the register's premise ("at most one is live") was wrong — they are renames, not rivals. `prod_hy3_results.md:248-251,313-320` annotated in place (history preserved), `ARCHITECTURE.md` §9.2 + §12 D21 flipped to RESOLVED. `scratch/gridbook-launch-post.md` still carries a third variant; left alone as out-of-scope.
>
> Suite: 99 passed across `test_fisher_normalization` / `test_architecture_doc` / `test_docs_staleness` / three allocator files / `test_aura_cost`.
>
> **Wave 4 addendum (2026-07-30) — the L4 hygiene half closed too.** **D15** is done, taking the
> approved option rather than defending the conservative default: `CB_SCALE_CODING` → `two_tier`
> (v2 shipped in the Hy3 295B and Laguna-S-2.1 artifacts and `STANDARDS.md` calls it the
> production fp4 scale coding; the "serve gates pending, do NOT ship" comment predated its own
> ship, and the knob is inert on fp8-CB-only menus) and `CB_EXPERT_EMPIRICAL` → `0` (the value
> every shipped MoE CB driver sets). Pinned by
> `tests/test_architecture_doc.py::test_cb_defaults_match_the_shipped_drivers`, which asserts the
> shell default *against the drivers themselves* so the pair cannot drift apart again. No shipped
> run changes: every driver sets both explicitly. **D14** is also closed — the gridbook README
> rewrite landed earlier the same day (delegation table, MoE section, version block); none of the
> four wrong claims survives.


---

## Explicitly not proposed

Null findings and buried ideas the panel declined to revive. Load-bearing for trust.

- **No cross-layer cost-modelling revival.** L2 cascade beats L1 by −1.5% while AURA beats it −38.5%; cross-layer residual +5–12% diffuse, 3/1180 pairs significant. Nothing to capture. (L1, L6)
- **No `pipeline.py`-as-executor / no python port of `run-pipeline.sh`** — recorded as an explicit "no" with its cost (L, 1.5–2 weeks + a byte-identical 27B re-validation) so it is not re-litigated (R23). (L2)
- **No served A/B on the CB ladder law** — it is per-tensor gated with a measurement fallback; the A/B would measure nothing (R20). (L1)
- **No gold-lane A/B on block-output-match (D16)** — the branch is unreachable on the shipping recipe; the A/B would cost a 27B export cycle to measure a `continue` (R25). (L5)
- **No λ (time term) in the allocator objective yet** — it would optimize against a cost table that does not exist, on a non-default lane, against a tax that moved 12×→1.75× in a week: a heuristic in an objective's clothes (R21). (L4)
- **No unconditional promotion of `validated-surrogate`** — ~2× render plus 11 KL evals to decide a bpp that a byte budget fixes for free (R1). (L1)
- **No collapsing of the 7 archived-lever `exit 2` gates** — 4 lines each, each carrying the measurement that killed the lever, load-bearing on `archive/*` names (§3.5). Leave them. (L2)
- **No auto-running the ship gate inside `run-pipeline.sh`** — §3.2's "nothing validates the artifact" is a physical lane boundary (`vllm` not importable in the build venv), not laziness; embedding a docker serve would make the build tool own the serving stack. The shipcard (R13) is the answer. (L2, L5)
- **No `pipeline.py` modelling of the 10 executed-but-unmodelled stages** — fiction-surface without teeth; R5's narrow job is the whole value. (L2)
- **KEEP the research formats.** `NVFP4A16 / MXFP8A16 / INT8_W8A16 / MXFP6_* / *_E5M2` are registry declarations plus explicit `deny_formats` rules. Those rules *are* the institutional memory ("no vLLM kernel", "E5M2 is kv-cache-only"), and the exporter hard-fails rather than silently BF16-coercing (`nvfp4_cb.json:11`). Deleting them deletes the refusal, not the format. (L6)
- **KEEP `l2_pipeline` (gridbook)** as DIAGNOSTIC-ONLY: 211 L + 370 L test, already excluded from `auto` after wedging live serving 3× (`afc64ec`). It is the record of a negative result on the hottest open perf question (MoE prefill); its guard is one `if`. (L6)
- **KEEP `vllm_qwen3_5_packed_moe.json`** — 6 lines, pure `extends`, three live references (`.github/scripts/check_installed.py:27`, `test_serving_profiles.py:54`, `test_allocator_packed_group_units.py:661`) + unknown shipped-metadata exposure. Cost to keep ≈ 0; cost to delete = a metadata audit. (L6)
- **KEEP `_RD_LOG_LINEAR_R2_THRESHOLD`** — honestly labelled a diagnostic threshold (`allocator.py:322-326`), not a heuristic in the objective. (L1)
- **KEEP `validation_harness` non-blocking** — it is a *measurement* registry, not a gate; the shipcard should be the thing that refuses. (L5)
- **No fundamental capability refused.** The format menu, the plugin registries, the one-cache rule, the CB/GGUF lanes and the AURA cost all earn their size. R4+R17+R18+R19 remove ~7k live lines and one whole conceptual level with zero effect on any shipped artifact. (L6)

## Open disagreements for Robert

1. ~~**R5**~~ — **RESOLVED 2026-07-30 for Lens 2's narrow promotion** (implemented; see the R5 outcome). `pipeline.py` is the settings-hash authority and nothing else; it is still not the executor.
2. ~~**R2 vs R19**~~ — **RESOLVED 2026-07-30 in favour of R2 (wave 4).** `aura_additivity_gate.py` is wired as AURA's standing trust-region readout: stage `[3c]`, non-blocking, stamped into `cost.pkl` provenance. Not walled.
3. ~~**R3**~~ — **RESOLVED 2026-07-30 (wave 4): Milestone C funded, default stays native, AURA-on-CB landed opt-in.** See the R3 outcome.
4. ~~**R8**~~ — **RESOLVED 2026-07-30 (wave 1): the reader was implemented**, not the field deleted (`SpecMatch` + `SpecMatchProfile`; see the R8 outcome). No `match` block is unread.

**Nothing on this list is open.** Every R1–R28 carries an Outcome line; the four items above are recorded resolved rather than deleted so a reader returning with a stale copy sees the resolution. What remains for Robert is deliberately *not* filed as a disagreement — it is a set of default flips and one measurement, each already implemented and shipped OFF: the R9 tail-veto's contract statistic and default-on flip; whether R16's lane gates become **blocking**; and the served CB objective A/B that would justify a non-`local` cost objective as a CB default (R3).

---

*Standing debt register: `docs/ARCHITECTURE.md` §12 (D1–D27). Mapping: R1→D12, R2→D6, R5→D6/D10, R9→D1, R10→D3, R11→D4, R12→D2, R16→D26, R24→D8/D9, R25→D16, R26→D11 residual, R27→D13, R28→D14/D15/D18/D21 (D27 closed under R10); R4 is the spine framing itself.*

*This document is a **decision record**. It changes nothing on its own. Once Robert rules, each `R<n>` gets its outcome recorded inline here (ACCEPTED / REJECTED / DEFERRED, with the reason) and accepted items move to issues/PRs. The graveyard convention applies: a rejected proposal keeps its evidence so it is not re-proposed without new measurement.*
