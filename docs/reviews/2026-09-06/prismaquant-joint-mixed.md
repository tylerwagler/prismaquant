# PrismaQuant joint AURA and mixed-LFM review

Reviewed `cb517da..c488b592`, using current source in `/home/rob/tmp/astra-pq-review` and an isolated repair worktree `/home/rob/tmp/astra-pq-joint`. Scope: PRs #250, #254, #256, #259, #260, #263, #264, #265, #266, #269, #270, #271, #274, #277, #279, #281. Root/math reviewers own the subsequent sampled-stack, menu and fanout changes (#286/#287/#289/#292). This report does not certify those changes.

## Findings and delivery

1. **P2, confirmed and fixed: expert projection misses a valid common family when stack menus differ.** At base `prismaquant/tessera_campaign.py:2511-2513`, the fallback applies the same menu index to every stack. Actual producer reproduction with `[E2M1x2,E4M3]` and `[E4M3,BF16]` fails both attempts although all-E4M3 is legal. The first refusal names stack A/NVFP4; the second names stack B/BF16. [Issue #295](https://github.com/RobTand/prismaquant/issues/295), [PR #297](https://github.com/RobTand/prismaquant/pull/297). Fix commit **`b3e9e4ec`** preserves all existing nominal plans and adds missing common-family plans matched by name. Requests remain whole-population and bounded by twice the largest distinct-family menu, with unchanged producer projection/source-byte checks. It does not Cartesian-enumerate assignments or introduce per-stack checkpoint hashing. The two-stack regression uses the actual producer, not mocked route admission. RED `11355529964f…`: one intended failure. GREEN `72ae634c6092…`: 2 projection tests passed; docs `bdd55f472f52…`: 19 passed; compile `304c94bedbe9…`: 2 modules. No skips. Actual terminal/worker/CAS records verified. Full keys and receipts are in `pq-joint-verified-terminals.json` and committed `docs/measurements/pq295-projection-family-review-2026-09-06.md`.

2. **P2, confirmed and handed to triage: streamed source manifest is not a closed source set.** `experiments/pq237_joint_aura_streamed.py:35-45` verifies only listed entries; `experiments/pq237_source_manifest.py:18-24` only hashes tracked files. An added package can shadow a sealed same-name module while verification succeeds. Standard Python's actual `PathFinder` confirms shadowing; PB RED `4779a9211c70…` fails the expected refusal assertion. [Issue #296](https://github.com/RobTand/prismaquant/issues/296), offpath handoff to triage-bot/claude-triage, no source change. Reproducer retained as `test_pq237_source_closure_review.py` in this report directory. This is a provenance guard defect, **not evidence that any stored numerical run was modified**. Stored joint and mixed archives were separately rehashed successfully.

3. **P3, fixed on sight in a separate documentation commit:** `experiments/pq237_joint_aura_streamed.md` unconditionally called `readlink()` on the three calibration links removed by #269, preventing its provided launch example from reaching submission on current main. Commit **`dccc03f7`** checks `is_symlink()` before reading/temporarily omitting the links and explains the removed-link case. Included as the separately identified second commit of PR #297; no model rerun warranted.

No wrong materialized artifact or bypassed mixed-LFM production-admission gate was established in this review. No merge was performed. PR #297 is open and mergeable; linked-issue and import checks had passed, broad CPU CI was still running at the last inspection.

## Independent checks of the actual mixed-LFM result

Source archive: `/mnt/shared/tessera-measurements/mixed-lfm-237-2026-09-06/serve-03/`.

- All **34** files named by `archive-receipt.json` were reread and rehashed; **zero mismatches**.
- The actual assembled checkpoint `model-export-02` was independently reread against `artifact-seal.json`: **33 sealed files, 4,834,455,492 bytes, zero mismatches**. Detailed fresh hashes: `pq253-independent-checkpoint-rehash.json`.
- The actual census and checker report **74 owners / 2,214 projection units**. The extra projection units are the row-partitioned ShortConv roles; they are not extra logical body matrices. The plan remains 2,178 logical body matrices, 22 routed E4M3 stacks, six E2M1x2 dense source tensors/four fused owners, and the other dense body weights on the BF16 trellis grid.
- Both prefill and decode have **22/22 routed owners covered by cells, 0/52 dense owners covered** in the EUGR image. `observed-check` exited 0; `strict-check` exited **1**, with the expected dense attestation refusal. This is an **observed eager TP=1 resident sanity result, not production admission**.
- Every other recorded phase and all three container launchers exited 0. Exact owned-container cleanup records finish absent, `cleanup_errors=[]`, both-host Netdata was recorded, `telemetry_errors=[]`. The serve was explicitly direct under the vLLM exemption; it does not pretend to carry a PB action/CAS receipt. Its own archived output seal is the relevant direct-service evidence.
- BF16/student before/after identity files are byte-identical. Student build identity records the exact EUGR image `0afec8d4…`, vLLM `0.28.1rc1.dev397+gfd4a15126.d20260904`, eager=true, compiled_forward=false, resident mode, serving source `d19e3ad…`.
- The paired smoke has **six repetitive raw completions on both arms**, two further raw pairs where BF16 is repetitive and Tessera recorded, and **six chat-template pairs recorded on both**. “Recorded” means the bounded repetition check; it is not task accuracy, NLL/PPL or an absence of quality regression. The actual contract classifies the observed repetition as shared with the reference.
- The actual preparation seal's **eight files** also rehash cleanly. Each of the six calibrated FP4 inputs has **16,384 observed rows**; no Hessians or expert observations are invented. The native prerequisite record has ten finite/shape calls and explicitly no runtime-price claim. Its original host action failed after child exit zero; the separate archived recovery says cleanup complete and GPU work not repeated. That ABI result never substitutes for the dense serving-cell gate.

Thus #253's bounded full-mixed sanity observation is supported. Promoting it to full quality, efficient-kernel, general serving, H-aware allocation or latency qualification is unsupported.

## Independent checks of the joint and prior bounded results

`pq261-2026-09-06/joint-streamed-run-03`: all **34** receipt-listed artifacts rehashed cleanly. PB terminal **`b48092a8e470c938acf9b73142f3c58679dbcd2bc182514556c7feaf4223f2b7`** is executed/exit 0; actual successful CAS payload was read and its hash checked. Teacher parity is bit-exact, checkpoint reload equal, all five production renders retained, resident bytes 31,457,280 with zero misses, and direct-oracle absolute errors are below **1.83e-7**. Candidate handoff retains five complete joint-currency rows over three dense units.

This is the historical **E2M1 K1 and preclipped FP8 QDQ panel**, with 128 probes, 2 calibration sequences, 4 historical holdout sequences and 32 fresh fixed-panel sequences, length 64. It does not qualify the served K2/unclipped-FP8 operators or packed routed-expert joint AURA. The additive A4-minus-A16 price is about +0.000443 on either background; fresh-panel measured KL contrasts are +0.000171 ± descriptive SE 0.000248 on A16 background, and −0.000207 ±0.000402 on A8 background. The background interaction −0.000378 ±0.000350 is inconclusive at this scale. These are fixed-panel descriptive errors, not population confidence intervals. Oracle fidelity establishes the measured residual implementation, not generalization of additive prices.

Prior `prismaquant-183-2026-09-05/run183-09`: actual terminal **`b25114fe9341bf4408a83e6a17aa5f2511c08cb5a17bc2ac210b8a49d4b823f9`** is executed/exit 0; CAS rehashed. Six phases are recorded exit 0. Independently read the safetensors container framing and embedded payloads: **96 container hashes and 96 blob hashes match**, embedded blobs total **178,161,760 bytes**. Details: `pq183-independent-wire-rehash.json`. Census is genuinely strict/attested for its **one routed stack**, both prefill/decode. This remains the earlier one-stack handoff, not a full mixed-model result.

## Per-change coverage

| PR | Reviewed surface and disposition |
| --- | --- |
| #250 | `pq183_lfm_bound.py`, `pq183_direct_export.py`: immutable continuation inputs, producer-owned source projection, direct-export metadata supplementation, shard readability, independent written-wire audit and actual stored terminal/census/smoke. Bounded claim supported as above. |
| #254 | Base-only pinned-action `pull_request_target` workflow and `linked_issue.cjs`: same-repository native closing references, actual head status, API/malformed fail-closed behavior. No new defect found by source review; no new workflow execution claimed. |
| #256 | `_collect_activations` increments observed rows outside the Hessian branch. Existing full-Gram accumulation remains independent. Actual mixed preparation confirms six no-H counts; no new defect found. |
| #259 | Complete plan/retained-source roster, existing dense calibration/static-scale policy, sealed inputs, native host/ABI wrapper and cleanup recovery. Eight preparation hashes and ten-call ABI record inspected. Scope is weights-only preparation and arithmetic prerequisite. |
| #260 | Joint row validation, assignment/source/probe binding, covariance labels, stable cancellation arithmetic, separate probe/sequence uncertainty and invalid measured-KL/SE rejection. Source and experiment usage checked; no new numerical defect established. |
| #263 | Frozen protocol, actual streamed teacher parity, cache, retained-wire decode agreement, checkpoint/pickle/candidate handoff and fixed-teacher comparisons. Actual run verified. Source closure defect #296 and stale launch example identified. |
| #264 | Geometry-derived test fixture includes `wire.span` and tuple arity, retains default trellis history and real render/export comparisons. Exact merge main CI run 34048063902 is recorded successful on 1edd0e55; source correction is sound. I did not independently remeasure the claimed fixture speedup. |
| #265 | Full mixed wrapper binds actual PB stdout result, artifact/source/plan/scales, runtime build identity, census, separate strict refusal, paired smoke and immutable-ID cleanup. Actual serve03 and current checkpoint hashes verified as above. |
| #266 | Shared static-max predicate and complete planned sibling scope inspected. Corrected GPU proof remains unresolved in checked-in evidence; see below. No measured numerical regression alleged. |
| #269 | Removed only three unused absolute calibration links. No application readers found in prior disposition; direct consequence on the later published launch example fixed separately. |
| #270 | Causal refill test; owner observation distinguishes failed reads from positively different owner; preserved start-time identity; already-ended termination succeeds; terminal report includes reason. Initial narrow fix was superseded. Exact merge CI 34054952697 at 78c02cd0 is successful: **5753 passed, 154 skipped, 3 xfailed, 184 subtests** from raw log. The five quoted PB follow-ups themselves failed infra/lease admission and are not passing evidence. No attribution to the original two unlogged CI failures is inferred. |
| #271 | Recorder keeps the current unit's Hessian-bearing source rather than a later identity-probe source. Actual run03 progresses beyond prior run01 refusal. |
| #274 | Stream accumulator and persisted joint row share multiply-based squaring. Actual run03 reload succeeds; validator tolerance is distinct from exact persisted list equality. |
| #277 | Layer-type resolver consults LFM `conv` child for layer index/config after existing sources; retains unresolved fail-closed behavior. No regression identified in cited source. |
| #279 | Count/log fix uses actual producer eligibility rather than length of explicit caller menu; research admission labeling kept separate. Later readable-menu implementation belongs to root's excluded review scope. |
| #281 | Actual two-stack regression confirmed and fixed in PR297; source/fixture/evidence detailed above. |

## Remaining primary acceptance

- **#237 remains open.** The software and narrow QDQ/solver handoff do not supply sparse-anchor generalization across held-out units/rungs, joint-currency interpolation/crossover evidence, packed-expert directional joint AURA, actual current served operator prices with complete scratch/resident/fixed-cost scope, end-to-end TTFT/decode SLO measurements, or uniform/downstream model qualification. The regret report's lowest-budget zero has only one feasible assignment; the DP reaches two assignments after dominance, not all four measured assignments. Do not read zero regret as broad ranking success.
- **#267 native receipt adapter** is a separate producer/consumer capability task; no implementation is present in this reviewed pushed range. Partial timing evidence must not become a full runtime row by inventing scratch/fixed-resource zeros.
- **#275** still lacks a completed LFM leave-one-out table/refusal diagnosis, informative exact-byte interpolated-vs-measured regret and served-KL allocation checks. The incomplete 552-anchor campaign has no completed `cost.pkl`; measured-only three-arm regret 1.0 is a construction. Root/math own the subsequent fanout/sampled-stack repair and should update this disposition against its actual results.
- **#266 current checked-in GPU evidence:** a8430eb1534c was withdrawn with attempts 0 as superseded by merged-source validation. 367511b94933 (parity) and 60172adc failed rc126 before tests, Docker mount source mkdir denied. No numerical failure or GPU pass follows. An evidence query was sent to claude-triage and then current triage-bot for replacement merged-source receipts; none received at report preparation. Avoid reusing the withdrawn action as proof.
- **#270 repeat-rate A/B** named in the PR remained a non-result in its listed PB actions (lease loss/rc125, empty stdout). Successful later main CI covers the changed software suite, but does not supply the proposed 120-run rate comparison. The real-process 0/240 corrected-rule reachability result and inability to attribute the original unlogged CI cases remain properly distinct.

No new performance comparison was run, and no speed/bottleneck claim is made. Older NFS timing commentary is historical; the new dedicated 100GbE/RoCE topology was not treated as a continuation of the old storage measurements.
