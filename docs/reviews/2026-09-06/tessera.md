# Tessera daily review — 2026-09-06

Reviewed source: `76ea29c..f8b87ce4` in isolated worktree `/home/rob/tmp/tessera-astra-daily` (`review/astra-daily-20260906`). The primary checkout was left untouched. No merge performed. PR386 and its host-sync follow-up remain with the encoder reviewer.

## Coverage and conclusions

- PR365: inspected snapshot subject/schema recognition and existing sealed action/owner/closure/fingerprint checks in `_dev/suite_source.py`, including v2/unknown-version tests. No new defect established. Independently read both worker terminal records, summary lines and CAS result payloads: CPU 3030 passed / 544 skipped; GPU 3591 passed / 10 skipped / 1 xfailed. Populations record matching effective source, 485 GPU-allocating tests, and no uncollected modules. This is evidence for that earlier source, not a current-master integration claim.
- PR367: inspected full resolver diff, traversed symlink collection, empty-glob-base handling, and non-Python text fallback; read new selector/resolver regressions. No defect established in the changed logic. Text fallback remains conservative; this review is not a general proof of all Python import/data-flow analysis.
- PR369: inspected exact sorted-neighbor distance search. For finite sorted grid values, the closest scalar is at the insertion point or predecessor; clamping the ends preserves the minimum. The rest of the objective/reduction/tie selection is unchanged. Read actual before/after batch JSON and verified all 13 fixture hashes, eight artifact hashes and final identity agree. Both principal CPU measurement terminal exits and CAS payloads independently verify. Profile/Netdata are scoped CPU-startup evidence; no fresh GPU throughput or work-per-joule result is inferred.
- PR370: inspected factored CHANNEL difference, its binary cancellation witness, new portable identity witness and legacy/corrected fixture separation. The factored difference preserves the same quadratic objective without separately subtracting large rounded losses. Read actual corrected-selection terminals/CAS: x86 1237 passed /159 skipped; strict-CUDA GB10 1407 passed /4 skipped. Existing report explicitly retains the earlier red full integration and the slicing fixture's small final SSE increase; targeted green is not a rewritten full green. No new defect established.
- PR368/371: assignment shorthand, producer-owned expert stacks, router handling, header-only dense translation, and atomic main/twin shard publication reviewed; geometry/census detail delegated independently below.
- PR373/378/382: dense and routed population, attested partitions, row-sliced roles and source-tensor census counting reviewed independently by dense reviewer. One confirmed implicit-fallback defect is recorded below; explicit campaign plans are unaffected.
- PR380: inspected immutable container IDs, cidfile startup-failure handling, cleanup signal path, existing-name refusal and CPU-affinity/thread arguments. No new defect established; SIGKILL remains a documented limitation.
- PR383: inspected fixture reduction, per-plane byte inventory and shipping-refit control assertions and reviewed the recorded measurement claims. This reduces test work and strengthens byte inventory checks; it is not production encoder performance evidence. No fresh timing rerun performed.

## Native dense operator receipt status (#376 / PQ#267)

The source is not on current remote master or any visible remote branch. `7476b34` and `11cf22b` named in #376 are PB snapshot IDs, not evidence of a pushed implementation branch. The latter was recovered from its exact CAS Git bundle into `/home/rob/tmp/tessera-native376-review` for read-only inspection. Input SHA-256 `78fdb135dd7badfec3613ec0c926f8826bffdfa850ccf300a24d68f1380e67f4`, 7,587,362 bytes, independently rehashed; snapshot `11cf22b246cd0fb056e916540e63a5a9f99ef528`, original parent `461adfae6297b479afed8b54a99a3c1ec96bb64b`.

Actual latest terminal action `8abde81d3eb7c2140fcaea5c3384e3cfb58f30ebf17dd3fc6efb2b525d493801` failed, exit 1, **39 passed / 10 failed**, no CUDA/skips/uncollected modules. It has no passing result CAS receipt. Most failures occur at `experiments/bench_native_operator.py:380`, `KeyError: source_weight`: the fake lifecycle fixture lacks newly required source/render/activation bindings, whereas real preparation populates source_weight at line357. The snapshot is unfinished groundwork, not delivered producer qualification.

Recovered source inspection covers original cached-wire verification, independent source/render fingerprints, actual create/load/process lifecycle, QDQ and complete-output gates before timings, raw per-apply CUDA event samples, observed route checks and explicit incomplete resource scopes. No GPU result was found or inferred. Do not wire PQ#267 to guessed schema fields or call this a usable pricing table merely because the snapshot exists. Publishing and completing this branch remains required before its consumer can be qualified.

## Independently checked evidence

`tessera-terminal-audit.json` records thirteen sampled terminal paths/statuses and twelve independently rehashed CAS result payloads (snapshot-v2 CPU/GPU, identity-startup before/after, CHANNEL correction CPU/GPU, and failed native376 iteration). These are historical evidence checks, not newly executed test populations. No baseline suite was rerun.


## Filed finding and disposition

**P1 [Tessera #387](https://github.com/RobTand/tessera/issues/387): implicit geometry fallback can publish an unloadable empty Tessera checkpoint.** At `f8b87ce4:experiments/export_tessera_serving.py:1756`, deleting the last owner has no following non-empty-plan check. With a synthetic valid total shape 96x32 and a mocked attested partition list `[16,16,64]`, the actual exporter falls back all roles, returns normally and publishes `quant_method=tessera`, `config_groups={}`, zero units/quantized parameters and a 6,280-byte passthrough checkpoint. `src/tessera/serving/config.py:302-305` rejects empty groups. The declaration/mock scopes the reproduction: this is not a served actual-model failure. Explicitly named plan tensors refuse at exporter lines1748-1753, so the active explicit-plan campaign is unaffected.

CPU reproduction through PB action `b1d20b33e870ba5602bc59a27e9d645807674576465532615b333068e3996ec5`, dl380g10, one CPU/4GiB/native threads1, no GPU. Worker exit1 is the regression assertion after successful export; no passing CAS output is claimed. Exact sealed snapshot input SHA `0f22c6219a9f9f9715e5ddb30af72fb52a0a9d3e090ae1bf7eb7b7cd4143cde9`, parent `f8b87ce4f2fa1273b95127f3f26a02facb623c55`. Reproducer: `tessera-empty-geometry-repro.py`; sanitized request/terminal evidence: `tessera-empty-geometry-evidence.json`; submitted body: `tessera-empty-geometry-issue.md`. Parent independently inspected the source lines, reproducer and actual terminal text. Per the user's offpath triage instruction, no fix was made; issue labeled existing `bug`, `P1`, `serving` (repository has no `claude-triage` label).

Native376 recovered-failure status was posted on the existing issue rather than spawning a duplicate: https://github.com/RobTand/tessera/issues/376#issuecomment-5564297067 . The recovered CAS bundle is retained as bounded review/recovery evidence, not integrated code.

The additional historical terminal/CAS checks cover PR380's 10 passing CPU container regression tests, PR383's six passing CPU ladder tests, PR382's 31 passing CPU census/partition tests, PR373's 94 passing/three CUDA-skipped CPU checks, PR371's 72 passing GPU checks, and PR368's 76 passing/three box-artifact-skipped CPU checks. All six sampled terminal statuses are executed/exit0 and their actual CAS output bytes rehash correctly. This sampled evidence does not replace current-merge integration or actual serving qualification.

Final Git status: review worktree clean at f8b87ce4; no source commits, pushes or merges made by this review. No second master baseline or GPU benchmark was launched.
